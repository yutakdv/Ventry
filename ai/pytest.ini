[pytest]
pythonpath = .
testpaths = tests
python_files = test_*.py
# -rs: skip 사유를 요약에 출력한다. 데이터 의존 테스트는 data/ 부재 시 자동 skip 되는데,
# 그 침묵이 곧 게이트 공백이다 — 있어야 할 테스트가 통째로 안 돌아도 CI 로그는 초록 한 줄뿐이었다
# (QA 리뷰 P2). 무엇이 왜 건너뛰어졌는지가 로그에 남아야 공백을 눈으로 셀 수 있다.
addopts = -rs
