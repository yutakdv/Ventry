# AI-07 평가 하네스 골드셋 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** AI-07 평가 하네스의 AI 기능 3스위트(추출·근거·민감도)와 골드 데이터를 구현해 `make eval`이 `metrics.json` + 성적표 PNG를 산출한다.

**Architecture:** `ai/eval/`에 읽기 전용 파이썬 하네스를 채운다. `common.py`가 적재 산출물(extracted.json·source txt·10_data_core.sql)과 골드 파일을 로드하고, 스위트 3종이 순수 함수로 지표를 계산하며, `report.py`가 `metrics.json`+PNG로 조립한다. 매칭은 BE 소관, 모델은 AI-08 소관이라 디스패처가 안내 후 통과시킨다.

**Tech Stack:** Python 3.11, pandas·matplotlib(기존 requirements), pytest, ruff.

## Global Constraints

- Python 3.11 / `cd ai && ruff check .` 무경고 (CI 동일).
- **읽기 전용**: 서비스 파이프라인·DB 스키마·API 계약 무변경 (스펙 §12-1).
- **수치 생성 금지**: 확정골드·근거 인용 값은 원문 PDF/txt에서 전사 (스펙 §0-1).
- **용어 컴플라이언스**: 작성 텍스트·라벨에 "승인/추천/권장" 금지 (CLAUDE.md §3).
- 금액 만원 단위 정수, 데이터 기준일 표기.
- **결정적 도구 재구현 금지**: 매칭(EligibilityFilter)은 BE 소관.
- 커밋: `[AI] type: 요약`, Claude attribution 금지. 토픽 브랜치 `ai07-eval` (이미 존재).
- 파라미터·정규화 규칙은 `docs/assumptions.md`에 등재.
- 실행 위치는 `ai/`, 모듈 실행 `python -m eval.run`, 테스트 `cd ai && pytest`.

**데이터 사실(고정):** 추출 초안 `ai/data/finance/extracted.json` = 29상품(KB 4·서울신보 8·소진공 17), `amount_max` 결측 8건. 적재 `finance_product` = 21상품. 근거 청크 = 6(전부 소진공 클린). 원문 PDF 12건 `ai/data/raw/*.pdf`, 클린 txt `ai/data/interim/funding_docs/*.txt`(소진공 클린·서울신보/KB 일부 CID깨짐). 서빙 점수 `db/init/10_data_core.sql`의 `location_score`(w1~w5·est_sales) + `rent`(monthly_rent), v_candidate_area 2,500행(cafe 1,061·food 1,439).

---

### Task 1: `common.py` — 공용 로더·정규화·SQL 파서

**Files:**
- Create: `ai/eval/common.py`
- Create: `ai/eval/suites/__init__.py` (빈 파일)
- Create: `ai/eval/.gitignore` (내용: `out/`)
- Test: `ai/tests/test_eval_common.py`

**Interfaces:**
- Produces:
  - `PRODUCT_KEYS: tuple[str, ...]`
  - `load_extraction_draft() -> list[dict]`
  - `load_confirmed_gold() -> list[dict]`
  - `load_source_text(doc: str) -> str`
  - `norm_field(key: str, value) -> object` (비교 정규화)
  - `load_serving_scores(sql_path: Path = DATA_CORE_SQL) -> list[dict]` — 각 dict: `area_code, industry, w1..w5, est_sales, monthly_rent, burden_ratio`
  - `is_clean_source(doc: str) -> bool`
  - 경로 상수 `AI_ROOT, REPO_ROOT, FINANCE_DIR, FUNDING_DOCS, GOLD_DIR, DATA_CORE_SQL, FINANCE_SQL`

- [ ] **Step 1: 실패하는 테스트 작성** — `ai/tests/test_eval_common.py`

```python
from pathlib import Path

from eval import common


def test_norm_field_arrays_and_nulls():
    assert common.norm_field("regions", None) == frozenset()
    assert common.norm_field("regions", []) == frozenset()
    assert common.norm_field("regions", ["서울"]) == frozenset({"서울"})
    assert common.norm_field("regions", "['서울']") == frozenset({"서울"})
    assert common.norm_field("rate", None) is None
    assert common.norm_field("rate", "2.1") == 2.1
    assert common.norm_field("amount_max", "") is None
    assert common.norm_field("amount_max", "7000") == 7000
    assert common.norm_field("pre_startup_only", "True") is True
    assert common.norm_field("name", "  KB소상공인  ") == "KB소상공인"


def test_load_serving_scores_parses_sql(tmp_path: Path):
    sql = tmp_path / "core.sql"
    sql.write_text(
        "INSERT INTO rent (area_code, quarter, monthly_rent, x) VALUES\n"
        "('3110002', '20261', 400, NULL);\n"
        "INSERT INTO location_score (area_code, industry, w1, w2, w3, w4, w5, "
        "est_sales, daily_floating, based_on_quarter) VALUES\n"
        "('3110002', 'cafe', 0.3, 0.6, 0.7, 0.4, 0.5, 800, 620376, '20261');\n",
        encoding="utf-8",
    )
    rows = common.load_serving_scores(sql)
    assert len(rows) == 1
    r = rows[0]
    assert r["area_code"] == "3110002" and r["industry"] == "cafe"
    assert r["w1"] == 0.3 and r["w5"] == 0.5 and r["est_sales"] == 800
    assert r["monthly_rent"] == 400
    assert abs(r["burden_ratio"] - 0.5) < 1e-9


def test_load_extraction_draft_real():
    draft = common.load_extraction_draft()
    assert len(draft) == 29
    assert all("doc" in p and "name" in p for p in draft)
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd ai && python -m pytest tests/test_eval_common.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'eval.common'`

- [ ] **Step 3: `common.py` 구현**

```python
"""AI-07 평가 하네스 공용 로더·정규화·SQL 파서 (스펙 §12-1). 읽기 전용."""
from __future__ import annotations

import csv
import json
import re
from pathlib import Path

AI_ROOT = Path(__file__).resolve().parents[1]           # ai/
REPO_ROOT = AI_ROOT.parent
FINANCE_DIR = AI_ROOT / "data" / "finance"
FUNDING_DOCS = AI_ROOT / "data" / "interim" / "funding_docs"
GOLD_DIR = Path(__file__).resolve().parent / "gold"
DATA_CORE_SQL = REPO_ROOT / "db" / "init" / "10_data_core.sql"
FINANCE_SQL = REPO_ROOT / "db" / "init" / "20_finance.sql"

# finance_product 검수 필드 (extract/funding_llm.py 와 동일 순서)
PRODUCT_KEYS = (
    "name", "org", "product_type", "max_age", "industries", "regions",
    "pre_startup_only", "amount_max", "rate", "rate_note", "term_months",
    "exclusive_group", "status", "notice_date", "source_url",
)

_TOKEN = re.compile(r"[\w가-힣]+")


def load_extraction_draft() -> list[dict]:
    return json.loads((FINANCE_DIR / "extracted.json").read_text(encoding="utf-8"))


def load_confirmed_gold() -> list[dict]:
    return json.loads((GOLD_DIR / "extraction_confirmed.json").read_text(encoding="utf-8"))


def load_source_text(doc: str) -> str:
    path = FUNDING_DOCS / f"{doc}.txt"
    return path.read_text(encoding="utf-8") if path.exists() else ""


def is_clean_source(doc: str) -> bool:
    """근거 청크 가능 문서 판정 (load/finance.py _is_clean 미러)."""
    text = load_source_text(doc)
    if not text:
        return False
    keywords = ("대출", "융자", "한도", "금리", "보증", "지원", "소상공인", "상환", "기업")
    return sum(text.count(k) for k in keywords) / len(text) * 1000 >= 3.0


def norm_field(key: str, value):
    """초안↔확정 비교용 정규화 — 배열은 집합·null==빈배열·문자열 공백정리·수치 캐스팅."""
    if key in ("industries", "regions"):
        if value in (None, "", "[]", "NULL"):
            return frozenset()
        if isinstance(value, str):
            return frozenset(_TOKEN.findall(value))
        return frozenset(value)
    if key == "rate":
        return None if value in (None, "", "NULL") else round(float(value), 3)
    if key in ("max_age", "amount_max", "term_months"):
        return None if value in (None, "", "NULL") else int(value)
    if key == "pre_startup_only":
        if isinstance(value, str):
            return value.strip().lower() == "true"
        return bool(value)
    if isinstance(value, str):
        return value.strip() or None
    return value


def _iter_sql_rows(sql_text: str, table: str):
    """INSERT INTO <table> ... VALUES (...),(...); 의 각 행을 필드 리스트로 산출.

    location_score·rent 는 문자열 필드에 괄호·개행이 없는 검증된 포맷이라 안전하다.
    동일 테이블 INSERT 가 여러 개면 전부 순회한다.
    """
    pattern = re.compile(
        rf"INSERT INTO {re.escape(table)}\b[^;]*?VALUES\s*(.*?);", re.DOTALL
    )
    for block in pattern.finditer(sql_text):
        for raw in re.findall(r"\(([^()]*)\)", block.group(1)):
            yield next(csv.reader([raw], quotechar="'", skipinitialspace=True))


def load_serving_scores(sql_path: Path = DATA_CORE_SQL) -> list[dict]:
    """서빙 점수 성분 로드 — 민감도 재계산 입력. burden_ratio = monthly_rent / est_sales."""
    text = sql_path.read_text(encoding="utf-8")
    rent = {r[0]: int(r[2]) for r in _iter_sql_rows(text, "rent")}
    out: list[dict] = []
    for r in _iter_sql_rows(text, "location_score"):
        area, industry = r[0], r[1]
        w1, w2, w3, w4, w5 = (float(x) for x in r[2:7])
        est_sales = int(r[7])
        mr = rent.get(area)
        out.append({
            "area_code": area, "industry": industry,
            "w1": w1, "w2": w2, "w3": w3, "w4": w4, "w5": w5,
            "est_sales": est_sales, "monthly_rent": mr,
            "burden_ratio": (mr / est_sales) if (mr and est_sales) else None,
        })
    return out
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `cd ai && python -m pytest tests/test_eval_common.py -v`
Expected: PASS (3 passed)

- [ ] **Step 5: ruff + 커밋**

```bash
cd ai && ruff check eval/ tests/test_eval_common.py
git add ai/eval/common.py ai/eval/suites/__init__.py ai/eval/.gitignore ai/tests/test_eval_common.py
git commit -m "[AI] feat: AI-07 평가 하네스 공용 로더·SQL 파서 (§12-1)"
```

---

### Task 2: 추출 확정골드 데이터 + 검증 테스트

**Files:**
- Create: `ai/eval/gold/extraction_confirmed.json` (원문 대조 확정골드 — 데이터 산출물)
- Test: `ai/tests/test_eval_extraction_gold.py`

**Interfaces:**
- Produces: `extraction_confirmed.json` — 리스트, 각 항목:
  `{"doc": str, "name": str, "review_action": "유지"|"수정"|"드롭"|"누락보완", "confirmed": {PRODUCT_KEYS...}, "review_note": str, "source_ref": str}`

**리뷰 방법론 (실행 시 준수):**
1. `extracted.json` 29상품 각각에 대해 원문을 연다 — `ai/data/raw/<doc>.pdf`(권위 원천, Read PDF 지원). 소진공은 `ai/data/interim/funding_docs/<doc>.txt`도 클린이라 교차 확인.
2. 필드별로 초안값이 원문과 일치하는지 확인. 일치=`유지`(confirmed=초안값 그대로). 불일치=`수정`(confirmed=원문 전사값, `source_ref`에 원문 위치·`review_note`에 근거). 원문에 부재/허위=`드롭`.
3. **amount_max 결측 8건 집중 검수**(소상공인 정책자금대출, 성장기반자금, 혁신성장촉진자금, 민간투자연계형 매칭융자, 일시적 경영애로자금, 상생성장 지원자금, 소공인 특화자금(일반), 긴급경영 안정자금(재해피해)): 원문에 **개별 업체 한도**가 명시되면 `수정`으로 채움(만원). 자금 **공급 규모(억원)**만 있고 개별 한도 미명시면 `confirmed.amount_max=null` 유지 + `review_action="드롭"`(서빙 부적격) + `review_note`에 "개별 한도 미명시".
4. **수치 생성 금지**: 원문에 없는 값을 만들지 않는다. 확신 없으면 null + 드롭.
5. 원문에만 있고 초안에 없는 상품 발견 시 `review_action="누락보완"`으로 추가.
6. confirmed 필드는 `PRODUCT_KEYS` 전 키를 포함(`doc` 제외, 별도 최상위 키).

- [ ] **Step 1: 실패하는 검증 테스트 작성** — `ai/tests/test_eval_extraction_gold.py`

```python
from eval import common

BANNED = ("승인", "추천", "권장")
VALID_ACTIONS = {"유지", "수정", "드롭", "누락보완"}


def test_gold_file_structural_integrity():
    gold = common.load_confirmed_gold()
    assert len(gold) >= 29  # 초안 전건 + 누락보완
    for g in gold:
        assert set(g) >= {"doc", "name", "review_action", "confirmed", "review_note"}
        assert g["review_action"] in VALID_ACTIONS
        assert isinstance(g["confirmed"], dict)
        # confirmed 는 PRODUCT_KEYS 전 키 보유(doc 제외)
        assert set(g["confirmed"]) >= set(common.PRODUCT_KEYS)


def test_every_modification_cites_source():
    gold = common.load_confirmed_gold()
    for g in gold:
        if g["review_action"] in ("수정", "누락보완"):
            assert g.get("source_ref"), f"{g['name']}: 수정/누락보완은 source_ref 필수"
            assert g["review_note"].strip()


def test_no_banned_wording_in_confirmed():
    gold = common.load_confirmed_gold()
    for g in gold:
        blob = "".join(str(v) for v in g["confirmed"].values() if isinstance(v, str))
        for w in BANNED:
            assert w not in blob, f"{g['name']}: 용어 위반 '{w}'"


def test_draft_products_all_covered():
    draft = common.load_extraction_draft()
    gold = common.load_confirmed_gold()
    gold_keys = {(g["doc"], g["name"]) for g in gold}
    for d in draft:
        assert (d["doc"], d["name"]) in gold_keys, f"미검수: {d['name']}"
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd ai && python -m pytest tests/test_eval_extraction_gold.py -v`
Expected: FAIL — `FileNotFoundError: extraction_confirmed.json`

- [ ] **Step 3: 확정골드 작성** — 위 방법론대로 `ai/data/raw/*.pdf`를 읽어 `ai/eval/gold/extraction_confirmed.json` 저장. 예시 형태(1건):

```jsonc
{
  "doc": "소진공_소상공인정책자금_지원사업안내",
  "name": "성장기반자금",
  "review_action": "드롭",
  "confirmed": {
    "name": "성장기반자금", "org": "소진공", "product_type": "loan",
    "max_age": null, "industries": ["제조업"], "regions": ["서울"],
    "pre_startup_only": false, "amount_max": null, "rate": null,
    "rate_note": "매분기별로 변동", "term_months": null, "exclusive_group": null,
    "status": "open", "notice_date": "2026-07-21",
    "source_url": "https://ols.semas.or.kr"
  },
  "review_note": "원문 p.17 공급규모 7,920억(자금 총량)만 명시, 개별 업체 한도 부재 → amount_max=null, 서빙 드롭",
  "source_ref": "소진공_소상공인정책자금_지원사업안내.pdf p.17"
}
```

- [ ] **Step 4: 검증 테스트 통과 확인**

Run: `cd ai && python -m pytest tests/test_eval_extraction_gold.py -v`
Expected: PASS (4 passed)

- [ ] **Step 5: 커밋**

```bash
git add ai/eval/gold/extraction_confirmed.json ai/tests/test_eval_extraction_gold.py
git commit -m "[AI] data: AI-07 추출 확정골드 — 원문 대조 검수본 (§12-1)"
```

---

### Task 3: 추출 정확도 스위트

**Files:**
- Create: `ai/eval/suites/extraction.py`
- Test: `ai/tests/test_eval_extraction.py`

**Interfaces:**
- Consumes: `common.PRODUCT_KEYS`, `common.norm_field`, `common.load_extraction_draft`, `common.load_confirmed_gold`
- Produces:
  - `evaluate(draft: list[dict], gold: list[dict]) -> dict`
  - `run(out_dir: Path) -> dict` (실데이터 로드→evaluate 반환)
  - 반환 dict 키: `field_accuracy, product_precision, product_recall, product_exact_rate, counts{유지,수정,드롭,누락보완}, per_field{key: accuracy}, total_products, total_fields_compared`

**지표 정의:**
- 대상 = review_action ∈ {유지, 수정}. 드롭 = 허위추출(정밀도 감점), 누락보완 = 미추출(재현율 감점).
- `field_accuracy` = Σ(일치 필드) / Σ(대상상품 × PRODUCT_KEYS), 비교는 `norm_field`.
- `product_precision` = |유지∪수정| / |유지∪수정∪드롭|.
- `product_recall` = |유지∪수정| / |유지∪수정∪누락보완|.
- `product_exact_rate` = |유지| / |유지∪수정|.
- `per_field[key]` = 해당 필드 일치 상품 / 대상 상품.

- [ ] **Step 1: 실패하는 테스트 작성** — `ai/tests/test_eval_extraction.py`

```python
from eval.suites import extraction


def _draft():
    return [
        {"doc": "D", "name": "A", "amount_max": 7000, "rate": 2.1, "regions": ["서울"]},
        {"doc": "D", "name": "B", "amount_max": None, "rate": None, "regions": None},
        {"doc": "D", "name": "C", "amount_max": 5000, "rate": 1.0, "regions": ["서울"]},
    ]


def _gold():
    # A: 완전일치(유지). B: amount_max 수정(1필드 불일치). C: 드롭. + 누락보완 E.
    base = {k: None for k in extraction.common.PRODUCT_KEYS}
    a = {**base, "name": "A", "amount_max": 7000, "rate": 2.1, "regions": ["서울"]}
    b = {**base, "name": "B", "amount_max": 3000, "rate": None, "regions": None}
    c = {**base, "name": "C", "amount_max": 5000, "rate": 1.0, "regions": ["서울"]}
    e = {**base, "name": "E", "amount_max": 1000}
    return [
        {"doc": "D", "name": "A", "review_action": "유지", "confirmed": a},
        {"doc": "D", "name": "B", "review_action": "수정", "confirmed": b},
        {"doc": "D", "name": "C", "review_action": "드롭", "confirmed": c},
        {"doc": "D", "name": "E", "review_action": "누락보완", "confirmed": e},
    ]


def test_counts_and_product_metrics():
    m = extraction.evaluate(_draft(), _gold())
    assert m["counts"] == {"유지": 1, "수정": 1, "드롭": 1, "누락보완": 1}
    assert m["product_precision"] == 2 / 3           # 유지+수정 / +드롭
    assert m["product_recall"] == 2 / 3              # 유지+수정 / +누락보완
    assert m["product_exact_rate"] == 1 / 2          # 유지 / 유지+수정


def test_field_accuracy_counts_single_mismatch():
    m = extraction.evaluate(_draft(), _gold())
    n_keys = len(extraction.common.PRODUCT_KEYS)
    # 대상 2상품(A,B) × n_keys, B 의 amount_max 1건만 불일치
    assert m["total_fields_compared"] == 2 * n_keys
    assert abs(m["field_accuracy"] - (2 * n_keys - 1) / (2 * n_keys)) < 1e-9
    assert m["per_field"]["amount_max"] == 1 / 2
    assert m["per_field"]["name"] == 1.0
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd ai && python -m pytest tests/test_eval_extraction.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'eval.suites.extraction'`

- [ ] **Step 3: 구현** — `ai/eval/suites/extraction.py`

```python
"""추출 정확도 스위트 (스펙 §12-1) — 초안↔원문 확정골드 필드 일치율."""
from __future__ import annotations

import json
from collections import Counter
from pathlib import Path

from eval import common


def _key(row: dict) -> tuple[str, str]:
    return (row.get("doc", ""), (row.get("name") or "").strip())


def evaluate(draft: list[dict], gold: list[dict]) -> dict:
    draft_by = {_key(d): d for d in draft}
    counts = Counter(g["review_action"] for g in gold)
    kept_mod = [g for g in gold if g["review_action"] in ("유지", "수정")]

    field_hits = 0
    field_total = 0
    per_field_hits: Counter = Counter()
    per_field_total: Counter = Counter()
    for g in kept_mod:
        d = draft_by.get(_key(g), {})
        conf = g["confirmed"]
        for k in common.PRODUCT_KEYS:
            match = common.norm_field(k, d.get(k)) == common.norm_field(k, conf.get(k))
            field_hits += int(match)
            field_total += 1
            per_field_hits[k] += int(match)
            per_field_total[k] += 1

    n_km = len(kept_mod)
    n_drop = counts.get("드롭", 0)
    n_miss = counts.get("누락보완", 0)
    n_keep = counts.get("유지", 0)
    return {
        "counts": {a: counts.get(a, 0) for a in ("유지", "수정", "드롭", "누락보완")},
        "product_precision": n_km / (n_km + n_drop) if (n_km + n_drop) else 0.0,
        "product_recall": n_km / (n_km + n_miss) if (n_km + n_miss) else 0.0,
        "product_exact_rate": n_keep / n_km if n_km else 0.0,
        "field_accuracy": field_hits / field_total if field_total else 0.0,
        "per_field": {k: per_field_hits[k] / per_field_total[k]
                      for k in common.PRODUCT_KEYS if per_field_total[k]},
        "total_products": len(gold),
        "total_fields_compared": field_total,
    }


def run(out_dir: Path) -> dict:
    return evaluate(common.load_extraction_draft(), common.load_confirmed_gold())
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `cd ai && python -m pytest tests/test_eval_extraction.py -v`
Expected: PASS (2 passed)

- [ ] **Step 5: 실데이터 스모크 + 커밋**

```bash
cd ai && python -c "from pathlib import Path; from eval.suites import extraction; import json; print(json.dumps(extraction.run(Path('eval/out')), ensure_ascii=False, indent=2))"
ruff check eval/suites/extraction.py tests/test_eval_extraction.py
git add ai/eval/suites/extraction.py ai/tests/test_eval_extraction.py
git commit -m "[AI] feat: AI-07 추출 정확도 스위트 (§12-1)"
```

---

### Task 4: 근거 충실도 골드 + 스위트

**Files:**
- Create: `ai/eval/gold/grounding_quotes.jsonl` (근거 골드 — 데이터 산출물)
- Create: `ai/eval/suites/grounding.py`
- Test: `ai/tests/test_eval_grounding.py`

**Interfaces:**
- Consumes: `common.load_source_text`, `common.load_extraction_draft`, `common.is_clean_source`
- Produces:
  - `evaluate(gold: list[dict], docs: dict[str, str]) -> dict`
  - `run(out_dir: Path) -> dict`
  - 반환 키: `verbatim_match_rate, linked, total, coverage, mismatches[list], legitimate_null_docs[list]`

**골드 스키마 (`grounding_quotes.jsonl`, 1행 1 JSON):**
`{"product_id": str, "name": str, "doc": str, "quote": str}` — `quote`는 해당 상품이 인용하는 원문 문단의 **verbatim 부분문자열**(소진공 클린 문서에서 전사, 6건). 인용은 검색이지 생성이 아니다(§5-4) — 재작성 금지.

**작성 방법론:** `finance_doc_chunk`가 6청크(전부 소진공)이므로, 각 청크가 연결된 상품별로 원문 `소진공_*.txt`에서 자금명이 포함된 문장/문단을 verbatim 복사해 `quote`로 기록한다. `quote`는 반드시 `load_source_text(doc)`의 바이트 정확 부분문자열이어야 한다.

- [ ] **Step 1: 실패하는 테스트 작성** — `ai/tests/test_eval_grounding.py`

```python
from eval.suites import grounding


def test_verbatim_match_and_mismatch():
    docs = {"D": "가나다 라마바 사아자 차카타"}
    gold = [
        {"product_id": "F-1", "name": "A", "doc": "D", "quote": "라마바 사아자"},
        {"product_id": "F-2", "name": "B", "doc": "D", "quote": "존재하지 않는 인용"},
    ]
    m = grounding.evaluate(gold, docs)
    assert m["linked"] == 2
    assert m["verbatim_match_rate"] == 0.5
    assert m["mismatches"] == ["F-2"]


def test_all_verbatim_passes():
    docs = {"D": "제9조에 의한 2026년 소상공인 정책자금"}
    gold = [{"product_id": "F-1", "name": "A", "doc": "D", "quote": "소상공인 정책자금"}]
    m = grounding.evaluate(gold, docs)
    assert m["verbatim_match_rate"] == 1.0 and m["mismatches"] == []


def test_real_gold_all_verbatim():
    from eval import common
    gold = grounding.load_gold()
    docs = {g["doc"]: common.load_source_text(g["doc"]) for g in gold}
    m = grounding.evaluate(gold, docs)
    assert m["verbatim_match_rate"] == 1.0, m["mismatches"]
    assert m["linked"] == 6
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd ai && python -m pytest tests/test_eval_grounding.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'eval.suites.grounding'`

- [ ] **Step 3: 골드 작성 + 스위트 구현**

먼저 `ai/eval/gold/grounding_quotes.jsonl`을 원문에서 전사(6행). 그 다음 `ai/eval/suites/grounding.py`:

```python
"""근거 충실도 스위트 (스펙 §12-1·§5-4) — 인용 청크의 원문 verbatim 충실도.

BE-06 라이브 RAG 서빙 인용 대조가 아니라, AI 배치가 만든 청크가 원문의 바이트 정확
부분문자열인지만 검증한다("인용은 검색이지 생성이 아니다").
"""
from __future__ import annotations

import json
from pathlib import Path

from eval import common

# 근거 청크가 불가능한(§5-4 정당 null) 깨진 원문 — 서울신보·KB 일부 CID/JS 렌더
_LEGIT_NULL_PREFIX = ("서울신보", "KB")


def load_gold() -> list[dict]:
    lines = (common.GOLD_DIR / "grounding_quotes.jsonl").read_text(encoding="utf-8").splitlines()
    return [json.loads(ln) for ln in lines if ln.strip()]


def evaluate(gold: list[dict], docs: dict[str, str]) -> dict:
    mismatches = [g["product_id"] for g in gold if g["quote"] not in docs.get(g["doc"], "")]
    draft = common.load_extraction_draft()
    total = len(draft)
    linked = len(gold)
    legit_null = sorted({d["doc"] for d in draft
                         if d["doc"].startswith(_LEGIT_NULL_PREFIX)
                         and not common.is_clean_source(d["doc"])})
    return {
        "verbatim_match_rate": (linked - len(mismatches)) / linked if linked else 0.0,
        "linked": linked,
        "total": total,
        "coverage": linked / total if total else 0.0,
        "mismatches": mismatches,
        "legitimate_null_docs": legit_null,
    }


def run(out_dir: Path) -> dict:
    gold = load_gold()
    docs = {g["doc"]: common.load_source_text(g["doc"]) for g in gold}
    return evaluate(gold, docs)
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `cd ai && python -m pytest tests/test_eval_grounding.py -v`
Expected: PASS (3 passed)

- [ ] **Step 5: ruff + 커밋**

```bash
cd ai && ruff check eval/suites/grounding.py tests/test_eval_grounding.py
git add ai/eval/gold/grounding_quotes.jsonl ai/eval/suites/grounding.py ai/tests/test_eval_grounding.py
git commit -m "[AI] feat: AI-07 근거 충실도 스위트 + 골드 (§12-1·§5-4)"
```

---

### Task 5: 민감도 스위트

**Files:**
- Create: `ai/eval/suites/sensitivity.py`
- Test: `ai/tests/test_eval_sensitivity.py`

**Interfaces:**
- Consumes: `common.load_serving_scores`
- Produces:
  - `WEIGHTS: dict[str, float]`, `THETA: float`
  - `composite(row, weights) -> float`
  - `top3(rows, weights, theta) -> list[str]`
  - `evaluate(rows: list[dict]) -> dict` — `{industry: {base_top3, mean_retention, n_perturbations, n_areas}}`
  - `run(out_dir: Path) -> dict`

**정의:** 가중치 = (w1 .30·w2 .20·w3 .20·w4 .15·w5 .15, assumptions #6), θ=0.15. 섭동 = 각 가중치 ±20%(단일 축, 10회) + θ∈{0.10,0.20}(2회) = 12회. 업종별 θ 통과 상권을 종합점수 내림차순 정렬한 상위 3곳의 섭동 후 유지율(교집합/3) 평균. 가중치는 섭동 후 재정규화하지 않는다(원 민감도).

- [ ] **Step 1: 실패하는 테스트 작성** — `ai/tests/test_eval_sensitivity.py`

```python
from eval.suites import sensitivity


def _rows():
    # cafe 4곳, 전부 θ 통과(burden 0.1). w1 지배 → area 순위 명확.
    def r(code, w1):
        return {"area_code": code, "industry": "cafe",
                "w1": w1, "w2": 0.5, "w3": 0.5, "w4": 0.5, "w5": 0.5,
                "est_sales": 1000, "monthly_rent": 100, "burden_ratio": 0.1}
    return [r("A", 0.9), r("B", 0.8), r("C", 0.7), r("D", 0.1)]


def test_composite_and_top3():
    rows = _rows()
    base = sensitivity.top3(rows, sensitivity.WEIGHTS, sensitivity.THETA)
    assert base == ["A", "B", "C"]


def test_theta_gate_excludes_high_burden():
    rows = _rows()
    rows[0]["burden_ratio"] = 0.9  # A 탈락
    t = sensitivity.top3(rows, sensitivity.WEIGHTS, 0.15)
    assert "A" not in t and t == ["B", "C", "D"]


def test_evaluate_reports_retention():
    m = sensitivity.evaluate(_rows())
    assert m["cafe"]["base_top3"] == ["A", "B", "C"]
    assert 0.0 <= m["cafe"]["mean_retention"] <= 1.0
    assert m["cafe"]["n_perturbations"] == 12
    assert m["cafe"]["n_areas"] == 4
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd ai && python -m pytest tests/test_eval_sensitivity.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'eval.suites.sensitivity'`

- [ ] **Step 3: 구현** — `ai/eval/suites/sensitivity.py`

```python
"""민감도 스위트 (스펙 §12-1·§4-3) — 가중치 ±20%·θ 변동 시 상위 3곳 유지율."""
from __future__ import annotations

from collections import defaultdict
from pathlib import Path

from eval import common

WEIGHTS = {"w1": 0.30, "w2": 0.20, "w3": 0.20, "w4": 0.15, "w5": 0.15}
THETA = 0.15


def composite(row: dict, weights: dict) -> float:
    return sum(weights[k] * row[k] for k in weights)


def top3(rows: list[dict], weights: dict, theta: float) -> list[str]:
    passing = [r for r in rows if r["burden_ratio"] is not None and r["burden_ratio"] <= theta]
    ranked = sorted(passing, key=lambda r: composite(r, weights), reverse=True)
    return [r["area_code"] for r in ranked[:3]]


def _overlap(base: list[str], pert: list[str]) -> float:
    return len(set(base) & set(pert)) / len(base) if base else 0.0


def evaluate(rows: list[dict]) -> dict:
    by_ind: dict[str, list] = defaultdict(list)
    for r in rows:
        by_ind[r["industry"]].append(r)
    out: dict[str, dict] = {}
    for ind, irows in by_ind.items():
        base = top3(irows, WEIGHTS, THETA)
        retentions = []
        for k in WEIGHTS:                       # 가중치 단일축 ±20% (10회)
            for delta in (0.8, 1.2):
                w = {**WEIGHTS, k: WEIGHTS[k] * delta}
                retentions.append(_overlap(base, top3(irows, w, THETA)))
        for th in (0.10, 0.20):                 # θ 변동 (2회)
            retentions.append(_overlap(base, top3(irows, WEIGHTS, th)))
        out[ind] = {
            "base_top3": base,
            "mean_retention": sum(retentions) / len(retentions) if retentions else 0.0,
            "n_perturbations": len(retentions),
            "n_areas": len(irows),
        }
    return out


def run(out_dir: Path) -> dict:
    return evaluate(common.load_serving_scores())
```

- [ ] **Step 4: 테스트 통과 확인 + 실데이터 스모크**

Run: `cd ai && python -m pytest tests/test_eval_sensitivity.py -v`
Expected: PASS (3 passed)
Run: `cd ai && python -c "from pathlib import Path; from eval.suites import sensitivity; print(sensitivity.run(Path('eval/out')))"`
Expected: cafe·food 각 `mean_retention` 출력(0~1)

- [ ] **Step 5: ruff + 커밋**

```bash
cd ai && ruff check eval/suites/sensitivity.py tests/test_eval_sensitivity.py
git add ai/eval/suites/sensitivity.py ai/tests/test_eval_sensitivity.py
git commit -m "[AI] feat: AI-07 민감도 스위트 — 가중치·θ 섭동 (§12-1·§4-3)"
```

---

### Task 6: 리포트 조립 + 디스패처 배선 + Makefile 통과

**Files:**
- Create: `ai/eval/suites/report.py`
- Modify: `ai/eval/run.py` (미구현 SystemExit 제거, 디스패처 배선)
- Test: `ai/tests/test_eval_report.py`

**Interfaces:**
- Consumes: `extraction.run`, `grounding.run`, `sensitivity.run`
- Produces:
  - `report.run(out_dir: Path) -> dict` — metrics.json 작성 + PNG 저장 + 집계 dict 반환
  - `run.py` CLI: `--suite {extraction,grounding,sensitivity,model,matching,report}` — matching/model 은 안내 후 통과

- [ ] **Step 1: 실패하는 테스트 작성** — `ai/tests/test_eval_report.py`

```python
import json
from pathlib import Path

from eval.suites import report


def test_report_writes_metrics_json(tmp_path: Path):
    result = report.run(tmp_path)
    out = tmp_path / "metrics.json"
    assert out.exists()
    data = json.loads(out.read_text(encoding="utf-8"))
    assert set(data) >= {"generated_at", "extraction", "grounding",
                         "sensitivity", "matching", "model"}
    assert data["matching"]["status"] == "BE 소관"
    assert data["model"]["status"] == "AI-08 소관"
    assert result["extraction"]["total_products"] >= 29
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd ai && python -m pytest tests/test_eval_report.py -v`
Expected: FAIL — `ModuleNotFoundError: No module named 'eval.suites.report'`

- [ ] **Step 3: `report.py` 구현**

```python
"""성적표 조립 (스펙 §12-1 부록 1) — metrics.json + 차트 PNG."""
from __future__ import annotations

import datetime as _dt
import json
from pathlib import Path

import matplotlib
matplotlib.use("Agg")
import matplotlib.pyplot as plt  # noqa: E402

from eval.suites import extraction, grounding, sensitivity  # noqa: E402


def run(out_dir: Path) -> dict:
    out_dir.mkdir(parents=True, exist_ok=True)
    ext = extraction.run(out_dir)
    grd = grounding.run(out_dir)
    sens = sensitivity.run(out_dir)
    metrics = {
        "generated_at": _dt.datetime.now().isoformat(timespec="seconds"),
        "extraction": ext,
        "grounding": grd,
        "sensitivity": sens,
        "matching": {"status": "BE 소관", "note": "EligibilityFilterTest + 통합 테스트"},
        "model": {"status": "AI-08 소관", "note": "§12-2 LightGBM+SHAP"},
    }
    (out_dir / "metrics.json").write_text(
        json.dumps(metrics, ensure_ascii=False, indent=2), encoding="utf-8")
    _chart_extraction(ext, out_dir / "extraction.png")
    _chart_sensitivity(sens, out_dir / "sensitivity.png")
    return metrics


def _chart_extraction(ext: dict, path: Path) -> None:
    keys = list(ext["per_field"])
    vals = [ext["per_field"][k] for k in keys]
    fig, ax = plt.subplots(figsize=(8, 4))
    ax.bar(range(len(keys)), vals, color="#4C78A8")
    ax.set_xticks(range(len(keys)))
    ax.set_xticklabels(keys, rotation=45, ha="right", fontsize=7)
    ax.set_ylim(0, 1)
    ax.set_ylabel("field match rate")
    ax.set_title(f"Extraction field accuracy = {ext['field_accuracy']:.1%}")
    fig.tight_layout()
    fig.savefig(path, dpi=120)
    plt.close(fig)


def _chart_sensitivity(sens: dict, path: Path) -> None:
    inds = list(sens)
    vals = [sens[i]["mean_retention"] for i in inds]
    fig, ax = plt.subplots(figsize=(5, 4))
    ax.bar(inds, vals, color="#54A24B")
    ax.set_ylim(0, 1)
    ax.set_ylabel("top-3 retention")
    ax.set_title("Sensitivity: weight ±20% / theta")
    fig.tight_layout()
    fig.savefig(path, dpi=120)
    plt.close(fig)
```

- [ ] **Step 4: `run.py` 배선** — `ai/eval/run.py` 전체 교체

```python
"""평가 하네스 엔트리포인트 — AI-07 (D9~10). 스펙 §12-1.

사용: python -m eval.run --suite extraction|grounding|sensitivity|report
matching(BE 소관)·model(AI-08 소관)은 안내 후 통과한다.
"""
import argparse
import json
from pathlib import Path

from eval.suites import extraction, grounding, report, sensitivity

DISPATCH = {
    "extraction": extraction.run,
    "grounding": grounding.run,
    "sensitivity": sensitivity.run,
    "report": report.run,
}
PASSTHROUGH = {
    "matching": "BE 소관 — EligibilityFilter 는 BE 단위 + 통합 테스트가 검증",
    "model": "AI-08 소관 — §12-2 LightGBM+SHAP 설계 교차 검증",
}
SUITES = (*DISPATCH, *PASSTHROUGH)


def main() -> None:
    parser = argparse.ArgumentParser(description="Ventry AI 품질 평가 하네스 (스펙 §12-1)")
    parser.add_argument("--suite", choices=SUITES, required=True)
    parser.add_argument("--out", default="eval/out", help="metrics.json·차트 출력 경로")
    args = parser.parse_args()

    if args.suite in PASSTHROUGH:
        print(f"eval suite '{args.suite}': {PASSTHROUGH[args.suite]} (본 하네스 미실행)")
        return
    result = DISPATCH[args.suite](Path(args.out))
    print(json.dumps(result, ensure_ascii=False, indent=2))


if __name__ == "__main__":
    main()
```

- [ ] **Step 5: 테스트 통과 확인**

Run: `cd ai && python -m pytest tests/test_eval_report.py -v`
Expected: PASS (1 passed)

- [ ] **Step 6: ruff + 커밋**

```bash
cd ai && ruff check eval/
git add ai/eval/suites/report.py ai/eval/run.py ai/tests/test_eval_report.py
git commit -m "[AI] feat: AI-07 성적표 조립 + 디스패처 배선 (§12-1)"
```

---

### Task 7: `docs/assumptions.md` 등재 + 전체 `make eval` 검증

**Files:**
- Modify: `docs/assumptions.md` (신규 등재 행 추가)
- Verify: `ai/Makefile` (변경 없이 통과 확인)

- [ ] **Step 1: assumptions.md 등재** — 표 하단에 행 추가 (등재번호는 현재 최대+1)

```markdown
| NN | 2026-07-24 | AI | **AI-07 평가 하네스 = AI 기능 한정(추출·근거·민감도).** 결정적 EligibilityFilter 매칭은 BE 단위+통합 소관으로 하네스에서 제외(재구현 금지). 추출 정확도 = extracted.json 초안↔원문 PDF 대조 확정골드(`ai/eval/gold/extraction_confirmed.json`) 필드 일치율(정규화: 배열=집합·null==빈배열·rate 반올림 3자리·수치 캐스팅). 근거 충실도 = 청크 quote 가 원문 txt 바이트 정확 부분문자열(§5-4), 깨진 서울신보·KB 는 정당 null. 민감도 = location_score 축값으로 종합 재계산, 가중치 ±20%(단일축 10)+θ∈{0.10,0.20}(2)=12섭동, 업종별 상위3 유지율(재정규화 없음). 부수: SessionMapper 가 region 전체문자열("서울 마포구")을 필터에 전달 → ["서울"] 상품 배제되는 잠재 버그 BE 전달. | `ai/eval/**`, `docs/superpowers/specs/2026-07-24-ai07-eval-gold-design.md` | 부록 1 성적표, BE 매칭 골드, AI-09 게이트 |
```

- [ ] **Step 2: 전체 스위트 테스트**

Run: `cd ai && python -m pytest tests/test_eval_*.py -v`
Expected: 전건 PASS

- [ ] **Step 3: `make eval` 1회 실행 검증 (AI-07 DoD)**

Run: `cd ai && make eval`
Expected: eval-matching(BE 소관)·eval-model(AI-08 소관) 안내 출력, extraction·grounding·sensitivity 실행, `eval/out/metrics.json` + `extraction.png` + `sensitivity.png` 생성, 종료코드 0

Run: `cat ai/eval/out/metrics.json | python -m json.tool | head -30`
Expected: extraction(field_accuracy·precision·recall)·grounding(verbatim_match_rate=1.0)·sensitivity(cafe·food) 수치 확인

- [ ] **Step 4: ruff 전체 + 커밋**

```bash
cd ai && ruff check .
git add docs/assumptions.md
git commit -m "[AI] docs: AI-07 평가 하네스 가정 등재 + make eval 검증 (§12-1)"
```

---

## Self-Review

**Spec coverage (설계 §2~§9 대조):**
- §4 추출 → Task 2(골드)+Task 3(스위트) ✓
- §5 근거 → Task 4 ✓
- §6 민감도 → Task 5 ✓
- §7 리포트 → Task 6 ✓
- §2-1 매칭 제외·§2-2 부수발견 → Task 6(디스패처 통과)+Task 7(등재) ✓
- §8 가드레일 → 각 Task 검증 테스트(용어·수치생성·읽기전용) + Task 7 등재 ✓
- §9 DoD → Task 7 Step 3 `make eval` ✓

**타입 일관성:** `common.PRODUCT_KEYS`·`norm_field`·`load_serving_scores` 반환 dict 키(`w1..w5,est_sales,monthly_rent,burden_ratio`)가 Task 3·5에서 동일하게 소비됨. 각 스위트 `run(out_dir)->dict`·`evaluate(...)->dict` 시그니처가 Task 6 report에서 일치. ✓

**Placeholder scan:** 코드 블록 전부 실제 구현. Task 2·4의 데이터 산출물은 방법론+검증 테스트로 제약(값은 원문 전사, 계획에 값 하드코딩 불가 — 생성 금지 원칙). ✓
