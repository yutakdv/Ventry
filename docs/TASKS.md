# TASKS — 역할별 상세 태스크 분해 (D1~D14)

기준: `docs/specs/최종_스펙문서.md` v6.3 · `docs/specs/exploration_agent_spec_v2_1.md` v2.1
역할: **AI**(데이터·배치·평가) / **BE**(Spring 서빙·에이전트) / **FE**(React·지도) / **CM**(공통)

- **팀원별 세부 체크리스트(파일 단위·명령·PR 슬라이스)**: [tasks/FRONTEND.md](tasks/FRONTEND.md) ·
  [tasks/BACKEND.md](tasks/BACKEND.md) · [tasks/AI.md](tasks/AI.md) — 이 문서는 총괄(의존·마일스톤)용.
- **BE 인수인계 (담당 교대 시 필독)**: [HANDOFF_BACKEND.md](HANDOFF_BACKEND.md) — 엔드포인트별
  목/실 상태·도구 계층 결선 현황·다음 순서·함정 목록·동작 확인 명령.
- **D0 확정 사항 (전 파트 필독)**: [DECISIONS.md](DECISIONS.md) — 판정 용어·금액 단위·계약 스키마
  보강·LLM 제공자·청크 저장 방식 7건 확정 기록.
- **진행 현황 대시보드**: GitHub Projects 로드맵(간트) — 구축·운영은 [BOARD.md](BOARD.md),
  원커맨드 세팅은 `./scripts/setup_board.sh`.
- PR 단위 = 태스크 ID 1~2개. PR 제목에 ID 표기 (`[BE-04] ...`).
- **브랜치는 2단계**: 토픽(`be04-frontier`) →(로컬 병합)→ 영역(`backend`) →(PR)→ `develop`.
  토픽에서 develop으로 직접 PR 금지 — 상세는 [CONTRIBUTING §2](../CONTRIBUTING.md).
- **서브이슈 규칙**: 한 태스크(부모 이슈)가 여러 독립 슬라이스로 갈리거나, 개발 중 별도로 추적할
  작업(엣지 케이스·후속 작업·스펙 모호점 해소)이 생기면 부모 하위에 서브이슈를 만든다.
  - 제목: `[<부모ID><a|b|c…>] 요약` (예: `[BE-03a] EligibilityFilter + 단위 테스트`).
  - 라벨은 부모와 동일 파트(BE/FE/AI/CM), 본문 첫 줄에 `부모: #<부모번호>` 명시.
  - 부모 이슈 본문에 `- [ ] #<서브번호>` 체크리스트로 연결(작업 완료 시 체크).
  - 커밋/PR 제목은 서브이슈 ID를 그대로 사용 (`[BE] feat: … (BE-03a)`).
- **이슈 종료**: PR 본문에 `Closes #<번호>`를 적으면 develop 병합 → main 반영 후
  `develop-ci`의 `close-referenced-issues` 잡이 자동 종료한다. 리포 default가 `main`이라
  **GitHub 기본 자동 종료는 develop 대상 PR에서 발동하지 않으므로**(closing 링크 자체가 생기지 않음)
  이 잡이 PR 본문 키워드를 직접 파싱해 닫는다.
- 우선순위: 스펙 §0-2의 P0 전부 → P1(D9~10 여유 시, ①RAG → ②차트 → ③개인화 순) → P2 금지(로드맵만).
- **D10 기능 동결** 이후 신규 기능 PR 금지.

## 마일스톤 (팀 공통 앵커)

| 체크포인트 | 일차 | 내용 | 게이트 |
|---|---|---|---|
| CP0 | D0 | 리포·CI·브랜치 세팅 (CM-01) | compose 스모크 통과 |
| CP1 | D3 | **API 계약 동결** + 목 데이터 E2E | 3인 합의, 이후 계약 변경은 CONTRIBUTING §6 절차 |
| CP2 | D6 | 실데이터 적재 완료 → BE 실데이터 전환 | 산출 테이블 검증 케이스 통과 |
| CP3 | D8 | 탐색+검증 에이전트 동작 (크루 모먼트 성립) | 데모 시나리오 3번 항목 재현 |
| CP4 | D10 | **기능 동결** + RAG 구현/이월 문서 확정 + 민감도 분석 | P1 반영 여부 판정 |
| CP5 | D12 | 통합 QA 완료 + `make eval` 최종 → 게이트 판정(§12-3) | 부록 1·2 수치 확정 |
| CP6 | D14 | 제출 (기술설명서 PDF + 소스코드 + 참가자 청서) | 리허설 2회 완료 |

---

## AI (데이터·배치·평가) — `ai` 브랜치

| ID | 일차 | 태스크 | 산출물 / 완료 조건 (DoD) | 의존 | 스펙 |
|---|---|---|---|---|---|
| AI-01 | D1 | **데이터 실사(실수집·스키마 확정)**: 서울 상권분석 7종 실수집+컬럼 확정 / 부동산원 신 API(15099345) 키 신청 / 상권 구획도 SHP(2024-10-31판)↔최신 분기 통계 상권코드 정합성 전건 대조 / 역사마스터 좌표계 확인(불명확 시 t-data 지하철역_GEOM 즉시 교체) / θ·θ' 문헌 확보 / Track A(열린데이터광장 임대시세) 존재 확인 | `ai/data/raw/` 샘플 + 컬럼 정의 노트, 정합성 대조 결과 `docs/assumptions.md` 등재 | — | §2-0, 리스크 #18 |
| AI-02 | D2 | **수집 완료**: 인허가 CSV(재분류 247 기준 카페·음식점 매핑) / 권리금 최신 연간치 / 정책자금·보증·대출 공개 문서 20~40건 / 교통(역사 좌표+승하차, 일→연·월 일평균 산출은 파일/OpenAPI 경로) / 좌표계 변환(EPSG:5181→WGS84) 검증 | 수집 스크립트 `ai/batch/collect/` 재실행 가능, 환승역 정규화·합산 규칙 등재 | AI-01 | §2, §2-2 |
| AI-03 | D3 | **스키마 DDL + 목 데이터 덤프**: 전 테이블(상권·매출·인구·임대료·점수·비용·금융상품) DDL + 소량 목 데이터 `db/init/` 제출 → CP1 E2E | `docker compose up` 후 BE가 목 데이터 조회 가능 | AI-01 | §3-1 스키마 |
| AI-04 | D4~5 | **공간 조인 3단계**: ①인허가 좌표→상권 폴리곤(경쟁밀도) ②구역 중심점→부동산원 구획(임대료 할당, 실패 시 자치구 평균+fallback_flag) ③중심점→최근접 역(sjoin_nearest, 거리+승하차) | 검증 3종(강남역 표본 육안 / 부동산원 상권 3곳 / 대표역 3곳) 통과 로그 | AI-02 | §3-1 |
| AI-05 | D5~6 | **산출 테이블**: 초기비용 4블록(보증금 역산·권리금 이중 표기·인테리어·예비 운영자금) / 이중 필터용 부담률 분모·분자 / 점수화(w1 3성분: 길단위+상주·직장+교통 접근성 `승하차×exp(−d/500m)`, 서울 전체 백분위 정규화) / 정렬 인덱스 | 정산 테이블 적재 + 파라미터 전부 `docs/assumptions.md` | AI-04 | §4-1~4-3 |
| AI-06 | D6 | **정책자금 구조화**: LLM 배치 추출 → 전건 사람 검수 → 구조화 DB(exclusive_group·term_months 포함) + 원문 문단 청크 벡터DB 적재(doc_chunk_ref 연결) | 검수 대조표 보존 (→ AI-07 추출 정확도 골드) | AI-02 | §3-1 |
| AI-07 | D9~10 | **평가 하네스**: 가상 프로필 12~20종×상품 전건 교차 골드셋 / `make eval` 5개 타겟(matching·extraction·grounding·sensitivity·model) 조립 / metrics.json+차트 PNG | `make eval` 1회 실행 성공, 산출물 = 부록 1 | AI-05, AI-06 | §12-1 |
| AI-08 | D9~10 | **LightGBM+SHAP 설계 교차 검증**: 프로토콜 사전 고정(비매출 피처만 / log(점포당 매출) 타깃 / 자치구 블록 5-fold CV / R²·ρ·WAPE·MAE / SHAP 방향 일치) — **게이트(§12-3)는 결과 확인 전 등재 완료 상태여야 함** / 민감도 분석(가중치 ±20% + θ 변동) 스크립트 | 재현 스크립트 + 프로토콜 assumptions.md 등재 | AI-05 | §12-2, §12-3, §4-3 |
| AI-09 | D11~12 | **최종 평가·게이트 판정**: `make eval` 최종 실행 → A/B/C 판정 → 부록 1·2 수치 확정, C면 부록 2 삭제+민감도 단독 유지(v6.1 상태 복귀) | 판정 결과·근거 문서화, README 3줄 섹션 유효성 확인 | AI-07, AI-08 | §12-3, 리스크 #19 |

## BE (서빙·에이전트) — `backend` 브랜치

| ID | 일차 | 태스크 | 산출물 / DoD | 의존 | 스펙 |
|---|---|---|---|---|---|
| BE-01 | D3 | **API 계약 동결 + 목 구현**: 6개 엔드포인트 DTO·목 응답 (`docs/API_CONTRACT.md` 확정, WGS84·만원 단위·source 필드·기준일 포함) → CP1 E2E | FE가 목으로 전 화면 개발 가능 | — | §6 |
| BE-02 | D4~5 | **DB 연동 + 캐시**: 사전 적재 테이블 read-only 조회, 탐색당 쿼리 1회+인메모리 원칙, Caffeine 캐시(키: 업종·자치구, 기동 시 예열), 정렬 비용 배열 사전 정렬 | 목→실데이터 스위치 (CP2) | AI-03 | expl §5 |
| BE-03 | D5~6 | **결정적 도구 계층 5종**: `eligibility_filter / cost_calculator / score_lookup / frontier(진입·지속) / reverse_check` — 순수 함수 + **단위 테스트 필수** | 테스트 전건 통과 (자기자본만 구간 m=0 ⇒ 필터2=필터2' 일관성 케이스 포함) | BE-02 | §5-1, expl §2 |
| BE-04 | D7 | **해석적 프론티어(진입·무권리) + 조합 제약 조달 검증**: 정렬 배열 기반 닫힌 형태 경계·갭·B_safe / 커버 사양(할당 순서·exclusive_group·open 미포함) / 커버 성공 시 조달 명세 | 경계 절단점 오차 0 검증 | BE-03, AI-06 | expl §2-1·2-2 |
| BE-05 | D8 | **탐색·검증 에이전트**: 확장 부담률 필터 2'·지속 프론티어(비단조) / 스코어링 Q·F·C·컷(≤3건, 0건 유효) / LLM 탐색 계획(plan)·언어화(refine)·템플릿 폴백 / `/explore` SSE(version 취소, 템플릿 즉시 송출) / tool-calling 결선 / 리스크 검증 에이전트(1왕복+장애 폴백) | 데모 시나리오(expl §8) 재현 = CP3 | BE-04 | expl §2-3~2-5·§4·§5, §5-2·5-3 |
| BE-06 | D9~10 | **P1**: ① RAG 근거 인용(source_quote=벡터DB 원문 그대로, LLM 재작성 금지) ② 근거문 캐시(상권×업종 사전 생성) ③ 개인화 언어화 — 여유 시 순서대로, 미구현 시 D10에 문서 이월 처리 | 구현분 계약 반영 or 이월 문서화 | BE-05, AI-06 | §5-4·5-5 |
| BE-07 | D11~12 | **통합 QA·하드닝**: 시나리오 10종 + LLM 전면 차단 상태 QA(템플릿 폴백 경로) + 검증 생략 플래그 + Docker 정리 | QA 리포트, compose 스모크 그린 | BE-05 | §9 D11~12, 리스크 #9 |

## FE (화면·지도) — `frontend` 브랜치

| ID | 일차 | 태스크 | 산출물 / DoD | 의존 | 스펙 |
|---|---|---|---|---|---|
| FE-01 | D1 | **카카오맵 스파이크**: 앱키 발급·도메인 등록('카카오맵 사용 설정' ON), 마커·폴리곤 표시 스파이크, SSE(EventSource) 스파이크 | 스파이크 브랜치 데모 | — | §2-0 |
| FE-02 | D3~4 | **화면 1 진단**: 폼+자연어 텍스트 하이브리드 입력, 파싱 결과 확인 UI, **"데모 프로필 불러오기" 버튼**(원클릭 데모 시작) — 목 API 연동 | CP1 E2E 참여 | BE-01 | §7, §0-8 |
| FE-03 | D4~6 | **화면 2·3 골격**: 시나리오 카드 2장(보수/적극)+출처 배지+SSE 수신 / 지도 마커 3종(🟢적합·🟠유의·⚪조건부)+근거 패널(임대료 출처·교통 셀 상시 표기)+예산 슬라이더 | 실데이터 전환(CP2) 후 전 화면 동작 | BE-01·02 | §7, §1-1 |
| FE-04 | D7~8 | **탐색·역방향 UI**: 탐색 인사이트 카드(**세션 최초 1회 자동 펼침**, 이후 접힘+카운트 배지, 헤더에 plan 근거 1줄) / T1 진입·지속 병기 표기 / 임의 상권 클릭 역방향 판정 4단계+자격 부합 상품 | CP3 데모 동선 성립 | BE-05 | §0-8, expl §3·§8 |
| FE-05 | D9~10 | **검증·반응성**: "왜? (검증 의견 n건)" 패널(클릭 시 반박 왕복 전개) / 슬라이더 debounce 300ms+version 공유, <100ms 재계산 체감 / (P1-②) 프론티어 미니 차트 — 기술설명서용 정적 이미지 내보내기 포함 | 기능 동결 전 완료 | BE-05 | §5-3, §7, §0-8 |
| FE-06 | D11~12 | **마감 스윕**: 스타일링 / **용어 컴플라이언스 전 화면 스윕**(금지어표) / 고지 문구 상시 노출 확인 / **데모 이미지 3종 캡처 → `docs/images/demo-{1,2,3}.png`** / 데이터 기준일 표기 확인 | README 이미지 교체 완료 | 전체 | §0-4, expl §7 |

## CM (공통) — 담당자 지정하여 진행

| ID | 일차 | 태스크 | 담당 | DoD |
|---|---|---|---|---|
| CM-01 | D0 | 리포·브랜치·CI·compose 세팅 (본 커밋) + 브랜치 보호 설정(CONTRIBUTING §2) + 훅 활성화 각자 확인 | 전원 | CI 그린 |
| CM-02 | D3 | CP1: 목 데이터 E2E 합동 점검 (화면1→진단→시나리오 목 흐름) + **D0 확정 사항([DECISIONS.md](DECISIONS.md)) 팀 검토 → 계약 최종 동결** | 전원 | 데모 동선 0~2단계 목으로 재현, 계약 동결 선언 |
| CM-03 | D10 | CP4: 기능 동결 선언 + RAG 구현/이월 판정 → 스펙 §5-4 조건부 문서 반영 (**구현/이월 이원화 문안 2종은 판정 전 심사_QA.md에 사전 작성**, 스펙 §5-4) | 전원 | 이월 시 "설계 확정·본선 반영" 문구로 문서 통일 |
| CM-04 | D11 | **README §8-1 심사 동선 가이드 작성**(D11 오전, 기술설명서 장 번호 앵커 포함) → 외부 1인(비개발자) 도슨트 3분 테스트 → 실패 시 가이드·화면 재조정 | FE 주관 | 3분 내 크루 모먼트 도달 |
| CM-05 | D13~14 | 기술설명서 PPT→PDF (본문 15~20 + 부록 10~15: 매핑 표 1장·설계 원칙 박스 5·사업성 1p·스크린샷 앵커·프론티어 차트·부록 1·2) / 심사_QA.md·assumptions.md 검수 / 제출 리허설 2회 | 전원 | 2026-08-03 16:00 前 제출 |

---

## 병렬화 구조 (누가 누구를 기다리는가)

```
D1~2   AI-01·02 ∥ FE-01 ∥ (BE: 계약 초안 작성)     ← 상호 의존 없음
D3     ★ 전원 수렴: AI-03 덤프 + BE-01 목 API + FE-02 착수 → CP1
D4~6   AI-04·05·06 ∥ BE-02·03 ∥ FE-02·03          ← FE는 목으로, BE는 D5부터 실데이터
D7~8   BE-04·05 ∥ FE-04 ∥ (AI: 골드셋 준비 선행)
D9~10  BE-06 ∥ FE-05 ∥ AI-07·08                    ← P1 우선순위: RAG > 차트 > 개인화
D11~12 BE-07 ∥ FE-06 ∥ AI-09 + CM-04
D13~14 CM-05 (전원 문서 모드)
```

주요 크로스 의존 (지연 시 즉시 공유):
- **AI-03 → BE-02/FE-03**: D3 덤프가 늦으면 BE·FE는 목 유지로 무중단, CP2가 밀림.
- **AI-06 → BE-04(조달 검증)·BE-06(RAG)**: exclusive_group·벡터DB 없으면 BE-04는 목 상품으로 선행.
- **BE-05 → FE-04·05**: SSE 이벤트 스키마는 계약에 D3 동결이므로 FE는 목 SSE로 선행 가능.
- **LLM 제공자 = OpenAI API로 확정 (7/21 변경, DECISIONS.md §6)**: AI-06 배치 추출과 BE-05
  서빙이 동일 키(.env `OPENAI_API_KEY`) 사용. 서빙 gpt-4o-mini / 배치 gpt-4o.

## 일정 리스크 대응 (스펙 리스크 레지스터 발췌)

- **D8 초과(#12)**: A2·A3 축 스킵, A1 이중 프론티어+검증 1왕복만 사수 → 크루 모먼트는 A1로 성립.
- **LLM 장애(#9)**: 템플릿 우선 설계이므로 폴백 경로가 항상 최종본 — QA에 전면 차단 시나리오 포함.
- **평가 하네스 파행(§12-4)**: ①RAG 연동분→문서 이월 ②모델→게이트 C 처리 ③골드셋 12종 축소.
  민감도 분석은 기존 P0 QA이므로 최후까지 유지. 파행해도 서비스에는 어떤 흔적도 남지 않는다.
