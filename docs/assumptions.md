# assumptions.md — 가정·폴백 일원화 대장

모든 배치 산출·계산의 가정은 발생 즉시 이 문서에 등재한다 (스펙 §2-2).
등재 형식: 날짜 / 담당 / 가정 내용 / 근거 / 영향 범위.

### 채번 규칙 (하드 룰)

- 번호는 **파트 무관 단일 시퀀스**다. AI·BE·FE·CM이 같은 번호대를 각자 쓰지 않는다.
- 새 항목은 **표의 마지막 번호 + 1**. 표는 번호 오름차순으로 유지한다.
- 두 파트가 동시에 작업해 같은 번호를 잡았다면, **나중에 병합되는 쪽이 재채번**한다
  (선점은 등재 날짜 기준). 재채번 시 리포 안의 상호참조를 같은 커밋에서 함께 옮긴다.

> **2026-07-25 중복 해소 — AI 파트 #21~#25 → #38~#42**
> AI 파트가 07-24에 BE(07-23 선점)와 같은 번호를 중복 채번해 #21~#25가 두 벌 존재했다.
> BE가 먼저 채번했고 아래 「등재 대기 항목」 체크리스트가 이미 #22~#24를 BE-04에 묶고 있어
> **BE를 유지하고 AI를 옮겼다.** 리포 안의 참조는 전부 갱신했으나, 이 날짜 이전에 작성된
> **GitHub 이슈·PR 코멘트는 수정할 수 없으므로** 아래 대응표로 읽는다.
>
> | 예전 표기 (AI 파트) | 현재 | 내용 |
> |---|---|---|
> | #21 | **#38** | 창업비용 통계 소스 2종 확정 (AI-02e) |
> | #22 | **#39** | 창업비용 단위 = 만원 · 가맹점 편의 미보정 |
> | #23 | **#40** | 인테리어·시설비 업종 상수 (카페 2,485 · 음식점 4,595) |
> | #24 | **#41** | AI-05 산출 산식 확정 |
> | #25 | **#42** | AI-05-2 실덤프 적재 검증·캘리브레이션 |
>
> BE 파트의 #21~#25(transit LEFT JOIN · 상환기간 T · 상환 여력 상한 · collateral 보류 ·
> 조달 시나리오 조합)는 **번호가 그대로다.**

> **2026-07-26 중복 해소 — AI 파트 #43~#49 → #50~#56**
> AI(07-25~26 작업)와 BE(07-25 develop 병합분)가 #43~#49를 동시에 잡았다. 채번 하드룰
> 「나중에 병합되는 쪽이 재채번」에 따라 **BE를 유지하고 AI를 옮겼다.** 리포 안의 상호참조
> (`ai/batch/load/finance.py`·`ai/eval/common.py`·`ai/eval/suites/grounding.py`·부록 1)는
> 같은 커밋에서 함께 갱신했다. 이 날짜 이전 GitHub 이슈·PR 코멘트는 아래 표로 읽는다.
>
> | 예전 표기 (AI) | 현재 | 내용 |
> |---|---|---|
> | #43 | **#50** | 원문 청크 PDF 인쇄 머리말·꼬리말 제거 |
> | #44 | **#51** | 권리금 비례계수 단가 비 교체 (코드리뷰 #1) |
> | #45 | **#52** | 업종별 환산임대료 `initial_cost.monthly_rent` (코드리뷰 #2) |
> | #46 | **#53** | 근거 인용 '첫 문단' 폴백 제거 (코드리뷰 #3) |
> | #47 | **#54** | 원문 청크 NUL 제거 (코드리뷰 #4) |
> | #48 | **#55** | 근거 충실도 지표 재정의 (코드리뷰 #5) |
> | #49 | **#56** | `gu_avg` 자치구 평균 구현 (코드리뷰 #8) |

## 등재 대기 항목 (스펙에서 예약됨 — 해당 태스크에서 채울 것)

- [x] AI-01: 상권 구획도(2024-10-31판) ↔ 최신 분기 통계 상권코드 정합성 대조 결과 (리스크 #18) → 등재 #12
- [x] AI-01: 역사마스터 좌표계 확인 결과 (t-data 교체 여부 포함) → 등재 #14 (교체 불필요)
- [x] AI-01: θ(부담률 임계)·θ'(확장 임계, 초기값 θ+5%p) 출처 문헌 — 실패 시 "통용 기준+조정 가능" 표현 → 등재 #15 (후퇴안 적용)
- [x] AI-02: 환승역 정규화·승하차 합산 규칙 → 등재 #4 (`transit.py`). 일→연·월 일평균 산출은 AI-05(접근성 성분)에서 확정
- [x] AI-02: 인허가 카페·음식점 재분류 매핑 → **확정본 등재 #20** (초안 #5는 폐기). 837→247 개편은 이 파이프라인 비적용
- [x] AI-04: 공간 조인 폴백 발동 내역 (임대료 자치구 평균 fallback / 접근성 0 처리) → 등재 #18·#19
- [x] AI-05: 거리 감쇠 파라미터(기준 500m) 근거 / w1 성분 결합·정규화 규칙 / 가중치 값 → 등재 #6·#41
- [x] AI-05: 권리금 "연간 조사(전년 기준)" 라벨·수치 / 예비 운영자금 = 월고정비×6개월 관행 출처 → 등재 #1(라벨 정본)·#6(×6 관행)·#41 ④⑤(산식)
- [x] AI-08: 검증 모델 프로토콜 고정 선언 (피처 목록·타깃·CV 설계·지표 — **결과 확인 전 등재**, §12-2)
      → 등재 **#35** (프로토콜·게이트) · **#36** (실행 결과 = 게이트 B). 등재 커밋이 모델 구현
      커밋보다 앞선다 (`fa4be7a` → `54539db`)
- [x] BE-04: 상환기간 가정 라벨 (상품 조건 우선, 부재 시 보증 가정 5년 + 화면 병기) → 등재 #22
- [x] BE-04: 상환 여력 상한 규칙 — 한계 조달 월 상환액 m ≤ `monthly_investable`(진단 폼 신규 필드) → 등재 #23.
      담보 제공 가능 여부(`collateral_available`)는 상품 담보 요구 컬럼 부재로 **미결선 보류** → 등재 #24
      (DECISIONS.md §8)
- [x] BE-05: θ′ 적용 조건 (m=0이면 θ, 필터2와 완전 동일) → 등재 #43
- [x] BE-05: 인사이트 목적함수 가중치(1,1,2)·품질 임계 완화·동점 규칙 + 최대차입 편향 발견사항 → 등재 #44
- [x] BE-05: 변동금리 rate=null → m 미산출·경계 제외(RATE_UNKNOWN) → 등재 #45
- [x] BE-05: `scenarios_explored` = 평가한 경계 수 합(A1+A4, 커버 실패 포함) → 등재 #46
- [x] BE-05: 탐색 축 A1·A4만 실행(A2·A3 미구현) → 등재 #47
- [x] BE-05: composition→상품 사용액 역매핑(동일 type 한도 큰 순) → 등재 #48
- [x] BE-05(C안): 계약 D8 `rate_type`·`rate_note` DTO 노출 + 상품 정렬. `marginal_payment_note` 이월 사유 → 등재 #49

## 등재 내역

| # | 날짜 | 담당 | 가정 | 근거 | 영향 |
|---|---|---|---|---|---|
| 1 | 2026-07-19 | CM | 권리금 통계는 연 1회(전년 기준) 발행 — 분기 아님. 화면·문서 전면 "연간 조사(전년 기준)" 명시 | 스펙 §2-0 사전 검증 | 초기비용 블록, 화면 라벨 |
| 2 | 2026-07-21 | CM | **부동산원 임대동향 API는 data.go.kr에서 직접 발급하지 않는다.** data.go.kr 카탈로그(15099345 '상업용부동산 임대동향'·15134761 '부동산통계 조회 서비스')는 둘 다 'API 유형: LINK'로 R-ONE(부동산통계정보시스템, reb.or.kr/r-one)으로 연결만 함 → 활용신청·포털 인증키 없음. 인증키는 R-ONE 회원가입 후 'Open API 인증키 신청'으로 발급. 호출: REST `…/r-one/openapi/SttsApiTbl.do?KEY=&STATBL_ID=`, 한 키로 STATBL_ID만 바꿔 임대동향·실거래가지수 조회. **.env 변수명 `DATA_GO_KR_API_KEY_REB_RENT`은 수집 스크립트 호환 위해 유지(값은 R-ONE 키)** | data.go.kr 15099345/15134761 상세(LINK) + R-ONE openApiIntroPage 실사 | AI-01 실사, AI-02 `reb_rent.py` 수집 경로. ⚠️ 스펙 §2-0 표의 "신규 API(15099345)로 신청" 문구는 팀 합의 후 정정 필요(현재 미수정) |
| 3 | 2026-07-21 | CM | **인허가 데이터 소스 이전 + 좌표계 실측.** localdata.go.kr 폐쇄(2026-04-16)로 인허가 CSV는 data.go.kr에서 수집(일반음식점 15045016·휴게음식점 15006730, CP949, 39컬럼). **좌표계 실측 3종: 인허가 `좌표정보(X/Y)`=EPSG:5174, 부동산원 상권구획도 SHP=EPSG:5179(.prj), 서울 상권영역=EPSG:5181 → 모두 WGS84 변환 필요.** 인허가는 폐업 포함 누적(일반 53.6만·휴게 14.6만행, 폐업 75~78%)이라 경쟁밀도는 `영업상태명=='영업/정상'`만 사용. 카페=휴게 中 `커피숍·다방`, SHP는 전국 368폴리곤→서울(`시도코드=='11'`) 필터 | ai/data/raw 실측(`ai/data/raw/README.md`) | AI-04 공간조인, `permits.py`·`reb_rent.py`. ⚠️ 스펙 AI-04 "인허가 좌표(WGS84)" 가정은 5174로 정정(팀 합의 후 스펙 반영) |
| 4 | 2026-07-21 | AI | **환승역 정규화·승하차 합산 규칙.** 역명에서 괄호 부기·호선 접두/접미·말미 '역'을 제거한 정규명으로 그룹핑해 승·하차를 합산한다(같은 물리 역이 노선별 복수 행 → 접근성 분모 V 합산). 예: `왕십리(성동구청)`→`왕십리`, `4호선 성신여대입구`→`성신여대입구`, `서울역`→`서울`. 일평균은 **최근 28일(요일 편차 제거) 중 그 역이 등장한 일수로 나눈 값**이다. 역사마스터가 옛 이름을 쓰는 개명 역은 별칭표로 맞춘다(`신천`→`잠실새내`) — 표기가 비슷해도 다른 역(2호선 뚝섬 ≠ 7호선 뚝섬유원지)은 넣지 않는다 | `ai/batch/collect/transit.py`(`normalize_station`·`aggregate_riders`·`STATION_ALIASES`) | AI-05 접근성 `V×exp(−d/500m)`, transit 수집. ⚠️ 승하차 서비스명·필드명(`CardSubwayStatsNew`/`SBWY_STNS_NM` 등)은 라이브 실행 전 데이터셋 페이지 재확인 |
| 5 | 2026-07-21 | AI | **인허가 카페·음식점 재분류(초안).** 경쟁밀도는 `영업상태명=='영업/정상'`만. `업태구분명` 기준 cafe={커피숍,다방}, other(제외)={편의점,백화점,슈퍼마켓,기타휴게음식점}, 그 외 food. 837→247 신분류 코드 정합·과자점/주점 포함 여부는 AI-02 확정 후 갱신 | `ai/batch/collect/permits.py`(`classify_category`) | AI-04 경쟁밀도 공간조인 |
| 6 | 2026-07-21 | BE | 결정적 도구 계층 기본 파라미터: θ(부담률 임계)=**0.15**, 예비 운영자금=월고정비×**6개월**, 교통 감쇠=승하차×exp(−거리/**500m**), w1 수요=3성분 백분위 **균등 평균**(스펙 "균등 합"→[0,1] 유지 위해 평균 채택), 업종 프리셋 가중치=(w1 .30·w2 .20·w3 .20·w4 .15·w5 .15, 카페·음식점 공용) | 스펙 §4-1·§4-3·expl §2-1. θ·감쇠·가중치 실값은 상단 대기 항목(AI-05·θ 문헌)에서 확정 → 확정 시 상수만 교체 | ScoreLookup·CostCalculator·ReverseCheck·LocationService |
| 7 | 2026-07-21 | BE | 데모 후보/상품 픽스처(`DemoCandidates`·`DemoProducts`)는 **입력값**만 설계: 예산 8,000·θ 0.15에서 expl §8 판정(🟢2·🟠1, A-9999 조건부·갭 1,320) 재현. 판정·비용·점수 등 **출력은 전부 engine이 계산** | BE-03f 목→실 결선 (입력만 픽스처) | recommend·check-area 응답 — BE-02(AI-03 DDL)에서 사전 적재 테이블 조회로 대체 |
| 8 | 2026-07-21 | BE | **부담률은 파생값이다**: `burden_ratio = 환산임대료(만원/월) ÷ 월 추정매출(만원)`. 픽스처 상수였던 값을 스펙 §4-2 정의대로 분모·분자에서 계산하도록 교체 → 픽스처는 `monthlyRent`·`estSales`·`dailyFloating` 원자재만 보유. **종합점수 노출값 = `round(Σ wᵢ·축ᵢ × 100)`** (0~100 정수, 계약 `score`) | 스펙 §4-2·§4-3, DECISIONS.md §10 | `CandidateArea`·`ReverseCheck.burdenRatio`·recommend·budget 프리뷰. AI-05 산출 테이블은 이 3개 컬럼을 제공해야 함 |
| 9 | 2026-07-21 | BE | 데모 픽스처의 **환산임대료·추정매출은 실측 수준(임대료 198~456만·매출 1,600~2,400만)으로, 월 고정비(`monthlyFixedCost`)는 데모 예산 8,000만 재현을 위해 축소 보정된 값으로 각각 설정**한다. 둘은 현재 정합하지 않으나 `monthlyFixedCost`는 API에 노출되지 않고 예비 운영자금(×6) 산출에만 쓰인다. 픽스처 내부 순서 정합(매출 순위=w2 순위, 매출÷임대료 순위=w5 순위)은 유지 | expl §8 데모 예산이 8,000만으로 고정되어 있어, 실측 수준 고정비(×6 버퍼)로는 후보 전원이 범위 외가 된다 | `DemoCandidates` — BE-02 실데이터 전환(CP2) 시 동시 해소 |
| 10 | 2026-07-21 | AI | **서울 상권분석 OpenAPI 서비스명 8종 실측 확정** (전건 실호출 INFO-000): 추정매출 `VwsmTrdarSelngQq`(55컬럼) · 길단위인구 `VwsmTrdarFlpopQq`(27) · 상주인구 `VwsmTrdarRepopQq`(29) · **직장인구 `VwsmTrdarWrcPopltnQq`(26)** · 집객시설 `VwsmTrdarFcltyQq`(25) · 점포 `VwsmTrdarStorQq`(14) · 상권변화지표 `VwsmTrdarIxQq`(11) · **상권영역 `TbgisTrdarRelm`(11)**. 골격 코드의 `VwsmTrdarWrcpopQq`·`VwsmTrdarArea`는 **미존재(ERROR-500)** 였음 → 교정. 조인 키는 전 데이터셋 공통 `TRDAR_CD`(상권코드), 분기 필드 `STDR_YYQU_CD`. 제공 분기 = **20211~20261(21분기)**, 최신 **20261**. 수집 범위는 팀 결정에 따라 **최신 4분기(20252·20253·20254·20261)** | `ai/data/raw/README.md` §6 실측 로그 (2026-07-21 전건 호출) | AI-02a 수집, AI-03 DDL 컬럼, AI-05 점수화 입력 |
| 11 | 2026-07-21 | AI | **분기 경로 파라미터가 서비스마다 다르게 동작한다.** `/{KEY}/json/{SERVICE}/{s}/{e}/{STDR_YYQU_CD}/` 필터는 Selng·Flpop·Stor·Ix에서만 적용되고, **Repop·WrcPopltn·Fclty는 무시**되어 전건이 반환된다(필터 20261 요청에 20241·20251 행이 섞여 옴). → 후자 3종은 **전건 수집 후 클라이언트 측 분기 필터**로 처리한다 | 필터별 반환 분기 분포 실측 (Repop 20261 요청 → 20241:1·20251:999) | `seoul_commercial.py` 수집 전략, 재실행 시간 |
| 12 | 2026-07-21 | AI | **리스크 #18 해소 — 구획도 ↔ 임대료 통계 정합성 전건 대조.** ⚠️ 구획도 SHP의 `지역코드` 컬럼은 **서울 72건 전부 NULL**(전국 368건 중 28건만, 그마저 시군구 코드) → **조인 키는 상권명 문자열**이다. R-ONE 임대동향 상권 항목(`ITM_FULLNM` = `서울>권역>상권`)과 정규화 대조(`/`·`·`·공백 제거) 결과: **소규모 59 · 중대형 68 · 집합 40, 합집합 = 72로 SHP 서울 전건 커버, 미커버 0**. R-ONE에만 있고 SHP에 없는 상권도 0. → 임대료 할당 우선순위 **소규모 → 중대형 → 집합**. 소규모 미제공 13곳(구로디지털단지역·구의역·낙성대·노원역·명일역·방배역/내방역·서래마을·쌍문역·양재말죽거리·양재역·여의도·학동/강남구청역·한티역) 중 노원역·명일역·여의도·한티역은 중대형도 미제공 → 집합 사용. **자치구 평균 폴백은 "항목 부재"가 아니라 "해당 분기 값 결측" 시에만 발동** | R-ONE `SttsApiTblItm.do` 항목 전건 + `최종상권368.shp` 서울 필터 대조 (2026-07-21) | AI-04c 임대료 할당·`fallback_flag`, 화면 임대료 출처 배지 |
| 13 | 2026-07-21 | AI | **R-ONE 엔드포인트 정정 + STATBL_ID 확정.** 통계표 목록 `SttsApiTbl.do`(738건 카탈로그) / 분류 항목 `SttsApiTblItm.do` / **실데이터 `SttsApiTblData.do`** — 골격 코드가 `SttsApiTbl.do`로 데이터를 받으려 한 것은 오류. 확정 ID: 임대료(2024-3Q~) 소규모 `T248223134698125`·중대형 `T244363134858603`·집합 `T244913134948657`, 전환율 소규모 `T246253134905233`·중대형 `T241883134877452`·집합 `T243133134985812`, 공실률 소규모 `T241833134686576`·중대형 `T249633134845544`, **권리금(매년) `A_2024_00445`**(시도별/업종별, 2022~). 임대료·전환율·공실률은 **분기**, 권리금은 **매년** 주기 → 화면 라벨 분리(등재 #1) | R-ONE 카탈로그 738건 실수집 (2026-07-21) | AI-02c `reb_rent.py`, AI-05 보증금 역산·권리금 블록 |
| 14 | 2026-07-21 | AI | **역사마스터 좌표계 확정 — t-data 교체 불필요.** OA-21232 `subwayStationMaster`는 `LAT`·`LOT` 필드로 **WGS84 위경도**를 직접 제공한다(서울역 37.556228 / 126.972135 = 실좌표 일치). 784행이며 수도권 전철 포함이라 위도 36.77~38.10 범위 — 서울 밖 역은 최근접 조인에서 자연 배제된다. 승하차 `CardSubwayStatsNew`는 **경로 파라미터 `USE_YMD` 필수**(미지정 시 ERROR-300); 20260718 조회 시 `REG_YMD` 20260721 = **3일 지연 갱신** 확인. 필드 `SBWY_STNS_NM`·`GTON_TNOPE`·`GTOFF_TNOPE`는 골격 추정치와 일치 | 서울 열린데이터광장 실호출 (2026-07-21) | AI-02b `transit.py`, AI-04d 최근접 역 조인, README 라이선스 표(t-data 미사용 확정) |
| 15 | 2026-07-21 | AI | **θ 문헌 확보 실패 → 스펙 후퇴안 적용.** 국가승인통계(소상공인실태조사 등)에 "적정 임차료 부담률 임계"는 존재하지 않는다(실태조사는 애로사항 응답률만 제공). 업계 통용 기준은 "월 임차료 ≒ 영업 3일치 매출"(≈ 매출 대비 10% 내외)이며 출처가 상업 매체·커뮤니티 수준이라 인용 근거로 부적합. → **θ=0.15 유지**(통용 기준 10%에 여유를 둔 값), **θ'=θ+5%p=0.20**. 화면·문서 표현은 **"통용 기준이며 조정 가능"** 으로 고정하고 특정 문헌을 근거로 제시하지 않는다 | 웹 문헌 탐색 결과 (2026-07-21) — 공식 통계 부재 확인 | 필터 2·2′ 임계, 근거 패널 문구, 예상 Q&A. 등재 #6의 θ 실값을 확정 |
| 16 | 2026-07-21 | AI | **Track A 종료 → Track B(부동산원) 확정.** 열린데이터광장에서 상가 임대시세 데이터셋을 확인하지 못했다. 스펙 §2-1대로 미존재 시 즉시 종료이므로 임대료는 **부동산원 단일 트랙**으로 확정한다(전환율·권리금과 출처 통일 유지). 화면 라벨 "한국부동산원 ○○상권 분기 평균 (추정)" 강제 | 스펙 §2-1 Track A 조건부 절차 | 임대료 소스 확정, 일정 영향 0 |
| 17 | 2026-07-21 | AI | **상권 폴리곤은 OpenAPI에 없다 — 별도 SHP 확보.** `TbgisTrdarRelm`은 중심점(`XCNTS_VALUE`·`YDNTS_VALUE`)과 면적(`RELM_AR`)만 제공하고 폴리곤 geometry가 없다. 스펙 §3-1 [1단계] 점-폴리곤 조인을 위해 열린데이터광장 OA-15560 파일(ZIP→SHP, 2023-10-20판)을 받아 `ai/data/raw/서울상권영역_SHP/`에 배치했다: **1,650 폴리곤(Polygon 1,561 + MultiPolygon 89), EPSG:5181, 인코딩 UTF-8(.cpg)**, `TRDAR_CD` 보유로 API 1,650건과 개수 일치. ⚠️ SHP는 2023-10판이고 통계는 2026-1Q → **상권코드 차집합은 AI-04a에서 재확인** | data.seoul.go.kr OA-15560 파일 다운로드 + geopandas 실측 (2026-07-21) | AI-04b 경쟁밀도 점-폴리곤 조인, AI-04a 좌표계 통일 |
| 18 | 2026-07-21 | AI | **스펙 §3-1 임대료 할당 5등급화 (✅ 스펙 v6.3에 반영 완료).** 부동산원 구획 72개는 서울 상권 1,650개 **면적의 32%**(53.4/164.8㎢)만 덮어, 스펙대로 "대표점이 구획 내부"만 인정하면 폴백이 **77.2%**가 되고 임대료가 사실상 25개 자치구 값으로 붕괴한다. 최근접 구획까지 거리 중앙값이 380m라 **할당 5등급**으로 확장한다: `exact`(내부) / `approx`(최근접 ≤**500m**, 도보 5~7분 — 같은 임대 시장으로 간주) / `dong`(같은 **행정동**에 구획이 있음 — 생활권 동일) / `gu_avg`(자치구 내 구획 평균, `fallback_flag`) / `region_avg`(자치구에 구획이 하나도 없는 **성북구·양천구** → R-ONE 서울 권역(도심·강남·영등포신촌·기타) 평균, `fallback_flag`). 실측 분포: exact 379 · approx 560 · dong 109 · gu_avg 524 · region_avg 78 → **폴백 602개(36.5%)**. **폴리곤 면적 중첩 기준은 검토 후 기각** — 조금이라도 겹치는 상권(641개)은 이미 전부 `approx` 안에 들어와 있어 추가로 건지는 상권이 **0개**였다. `dong`은 거리 임계를 늘리는 대신 기준을 바꿔 얻은 것으로, 남는 폴백 602개는 최근접 구획까지 중앙값 1,149m라 정직하게 폴백으로 둔다 | 부동산원 구획 면적 커버리지(53.4/164.8㎢)·최근접 거리 분포·면적 중첩률 실측 (2026-07-21). 임계 500m는 스펙 §2-1 "임대 수준은 광역 상권 해상도" 서사와 정합 | `rent_join.py`, `rent.fallback_flag`, 화면 임대료 출처 배지. **스펙 v6.3 §3-1에 반영 완료** |
| 19 | 2026-07-21 | AI | **공간 조인 3단계 검증·폴백 실적 (스펙 §3-1 DoD).** [1] 경쟁밀도: 인허가 카페·음식점 150,751건 중 좌표 유효 148,384, 상권 폴리곤 내부 **120,521건(81.2%)**, 폴리곤 밖 27,863건. ~~**상권 폴리곤 53쌍이 상호 중첩**해 11,782건이 두 상권에 잡히므로 `area_code` 기준 1건으로 정리한다(정리 전에는 밀도가 부풀려짐).~~ **⚠️ 이 규칙은 2026-07-29 등재 #99 가 철회했다** — 밀도는 상권마다 독립 계산되므로 「부풀려짐」이라는 전제가 틀렸고, `area_code` **사전순** 정리가 중첩 상권 중 코드가 작은 쪽에 인허가를 몰아줘 명동거리·북창동·을지로입구역·방이동먹자골목의 w3(경쟁여유)를 1.0 으로 뒤집었다. 현행은 **중첩 상권 양쪽에 각각 계상**한다. 또한 이 행의 「11,782건」은 업소 수가 아니라 **조인 행 수**이며 해당 업소는 그 절반이다(단위 혼동). 검증: 강남역 상권 cafe 144개·food 1,035개, 업소 수 순위 3/1,353·5/1,597 — 실제 밀집도와 정합. [2] 임대료: 등재 #18 (5등급, 폴백 602개 36.5%). 검증 3곳 전부 `exact`(망원역 1번→망원역 / 강남역→강남대로 / 홍대입구역 3번→동교·연남). [3] 최근접 역: 거리 **중앙값 414m**, 폴백 **23개(1.4%)** — 전부 "승하차 미매칭"이고 거리 초과(>3,000m)는 0. 승하차 미매칭 역 138/784개는 대부분 수도권 전철·신분당선(운영사가 달라 OA-12914 미포함). 검증: 강남역→강남 24m·156,554명 / 망원역 1번→망원 237m·33,973명 / 홍대입구역 3번→홍대입구 268m·197,431명 | `python -m batch.preprocess` 실행 로그 (2026-07-21) | AI-05 점수화 입력, `store_density`·`rent`·`transit` 적재 |
| 20 | 2026-07-21 | AI | **인허가 업종 재분류 확정본 (등재 #5 초안 대체).** 기준은 **한국표준산업분류 I56 음식점 및 주점업**이다. `cafe` = I5622 비알코올 음료점업 = {커피숍, **까페**, 다방, 전통찻집, 떡카페, 아이스크림} / `food` = I561 음식점업 + I5621 주점업 (한식·중식·일식·경양식·분식·호프/통닭·정종·대포집·소주방·감성주점·제과점 등) / `other`(제외) = 비요식·시설 내 매점 = {편의점, 백화점, 슈퍼마켓, 철도역구내, 극장, 관광호텔, 유원지, 공항, 고속도로, **키즈카페**}. 초안 대비 교정 2건: ① **`까페`(일반음식점 표기, 1,272건)가 food로 새던 것을 cafe로** ② 키즈카페는 실내놀이터업이라 카페 경쟁에서 제외. 주점은 KSIC상 I56 소속이라 `food`에 포함한다. 실적: 영업중 157,279건 → cafe 17,619 · food 132,876 · other 6,784. ⚠️ **소진공 업종분류 개편(837→247)은 소진공 상가업소 데이터의 코드 체계**이고 이 파이프라인은 행안부 인허가 CSV의 `업태구분명`(한글 명칭)을 쓰므로 **비적용**이다 | 영업중 업태구분명 전건 분포 실측 (2026-07-21) + KSIC I56 | `permits.py` `classify_category`, AI-04b 경쟁밀도 |
| 21 | 2026-07-23 | BE | **v_candidate_area 조회 시 transit LEFT JOIN 실패(역 미매칭 상권) 처리.** RowMapper(`CandidateRowMapper`)가 조인 실패를 `transit_fallback`(원천 `transit.fallback_flag` — transit 테이블 유일 NOT NULL 컬럼)의 `wasNull()`로 판별해, 실패 시 `transit_fallback=true`·`transit_distance_m=0`·`transit_daily_riders=0`·`transit_station/line=""` 로 매핑한다(`nearest_station`·`distance_m` 은 원래 nullable이라 판별 기준에서 제외). 비용은 원재료 4블록만 매핑하고 합계(cost_ex/incl_premium)는 CostCalculator가 계산 | 스펙 §3-1 "공간조인 3단계 실패 → 유입 성분 0 + 플래그" + 뷰의 `LEFT JOIN transit` | recommend·check-area 교통 표시·접근성 성분, 실데이터의 역 없는 상권 (데모 픽스처는 전 후보 역 보유라 미노출) |
| 22 | 2026-07-24 | BE | **상환기간 T 가정**: 조달 명세의 상환기간은 **상품 조건(`term_months`) 우선**, **부재(null) 시 60개월(5년)** 을 적용하고 결과에 `termAssumed=true` 플래그를 남겨 **화면에 가정임을 병기**한다. 월 상환액은 원리금균등 `m = P·i/(1−(1+i)⁻ⁿ)`, `i = 연이율/12`이며 **이율 0이면 원금 균등분할**(P/n)로 계산한다. 금액은 만원 단위, 금리는 연 % | expl §2-2 "상품 조건 또는 보수 가정 5년 — assumptions.md 등재", §7 "월 상환액 표기 시 가정(금리·기간) 병기 필수" | `Loan.DEFAULT_TERM_MONTHS` · `FundingCheck` · 조달 명세 화면 표기 |
| 23 | 2026-07-24 | BE | **상환 여력 상한 규칙**: 한계 조달의 월 상환액 **m ≤ `monthly_investable`**(진단 폼 월 투자 가능액, 만원). 초과하면 그 경계는 **커버 실패로 처리해 보고에서 제외**한다 — 조달은 가능하나 상환이 불가능한 경계를 기회로 제시하지 않기 위함이다(expl §2-2 "커버 불가 경계는 보고 제외"의 상환 여력 확장). `monthly_investable` 미기재(null)면 상한 없음으로 본다. m 은 원리금균등 결정적 계산이며 LLM이 산출하지 않는다 | expl §2-2·§2-3, DECISIONS.md §8 (BE-01a 수집 → BE-04 결선) | `FundingCheck.cover` · T1 인사이트 경계 필터(BE-05 송출) |
| 24 | 2026-07-24 | BE | **`collateral_available`(담보 제공 가능) 조달 검증 반영 보류.** 진단 폼으로 수신·세션 기록은 하되 **판정에는 반영하지 않는다** — 상품 측에 "담보 요구 여부"를 나타내는 필드가 어디에도 없기 때문이다(`engine.FundingProduct`·`Eligibility`는 물론 **`db/init/01_schema.sql`의 `finance_product` 테이블에도 해당 컬럼이 없음**: max_age·industries·regions·pre_startup_only·amount_max·rate·term_months·exclusive_group·status만 존재). 상품별 담보 요구가 없으면 "담보 가능 여부"로 걸러낼 대상이 특정되지 않아, 규칙을 만들면 근거 없는 추정이 된다. **AI-06 정책자금 구조화에서 담보/보증 요구 필드가 확정되면 결선**한다. 그 전까지 화면은 이 값이 결과를 바꾼다고 표기하면 안 된다(계약 문서의 "현재 판정 미사용" 유지) | 상품 스키마 전수 확인 (2026-07-24) + BACKEND.md BE-04 "규칙 확정 시 등재" | `FundingInput.collateralAvailable`(수신만) · 조달 검증 · 화면 문구. **AI-06 컬럼 추가 시 재개** |
| 25 | 2026-07-24 | BE | **조달 시나리오 조합 규칙 + 상품 종류(type) 매핑.** ⚠️ **2026-07-26 개정 (이슈 #87·#90, DECISIONS §13-2·§13-3)** — 구 규칙 「보수 = 보증형 중 한도 최대 / 적극 = 전체 중 한도 최대」는 실적재 상품(한도 8억 보증 3건)에서 소상공인 카페 창업에 8억을 제시했다. **「부족분을 커버하는 최소 한도」로 교체**한다: 부족분 = 해당 업종 후보 상권의 진입 비용 중앙값 − 자기자본이며 **보수는 권리금 제외·적극은 권리금 포함** 중앙값을 쓴다(덮는 상품이 없으면 한도 최대 폴백). **보수/적극은 금액의 대소가 아니라 수단**이며(보증형 / 전체) 적극 ≥ 보수 보장은 없다. **슬라이더 초기값 = budget_min + 부족분**(구: budget_max). **type 판별은 상품명의 '보증' 우선, 그다음 기관명** — 기관명 단독 판별은 적재 org 가 '소진공'이고 KB 보증서대출이 정책자금으로 오분류돼 보증형 후보가 서울신보 2건만 남는 문제가 있었다. `budget_max = budget_min + Σ products[].amount_max` 는 그대로 성립한다 | 실데이터 기동 실측 · DECISIONS §13-2·§13-3 | `ScenarioBuilder`·`ProductType`·계약 `budget` 값 의미 |
| 26 | 2026-07-24 | AI | **AI-06 정책자금 gpt-4o 추출 — 검수 전 잠정 덤프 (`20_finance.sql`, product 34·chunk 5).** 공고문 12건 → gpt-4o(temperature 0, JSON 모드) 추출 52상품, 자동검증(필수·용어·범위) 통과 **34** 적재(KB 18·소진공 16). **org·source_url 은 LLM 아닌 문서 메타로 결정적 주입**(초기 org 0% 채움 → 문서 prefix 매핑). ⚠️ **전건 사람 검수 전 잠정** — 3가지 한계는 검수에서 해소: ① **소진공 변동금리 rate=가산금리만 추출**(effective=정책자금 기준금리+가산, `rate_note`에 "기준금리+X%p" 보존) → 표시 금리 과소, 검수 시 기준금리 반영 ② **청킹 coarse(5개)**: 자금명이 문단에 verbatim 부재 시 doc 첫 청크로 폴백 → source_quote 정밀도 낮음, 검수·청킹 정련 필요 ③ **서울신보 보증상품 0건**: 보증한도 미명시로 추출 스킵 → 검수 시 수기 보완. 검수대조표 `ai/data/finance/검수대조표.csv`(추적)가 AI-07 추출정확도 골드. LLM은 오프라인 배치·구조화만, 수치 생성 없음(스펙 §0-1). | gpt-4o 실추출 2회(org 주입 전후) + Postgres finance 적재(ON_ERROR_STOP) (2026-07-24) | `extract/funding_llm.py`·`load/finance.py`, `db/init/20_finance.sql`, 전건 검수 게이트, AI-07 골드 |
| 27 | 2026-07-24 | AI | **AI-06 코드리뷰 6건 반영 (S1~S6) — 재추출 후 product 21·chunk 6.** ① **S1(사일런트 갭)**: interim txt가 CID폰트/JS렌더로 깨진 문서(서울신보 6·KB상품목록)가 있어 추출 입력을 **raw PDF 네이티브(gpt-4o file 입력)** 로 전환 → **0건 문서 0개**(서울신보 8상품 복구, 이전 전무). 검수대조표에 **문서별 커버리지(0건 포함)** 섹션 추가로 누락 가시화. ② **S3**: 서비스 스코프 = 서울 → 프롬프트 서울 기준 필터(KB 지역재단 17행 지역난립 → 서울 1행), `product_type`(loan/guarantee)로 대출금리≠보증료율 구분. ③ **S4·S6**: 변동금리는 `rate=null`+`rate_note`(원문 표현 보존)로 통일, 자동검증에서 rate는 rate_note 있으면 정당 null 처리(고정금리만 필수). ④ **S2**: `(org, 정규화 자금명)` 중복 제거(금액 있는 행 우선) — 소진공 두 문서 중복 24→17. ⑤ **S5**: 검수대조표 필드별 편집 + `검수액션(유지/수정/드롭)` 컬럼. ⑥ **source_quote(§5-4)**: 깨진 txt는 verbatim 청크 불가 → **날조 금지, doc_chunk_ref=null**(클린 밀도 판정, 소진공만 청킹). ⚠️ **DDL `finance_product.rate` NOT NULL(D3 동결)** 때문에 변동금리도 DB엔 숫자 필요 → effective = **정책자금 기준금리(잠정 3.5%) + 가산(문서 추출값)**, 기준금리는 공시 상수라 §0-1 위배 아님. 검수에서 분기 확정치로 갱신. rate 평균: 소진공 3.78·KB 3.07·서울신보 2.12(보증료율). | gpt-4o PDF 네이티브 추출 검증(서울신보 창업기업 정독) + 재추출 + Postgres 적재(ON_ERROR_STOP) (2026-07-24) | `extract/funding_llm.py`·`load/finance.py`, `db/init/20_finance.sql`, 검수대조표 커버리지 |
| 28 | 2026-07-24 | AI | **⚠️ finance rate 조작 제거 → 스키마 A안(변동금리 기록 칸 추가, 3인 합의 대상).** 직전 잠정안은 변동금리를 "기준금리(임의 3.5%)+가산"으로 **숫자를 지어냈다** — 실측 결과 적재 21건 중 10건이 이 조작에 오염(6건 3.5+가산·4건 3.5 통째). 3.5는 어떤 출처도 없는 placeholder였다(§0-1 위배 소지). 근본 원인은 `finance_product`에 변동금리를 담을 칸이 없고 `rate NOT NULL` 뿐이라는 **동결 계약의 공백**. → **A안**: `finance_product.rate` **nullable**화 + `rate_type`(fixed/variable)·`rate_note`(원문 표현) 컬럼 추가. `rate_fields()`가 조작 없이 기록: 고정=숫자, 공시 절대값(예 KB "최저 연 3.62%")=그 값, "기준금리+가산"·순수 변동=**NULL + rate_note 원문**. 재적재 결과 fixed 11(rate 9)·variable 10(rate 2·null 8). 소진공 분기 기준금리 실값은 지어내지 않고 검수 시 소진공 공시값으로 채운다. **API_CONTRACT 변경**(rate nullable+rate_type+rate_note) 반영 — BE·리더 3인 합의·ratify 대기(변동금리를 표현 못 하던 계약 보완). | 적재 21건 rate 출처 전수 분류(근거 11·조작 10) + 스키마 A 재적재(ON_ERROR_STOP) (2026-07-24) | `db/init/01_schema.sql`·`load/finance.py`·`docs/API_CONTRACT.md`, BE finance 소비(BE-04/05) rate NULL 처리 |
| 29 | 2026-07-25 | AI | **AI-07 평가 하네스 = AI 기능 한정(추출·근거·민감도).** 결정적 `EligibilityFilter` 매칭은 BE 단위+통합 소관으로 하네스에서 제외(재구현 금지). ① **추출 정확도** = `extracted.json` 초안(29상품) ↔ 원문 PDF 대조 확정골드(`ai/eval/gold/extraction_confirmed.json`, 유지13·수정15·드롭1) 필드 일치율. 정규화: 배열=집합·null==빈배열·rate 반올림 3자리·수치 캐스팅. 실측 field_accuracy 0.907·정밀도 0.966·재현율 1.0, 약필드 amount_max 0.54·term_months 0.61. **재현율 1.0은 검수한 29상품 내 미추출 0 기준**(원문 전수 상품 센서스 대비 재현율이 아니며 부록 서술 시 이 범위 명시). **amount_max 0.54 일부는 지원사업안내 doc에 한도 미기재(②)에서 기인**하므로 순수 오추출이 아님을 부록에 병기. **서울신보 초안 amount_max 8~20× 과소(PDF "8억원 이내" 등)**·안심'동행'→'통장' 오추출을 검수에서 교정. ② **6 지원사업안내 상품**: 해당 doc엔 한도·기간 미기재라 LLM null이 그 문서엔 충실 → 확정골드는 **융자공고(동일 프로그램 원문)에서 개별한도 전사(수정)** 로 채워 파이프라인 실제 갭으로 계상(하향 안전·정직 수치). 성장기반자금은 공급규모만 있어 드롭. ③ **근거 충실도** = 청크 quote가 원문 txt 바이트 정확 부분문자열(§5-4), verbatim 1.0·커버리지 6/29(링크 청크 3 KB+3 소진공, 깨진 서울신보 6건 정당 null). ④ **민감도** = `location_score` 축값으로 종합 재계산, 가중치 ±20%(단일축 10)+θ∈{0.10,0.20}(2)=12섭동, 업종별 상위3 유지율(재정규화 없음) cafe 0.917·food 0.944 (2026-07-29 등재 #99 재산출 기준. 등재 당시 값은 cafe 0.972·food 0.944, 2026-07-27 2차 재산출 이후 값은 양쪽 0.972 였다). **부수 발견**: `SessionMapper`가 region 전체문자열("서울 마포구")을 필터에 전달 → `["서울"]` 상품 배제되는 잠재 버그 BE 전달. | `make eval` 실행(metrics.json+PNG)·pytest `test_eval_*`·확정골드 원문 대조 (2026-07-24~25) | `ai/eval/**`, 부록 1 성적표, BE 매칭 골드·region 정규화, AI-09 게이트 |
| 30 | 2026-07-25 | AI | **계약 #73 ratify 반영 + finance 검수 게이트 2건 등재.** BE(@Jongkwang131)·FE(@youngjun1227)가 `rate` nullable+`rate_type`+`rate_note`를 ratify(이슈 #73). FE 조건 4건을 `API_CONTRACT.md`에 반영: JSON 예시 3곳 `rate_type`/`rate_note`·분기 키 `rate_type`(DDL NOT NULL, 항상 존재) 명문화·`rate_note`/폴백 문구 서버 단일 통제(axis_labels 선례)·변동금리 `marginal_payment` 생략+`marginal_payment_note`(서버 송출)·`matching_products` `amount_max` desc 고정 정렬(금리 정렬 금지, 순위 조작 방지). `01_schema.sql` rate 주석의 "NULL=variable" 오등식도 정정(NULL≠variable, fixed도 숫자 미표기 시 NULL 가능). **검수 게이트 2건(`20_finance.sql`, 전건 검수 시 해소)**: ① **F-002·F-010이 `rate_type='fixed'`+`rate=NULL`** — #28 추출규칙("고정=숫자")의 반례라 검수에서 fixed/variable 재확정 필요(신규 계약은 `rate_type` 분기+`rate_note` 폴백이라 표시 자체는 안전). ② **F-010 `rate_note='신용보증 이수자: 연체된 5천만원 이내'`는 금리 표현이 아님**(doc_chunk 부재로 근거 없음) — "원문 그대로 표기" 시 금리 자리 오노출이라 검수에서 rate_note 교정/드롭 필수. 생성 SQL은 재생성 규약(파일 헤더)이라 수기 미편집, 검수 레이어에서 해소. | 이슈 #73 코멘트(BE·FE) 대조 + `20_finance.sql` 전수 확인 (2026-07-25) | `docs/API_CONTRACT.md`·`db/init/01_schema.sql`(rate 주석)·`20_finance.sql` 전건 검수, 검수대조표 F-002·F-010 |
| 31 | 2026-07-25 | AI | **#72 전건 검수본 → `20_finance.sql` 재생성(21→26) + `rate_fields` 의미 교정.** 확정골드(AI-07)를 `ai/data/finance/reviewed.json`(검수본 26건)으로 materialize(드롭 1 성장기반·**dedup 2**: 지원사업안내 "일시적경애로"·"재해피해" = 융자공고 동일 자금). `rate_fields()`를 **product_type 인지**로 재작성: rate=월 상환액(§4-2·§353)용 **연 대출금리(%)**, **보증료율·차감폭("은행금리에서 X% 차감")·보증금비율은 rate 제외**. ⇒ **F-010 rate=80% 오추출(임차보증금 80%) 제거→NULL variable**(#73 게이트 실해소), 보증 8건 재정의(차감/변동 6건 NULL variable, note에 고정 대출금리 명시 2건 미래성장 3.0·사회적경제 1.75 fixed). F-011 rate_note 보증한도 선두 문장 트림(금리부만, verbatim). **부수발견: 소진공 정책자금 기준금리 ≈ 3.85%(2026 1분기)** — 지원사업안내 일시적경애로 3.85(=융자공고 정의상 기준금리+0)에서 역산(혁신성장 4.25·소공인 4.45 정합). §0-1 준수로 소진공 변동 8건은 NULL+note 유지, **공시 확인 후 rate=3.85+가산 fill(#72 잔여)**. 결정: amount_max 80000(PDF 업체당 8억 유지)·지원사업안내 fixed 유지(스냅샷, 공시 검수 시 variable 재판정). doc_chunk 승인/추천은 공고 원문 verbatim(§5-4 정당). `run()`이 reviewed.json 우선 소비, rate_fields 4건 포함 **전체 55테스트·ruff 통과**. | reviewed.json 재생성 + pytest + 골드 원문 대조 (2026-07-25) | `ai/batch/load/finance.py`·`ai/data/finance/reviewed.json`·`db/init/20_finance.sql`, #72 잔여(기준금리 공시 fill·서울신보 source_quote 정련) |
| 32 | 2026-07-25 | AI | **서울신보 6종 원문 = 웹 HTML 재수집본을 정본으로 한다 (#72).** raw PDF는 브라우저 프린트본이라 폰트 ToUnicode 누락으로 텍스트 레이어가 모지바케(`폀옠6먗퓏핯…`)였다 — `funding_docs.looks_garbled()`가 경고하던 그 문서들. OCR(tesseract 미설치·재현성 저하) 대신 **1차 출처인 웹 본문**을 `batch/collect/funding_web.py`로 수집한다(내비게이션 제거, 재실행 가능). `requests`는 두 호스트 모두 `SSLV3_ALERT_HANDSHAKE_FAILURE`(레거시 cipher만 수용)라 `curl` 사용 — 검증은 끈 적 없다(`--insecure` 미사용). 상품↔문서 매핑 키를 보존하려 문서명은 기존 PDF stem과 동일하게 유지 | 스펙 §5-4 "인용은 검색이지 생성이 아니다", `ai/data/raw/서울신보_*.pdf` 추출 실패 | grounding coverage **6/29 → 13/29 (20.7%→44.8%)**, verbatim 1.0 유지 · `doc_chunk_ref` 전건 non-NULL · 부록 1 근거 충실도 행 |
| 33 | 2026-07-25 | AI | **골드셋의 `rate` 를 `rate_fields` 의미(#31)에 맞춰 정렬 — 보증상품 6건 교정 (#72).** 서울신보 골드가 `rate` 에 **보증료율**을 담고 있었다(ESG 1.0·안심통장 1.0·미래성장 1.0·일자리 1.0·사회적경제 0.5·장애인 0.7). 재수집 원문의 `[보증료]`/`[대출금리]` 라벨로 전건 대조해 → ESG·안심통장·일자리·장애인 = **null**(대출금리가 "은행금리에서 X% 차감" 차감폭이라 절대금리 미상), 미래성장 = **3.0**, 사회적경제 = **1.75**(원문 명시 고정 대출금리). 상품명 '안심동행'→'안심통장' 교정(바깥 `name` 은 초안 식별 키라 유지, `confirmed.name` 만 수정) | assumptions #31, 서울신보 웹 재수집 원문 | 부록 1 추출 정확도 `rate` 행 — 골드와 서빙값이 이제 일치 |
| 34 | 2026-07-25 | AI | **소진공 정책자금 기준금리 = 3.85%** (‘26년 3/4분기, 2026-07-10 적용) → 변동 11건 `rate` 확정 (#72). 융자공고 PDF는 "기준금리(분기별 변동) + 사업별 가산금리" 산식만 규정하고 수치를 홈페이지로 넘기지만, 그 「금리안내」 표가 **`소진공_소상공인정책자금_지원사업안내.txt` 에 이미 전문 수록**돼 있었다(별도 수집 불필요 — 중복 문서는 grounding 모수만 흐린다). 표의 **「금리」 열 값을 그대로** 옮긴다(기준금리+가산 직접 계산 금지, 반올림 불일치 방지): F-012 4.45 · F-014 3.85 · F-015 5.45 · F-017 5.45 · F-018 4.45 · F-019 4.25 · F-021 3.85, 기존값 유지 F-022~F-025. 2차 출처(블로그의 1·2분기 값)는 §0-1 미충족으로 미채택. **분기 변동값이므로 `rate_note` 에 적용 분기·시행일 병기 필수**이며, 값이 있어도 `rate_type='variable'` 을 유지한다(`rate_fields` 교정) | 스펙 §0-1·§4 기준일 표기, 융자공고 「융자한도 및 금리」 | `20_finance.sql` rate NULL **14→7**(남은 7건은 절대금리 미상 보증상품) · BE 월 상환액 산정 · 부록 1 |
| 35 | 2026-07-25 | AI | **AI-08 검증 모델 프로토콜·게이트 사전 고정 (결과 확인 전 등재 — 리스크 #19, 스펙 §12-2·§12-3).** ① **타깃** = `log(월 점포당 추정매출)` = `log(sales.monthly_sales / store_density.store_cnt)`, 분모·분자 0/결측 행 제외 ② **피처 6종** = 길단위 유동(`floating_pop.daily_floating`) · 상주인구(`resident_pop.resident_pop`) · 직장인구(`worker_pop.worker_pop`) · 교통유입(`transit.daily_riders × exp(−distance_m/500)`) · 경쟁밀도(`store_density.store_per_10k_m2`) · 상권변화(`change_index.oper_avg_months`). **`w2`·`w5`·`location_score.est_sales` 는 매출 파생이라 배제(누수)** ③ **분할** = `commercial_area.sigungu_code` 기준 `GroupKFold(n_splits=5)` — 공간 자기상관 탓에 랜덤 CV는 성능을 과대평가하므로 금지 ④ **지표** = fold **중앙값** R²·Spearman ρ·WAPE·MAE(만원)·베이스라인(train 자치구 평균 예측) 대비 개선, **MAPE 미사용**(저매출 구역 분모 폭발) ⑤ **SHAP** = 피처별 평균 SHAP 부호 vs 설계 부호(유동+·상주+·직장+·교통+·경쟁밀도−·상권변화+)를 **설계 축 단위로 집계**(w1 = 유동·상주·직장·교통 4피처 다수결 / w3 = 경쟁밀도 / w4 = 상권변화) → 판정 가능 축이 3개면 `n/3` 로 보고하고 게이트는 **비율**로 환산 ⑥ **LightGBM 하이퍼파라미터 고정** `n_estimators=400, learning_rate=0.05, num_leaves=31, min_child_samples=20, random_state=42` — 튜닝 금지(사후 조정 방지) ⑦ **게이트(§12-3)** A: R²≥0.30 ∧ ρ≥0.60 ∧ 방향일치비율≥0.8 / B: R²≥0.15 ∧ (ρ≥0.45 ∨ 방향일치비율≥0.8) / C: 그 외. **C 판정은 실패가 아니라 정상 동작**(부록 2 미수록·민감도 단독 유지 = v6.1 복귀) | 스펙 §12-2·§12-3, 리스크 #19 | 부록 2 수록 여부 · `make eval-model` · AI-09(#25) 판정 입력. **이 행의 커밋은 `eval/suites/model.py` 커밋보다 앞선다** |
| 36 | 2026-07-25 | AI | **AI-08 실행 결과 — 게이트 B, 그리고 w3(경쟁밀도) 설계 부호의 반증 (#21).** 프로토콜 #35 그대로 실행: 2,426행·25자치구, R² 중앙값 **0.1785**(베이스라인 −0.0030), Spearman ρ **0.5143**, WAPE 0.551, MAE 745만원, SHAP 방향 일치 **0/3축** → **게이트 B**. 단 B는 "방향 일치"가 아니라 **"순위상관(ρ≥0.45)" 경로로 성립**했으므로(`gate_path`), §12-3 표의 B 문안("방향 정합 확인")을 그대로 쓰면 사실과 어긋난다 — 부록 2는 **설명력 참고 + 방향 미확인**으로 서술해야 한다. **핵심 발견**: `store_per_10k_m2`(경쟁밀도)의 설계 부호는 −이나 실측은 +이며, 이는 SHAP 아티팩트가 아니라 원자료 Spearman **ρ=+0.481** 로 확인되는 데이터 신호다(집적효과 — 점포 밀도가 높은 상권일수록 점포당 매출도 높다). `resident_pop` 도 설계 + 대비 실측 −0.158. w1은 4피처 중 2:2 동수(다수결 미달)라 불일치 처리. **설계 부호를 결과에 맞춰 수정하지 않는다** — 사전 등록 게이트의 존재 이유가 이것이다. 이 반증은 부록 2·심사 Q&A의 소재로 그대로 싣는다 | `make eval-model` 실행 로그, `eval/out/metrics.json`·`shap_summary.png` | 부록 2 서술 수위(B 축소) · 스펙 §4-3 w3 가중치 부호 재검토 후보(D10 동결 이후이므로 본선 과제) |
| 37 | 2026-07-25 | AI | **추출 정확도 하락은 회귀가 아니라 정직화다.** 골드 교정(#33·#34) 후 `rate` 필드 정확도 0.857→**0.643**, 전체 field accuracy 0.907→**0.867**. 지표는 초안(gpt-4o `extracted.json`) ↔ 확정골드 대조이므로, 골드를 바로잡으면 초안의 실제 오류(보증료율을 rate 로 채택·소진공 분기금리 누락)가 그대로 드러난다. 부록 1은 이 낮은 값을 쓰되 "검수 게이트가 걸러낸 오류량"으로 서술한다 — 사람 검수의 가치를 보여주는 수치다 | `eval/out/metrics.json` 전후 비교 | 부록 1 「추출 정확도」 행 서술 |
| 38 | 2026-07-24 | AI | **창업비용 통계 소스 2종 확정 + 수집 스크립트 완료 (AI-02e).** 스펙 §4-1 「인테리어·시설비 = 업종 상수」는 **한 소스로 채워지지 않아 병용**한다 — 공정위는 **분해**(인테리어 성분 분리)를, KOSIS는 **총액 상한**을 준다. 수집기 `ai/batch/collect/startup_cost.py` 작성·실행(2019~2025, FTC 308행 + KOSIS 18행 `raw/startup_cost/`). ① **공정위 가맹정보** (15110293, `DATA_GO_KR_API_KEY_FTC_COST`) — `FftcSclasIndutyFntnStatsService/{getSclaIndutyFntnOutStats(외식15)·…SrvcStats(서비스22)·…WhrtStats(도소매7)}`. 업종이 `커피`·`제과제빵`·`음료(커피 외)`·`한식`·`분식`·`주점`으로 분리돼 카페/음식점 매핑 가능. **⚠️ 필드명이 실제 의미와 어긋나 있다 — 108행 합 항등식 `smtnAmt == frcsCnt + avrgFrcsAmt + avrgFntnAmt + avrgJngEtcAmt`(±1)로 실증**: `jnghdqrtrsCnt`=가맹본부 수(유일한 count)·`frcsCnt`=**평균 가맹보증금액(오배치·count 아님)**·`avrgFrcsAmt`=가맹비·`avrgFntnAmt`=교육비·`avrgJngEtcAmt`=**기타(필수설비+도면+인테리어+시공+초도물품)=인테리어·시설 프록시**·`smtnAmt`=합계. ② **KOSIS 소상공인실태조사** (`KOSIS_API_KEY`) — `orgId=142`·`tblId=DT_3ME0126`, `objL2=I/I56`·`objL3=10/123/5325`. 2024 전국 음식점및주점업 창업비용 **101백만**·본인부담 74백만, 서울 숙박및음식점업 115백만 | 2026-07-24 두 API 실호출 + 합 항등식 108행 검증 | AI-05 초기비용 인테리어·시설비 블록 |
| 39 | 2026-07-24 | AI | **✅ 창업비용 소스 미해결 3건 — 전부 해소.** ① **단위 = 만원 확정** (라벨 `(단위 :천원)`은 data.go.kr 메타 버그): 일식 smtnAmt 11,016·서양식 10,238은 **만원 해석 시 1.10억·1.02억**(풀서비스 프랜차이즈 실측 정합), 천원 해석 시 11백만(불가능·KOSIS 총액 대비 130배 낮음). 한식(만원) 0.78억 < KOSIS 음식점업 총액 1.01억 정합. → 수집기 docstring `COST_UNIT_MANWON=1`. ② **가맹점 편의 = 안전 방향이라 그대로 사용**: 프랜차이즈 기준이라 독립 대비 상향이나, "내 한도로 어디까지" 서비스에선 비용 과대 = 도달범위 과소 = **하향 안전 마진**(스펙 §0-1 철학)과 정합. 보정계수 미적용(근거 없는 할인 금지), 라벨에 "가맹점 기준(상향)" 명시. ③ **보증금 이중계상 없음**: 보증금은 `frcsCnt`(별도 필드)이고 인테리어 프록시로 쓰는 `avrgJngEtcAmt`(기타)에 **미포함** → 우리 보증금 블록(환산임대료 역산)과 안 겹침. **KOSIS 서울×I56 부재** → 서울 보정계수 = 서울I/전국I = 115/108 = **1.065**(총액용); 인테리어 시공비는 지역차 작아 **인테리어 상수엔 미적용**. **⚠️ ①단위·②편의처리 방침은 3인 합의 대상** (심사 감점 직결 수치라 §API_CONTRACT 준하는 합의 필요) | 2026-07-24 자릿수·KOSIS 교차 대조 | AI-05 산식·라벨, 심사 QA, 3인 합의 |
| 40 | 2026-07-24 | AI | **인테리어·시설비 업종 상수 잠정 확정 (2025 최신·만원).** 값 = FTC `avrgJngEtcAmt`(기타=인테리어·시설). **카페 = 2,485만원**(커피 단독; 커피군 3종 평균 3,045). **음식점 = 4,595만원**(외식 비카페 12종 기타 **중앙값** — 한식 6,636·평균 4,785보다 소규모 독립 음식점을 대표. 우리 permit food는 한식·분식·치킨·일반이 섞여 있어 단일 고가 업종보다 중앙값이 정직). ⚠️ **잠정치** — AI-05에서 permit 카테고리 분포로 음식점 대표값을 정련하고(가중평균 옵션), 최종 라벨에 출처("공정위 가맹정보 2025, 가맹점 기준")·기준일·단위(만원)를 명기한다. 전 15업종 시계열은 `raw/startup_cost/ftc_franchise_cost.json`에 보존 | `startup_cost.py` 산출 + 2025 외식 기타비용 분포 실측 | AI-05 초기비용 인테리어·시설 상수, 근거 패널 라벨 |
| 41 | 2026-07-24 | AI | **AI-05 산출 산식 확정 (백엔드 BE-02 소비계약 + 보유 데이터 기준 재도출, design 2026-07-24).** ① **대표면적** = 인허가 `소재지면적` 영업중 중앙값 **카페 44.0㎡·음식점 51.7㎡**(채움률 99.7%, `classify_category()` 업종별 — **2026-07-27 수치 정정, 등재 #84**. 정의는 처음부터 이 문안이었고 코드 상수만 대장 구분으로 산출돼 있었다). 환산임대료 `monthly_rent`(만원/월)=`단가(천원/㎡)×대표면적÷10`. ② **est_sales**(부담률 분모·표시) = `분기추정매출÷점포수÷3÷10000`(점포당 월매출 만원) — `THSMON_SELNG_AMT`가 요일별 합과 정확히 일치해 **분기합 확정**(/3 정당), 점포수=`store_cnt`. ③ **보증금** = `monthly_rent×관행배수(low 8·high 12)` — 인허가 `보증액`·`월세액` 채움률 **0%**라 실측 불가, 전월세전환율은 라벨 병기. ④ **권리금** = `72.6만/㎡(서울 숙박·음식점 2025 R-ONE)×대표면적×임대료비례(clip 0.5~2.0)×업종보정(카페 0.85·음식점 1.0)`, 구간 ×0.72(중위/평균)~×1.15. 권리금 유 비율 80.4%는 이중표기 근거로만. ⑤ **월 고정비(가산형)** = `환산임대료 + 표준인원×최저임금월(카페 1.5·음식점 2.0인 × 210만) + 공과금(환산임대료×0.20 잠정)`; 예비운영자금=×6. **⚠️ 종사자수 채움률 1.9~7%라 인건비는 실측 대신 최저임금×표준인원**. 공과금 0.20은 KOSIS 영업비용 구조로 확정 예정(Task A0). ⑥ **점수 w1~w5** 서울 백분위(업종별): w1=mean(pct유동,pct(상주+직장),pct교통유입), 교통유입=승하차×exp(−d/500); w2=pct(est_sales); w3=pct(−밀도); w4=pct(성장); w5=pct(est_sales/monthly_rent). BE `ScoreLookup`·`CostCalculator` 정의와 일치, 부담률·종합점수 미굽. | 인허가 소재지면적·종사자·보증액 채움률 실측 + 매출 요일합 검증 + BE engine 소스 대조 (2026-07-24) | `preprocess/cost.py`·`score.py`, AI-05 initial_cost·location_score, 근거 패널 라벨 |
| 42 | 2026-07-24 | AI | **AI-05-2 실덤프 적재 검증·캘리브레이션 (`10_data_core.sql`, 1,650 상권 → v_candidate_area 2,500행 cafe 1,061·food 1,439).** ① **THSMON=분기합 최종 확정**: 강남역 커피-음료 점포당 월매출 = 분기가정 /3 시 **3,827만원**(현실적), 월가정 시 11,481만원(비현실적·건수/점포 12,421도 분기 정합) → est_sales=분기÷점포수÷3 확정. ② **매출 업종매핑**: cafe=`CS100010`(커피-음료) 단독, food=그 외 `CS1xxxxx`(한식·중식·분식·치킨·제과점·호프 등). ③ **환산임대료 area-level = 음식점 55.2㎡ 기준**(rent DDL이 area PK라 업종 무관 단일값): 카페 부담률은 이 기준에서 상향(보수)이라 하향 안전 방향과 정합. 초기비용 블록의 보증금·권리금은 업종별 대표면적으로 별도 계산. ④ **부담률 분포 실측**: 중앙값 **0.27**, θ=0.15 이하 26%(≈650 area×industry 적합) — 서울 소상권 소규모 점포 매출 대비 임대료 실태로 정직·보수적. θ 민감도는 AI-07에서 검증(가중치±20%·θ 변동). ⑤ **성장순위(w4)** = 상권변화지표 LH(확장)3>LL(다이나믹)2>HH(정체)1>HL(축소)0. ⑥ 적재 검증: `01_schema+02_mock+10_data_core` 순차 적재 무오류(TRUNCATE로 실데이터가 mock 대체), 폴백 임대료 36%(등재 #18 정합). | Postgres 16 임시 컨테이너 적재(ON_ERROR_STOP) + 강남역 매출 자릿수 검증 (2026-07-24) | `load/serving.py`·`emit.py`, `db/init/10_data_core.sql`, BE-02 실데이터 전환 |
| 43 | 2026-07-25 | BE | **θ′ 적용 조건 — m=0이면 필터2와 완전히 동일.** 확장 부담률 임계 θ′=0.20(=θ+5%p, 등재 #15)은 **차입이 있을 때(m>0)만** 적용한다. **차입이 없으면(m=0) θ=0.15를 적용**해 필터 2′의 좌변 `(임대료+0)/매출`과 임계가 모두 필터 2로 축약된다 — expl §2-3의 "자기자본만 구간에서 필터 2와 동일" 문구를 **문자 그대로** 만족시키기 위함이다(좌변만 같고 임계가 다르면 판정이 갈린다). 이 일관성은 부담률 0.11~0.20 5개 구간에서 테스트로 못 박았다 | expl §2-3 일관성 조건 · 등재 #15 θ·θ′ 값 정밀화 | `SustainFilter.passes` · `SustainFilter.nSustain` · `InsightBuilder` 지속 후보 계산 |
| 44 | 2026-07-25 | BE | **인사이트 목적함수 가중치·컷·정렬.** `Q = 1·ΔN_green + 1·ΔS_top(등급 구간 차) + 2·ΔN_sustain`. **지속 계수 2배**는 "버틸 수 있는 곳"이라는 서비스 철학의 반영이며 문헌 근거가 아닌 **초기값(조정 가능)**. 품질 조건 임계는 스펙의 `ΔN_green≥2`에서 **`≥1`로 완화**(데모 후보 풀 규모에서 ≥2는 사실상 미발동). ΔS_top은 `ScoreLookup.score`를 그대로 쓰되 **등급 구간(10점) 차**로 환산해 Q의 다른 항과 단위를 맞춘다. **★발견사항**: ΔN_sustain이 경계마다 커져 **최대 차입 경계가 score 1위로 뽑히는 경향**이 있다(§0-2-4 "더 빌려라가 아니다"와 표면상 긴장). C 페널티를 임의로 키우지 않고, **상향 단독 노출 금지 + T2 안전마진 동반 + 고지 문구의 3중 방어**로 상쇄한다. **★동점 규칙**: score 동점이면 **갭이 작은 쪽(덜 빌리는 쪽)** 우선 → 그래도 같으면 경계값 오름차순. score를 **1e6 배율 정수 양자화**해 비교하므로 부동소수 잔차로 순위가 뒤집히지 않는다(전순서 보장, TimSort 계약 위반 방지) | expl §2-4 목적함수 · §0-2-4 · 5-A 실측(9,980이 1위로 뽑힘 확인) | `InsightScore` · `BoundaryEval` |
| 45 | 2026-07-25 | BE | **변동금리(확정 이율 미상) 처리 — 가정 금리 금지.** 금리를 모르면 월 상환액 m을 계산할 수 없으므로 **가정 금리를 만들지 않고 그 경계를 보고에서 제외**하며, 실패 사유를 `RATE_UNKNOWN`으로 남긴다(단순 한도 부족 `LIMIT_SHORT`와 원인이 다르다). `FundingProduct.rate`를 원시 `double`→`Double`(null 허용)로 바꾸고 `rateType`(fixed/variable)·`rateNote`(예: "기준금리+0.6%p")로 사유를 보존한다. 계약 DTO(`Product.rate`·`Funding.rate`)도 `Double`로 바꾸되, null은 **필드 누락**으로 직렬화되어(non_null 규약) 화면이 무이자로 오인하지 않는다 — 실검증 완료. 데모 픽스처는 두 상품 다 2.5 고정이라 현재 미발동, AI-06 실데이터 전환 시 대비 | 불변 원칙 §0-2-1(수치는 결정적 계산) · 계약 "값 없는 필드 생략" · expl §2-2 ③ 커버 불가 경계 제외 | `FundingProduct` · `FundingCheck.coverWithReason` · `CoverOutcome` · `FinanceDtos.Product` · `ExploreDtos.Funding` |
| 46 | 2026-07-25 | BE | **`scenarios_explored` 정의 = 평가한 경계 개수의 합.** done 이벤트의 이 값은 **보고된 인사이트 수가 아니라 검토한 경계 수**다: A1(예산) 상향 경계 + A4(무권리) 경계를 합하며, **커버 실패로 제외된 경계도 "평가함"에 포함**한다(검토했으나 조달 수단이 없어 뺀 것과, 검토조차 안 한 것은 다른 사실이기 때문). 계약 예시값 6은 예시일 뿐 고정값이 아니므로 실계산값으로 교체한다(db 경로 8 = A1 7 + A4 1) — `API_CONTRACT.md`는 수정하지 않는다 | expl §4 done 스키마 · 계약 예시값은 비고정 | `InsightScore.evaluatedCount` · `InsightBuilder` · `ExploreService` done |
| 47 | 2026-07-25 | BE | **탐색 축 실행 범위 — A1·A4만.** 데모 데이터가 **단일 업종(cafe)·단일 생활권**이라 A2(지역 확장)·A3(업종 스왑)은 재계산 대상이 없어 **미실행**한다(expl §10 "D8 초과 시 A2·A3 스킵" 폴백과 동일 결과). 실행 축은 **A1(항상)·A4(무권리 경계가 있을 때만)** 이며 보고 타입은 **T1·T2·T5** 3종이다. LLM 탐색 계획이 A2·A3·A4를 요청해도 서버가 **계산 가능한 축만** 남긴다(A4는 무권리 경계 존재 시). 0건 보고 시 사유 문장은 계약에 별도 필드가 없어 `plan.rationale`에 싣는다 | expl §1 축 4종 · §10 폴백 · 데모 스코프(서울·카페) · 계약 `plan.rationale` 자유 문자열 | `ExploreService.plan` · `PlanPrompt` · `InsightBuilder` |
| 48 | 2026-07-25 | BE | **B₀ 구성 → 상품 사용액 역매핑 (잔여 한도 원칙의 잃어버린 고리).** 세션 `composition[].type`(equity/guarantee/policy_loan)에는 **상품명이 없어**, expl §2-2 ①의 "이미 쓴 금액"을 특정 상품에 연결할 수 없다. 그래서 등재 #25의 `기관명→type` 규칙을 **역으로** 써서, composition의 각 type 금액을 그 type에 속한 상품에 귀속시킨다. **동일 type 상품이 복수면 한도 큰 순으로 소진**한다 — 시나리오 카드가 그 type에서 "한도 최대 상품"을 골랐으므로(#25) 같은 순서를 따라야 B₀ 구성과 조달 검증이 어긋나지 않는다. `equity`처럼 대응 상품이 없는 type과 미지의 type은 **어떤 상품에도 귀속시키지 않는다**(근거 없는 잔여 한도 삭감 방지). AI-06에서 상품 종류 컬럼이 확정되면 제거 | expl §2-2 ① 잔여 한도 원칙 + composition에 상품명 부재(계약 §2·§3) + 등재 #25 매핑 재사용 | `UsedLimits.byProduct` · `InsightBuilder`(T1 조달 상품 지정) · `FundingCheck.cover` |
| 49 | 2026-07-25 | BE | **계약 D8 금리 표기 표면 대응 + `marginal_payment_note` 이월.** ratify된 계약 D8이 `rate_type`(항상 존재, FE 분기 키)·`rate_note`(변동 시 서버 채움)·상품 고정 정렬(`amount_max` desc)을 요구한다. **fixed 상품에서도 `rate_type`이 필요**해 지금 발동하므로 DTO(`Product`·`Funding`)에 노출한다 — `rate_type`은 non_null 직렬화에서도 **항상 실리고**(값 "fixed"), `rate_note`는 null이면 생략(fixed 상품). 값은 engine `FundingProduct`에 이미 있어 **노출만**(engine 순수성 유지). ⚠️ **`marginal_payment_note`는 이월**한다: 변동금리 상품은 `FundingCheck`가 커버에서 제외(RATE_UNKNOWN, #45)하므로 **변동-backed T1이 구조적으로 생성될 수 없어** note 트리거가 부재하다 — 지금 넣으면 테스트로 검증 못 하는 죽은 코드다. DB 상품 소스(변동 상품 서빙 경로)가 생기는 BE-06/07에서 결선한다 | 계약 D8(2026-07-25 ratify) · `FundingCheck.coverWithReason` 변동 제외 로직 · 데모 상품 전부 fixed | `FinanceDtos.Product` · `ExploreDtos.Funding` · `FundingProduct.toProduct` · `ScenarioBuilder` · `LocationService.checkArea` · `InsightBuilder` |
| 50 | 2026-07-25 | AI | **원문 청크에서 PDF 인쇄 머리말·꼬리말 줄을 제거한다 (`finance_doc_chunk.text`).** 대상은 브라우저 인쇄가 주입한 페이지 장식 2종뿐 — `^\d{4}\. \d+\. \d+\.`(인쇄 날짜·시각)과 `^N…/M…https?://`(N/M 페이지 + 원본 URL). 한글 폰트 CID가 깨져 모지바케로 남는 줄이라 인용문에 섞이면 화면에 깨진 글자가 노출된다. **본문 문장은 한 글자도 고치지 않는다** — 장식 줄만 버리므로 남는 문장은 원문 그대로이고 「인용은 검색이지 생성이 아니다」(§5-4)에 저촉되지 않는다. 적용 결과: 전체 1,336줄 중 **50줄 제거**, 14개 청크의 모지바케 **0.88~2.03% → 0%**, `finance_product` 26행 **전 필드 diff 0**, `make eval-grounding` 지표 불변(verbatim 1.000 · coverage 13/29) — 골드 인용 13건 중 제거 대상 줄을 포함한 인용은 0건이었다 | `strip_print_artifacts()` 적용 전후 실측 + `git diff db/init/20_finance.sql` (5+/35−, 상품 행 무변동) | `batch/load/finance.py`, `db/init/20_finance.sql` 청크 텍스트, BE-06 원문 인용 구현 시 화면 인용문 품질 |
| 51 | 2026-07-26 | AI | **권리금 비례계수를 단가 비로 교체 (코드리뷰 #1).** 구 산식 `ratio = 환산임대료 ÷ 서울 중위 환산임대료` 는 분자에 대표면적이 들어가고 분모는 음식점 55.2㎡ 기준 단일값이라, 카페가 구조적으로 0.53배 작아져 하한 클립(0.5)에 몰렸다 — 실측 **카페 419/1,650행(25.4%)의 권리금이 649만원 상수로 붕괴**. `ratio = 단가 ÷ 서울 중위 단가`(둘 다 천원/㎡)로 바꿔 대표면적 이중 반영을 제거했다. 수정 후 **하한 클립 0건**, 업종 간 비례계수 동일 | `ai/batch/preprocess/cost.py:premium_interval`·`build_initial_cost`, `serving._derive` | `initial_cost.premium_*`·`cost_incl_premium_*` 전건, 프론티어 경계 |
| 52 | 2026-07-26 | AI | **업종별 환산임대료를 `initial_cost.monthly_rent` 로 노출 (코드리뷰 #2).** `rent` 는 `area_code` 단일 PK라 업종 축이 없어, 카페가 음식점 55.2㎡ 기준 임대료를 29.2㎡ 점포당 매출로 나눴다 — 부담률 중앙값 **카페 0.472 vs 음식점 0.209**, θ=0.15 통과 카페 176/1,060. `initial_cost` 에 `monthly_rent` 를 되살리고 `v_candidate_area` 가 그것을 노출한다(뷰 컬럼 이름·타입 불변 → BE 코드 무변경, **값만** 바뀐다). 수정 후 카페 중앙값 **0.250**, θ 통과 **363/1,060**. ⚠️ **이 균형은 과소 대표면적이 만든 것이었다** — 대표면적을 `category` 기준으로 교정한 뒤(등재 #84) 카페 중앙값 **0.380**·θ 통과 **238/1,059**, 음식점 **0.198**·**519/1,438** 로 재측정됐다. 데모 픽스처(`02_mock_data.sql`)는 `rent.monthly_rent` 와 같은 값을 업종 공통으로 넣어 expl §8 데모 판정을 보존한다(#7·#9) | `db/init/01_schema.sql`, `ai/batch/load/serving.py`, `ai/eval/common.py` | 부담률·θ 필터·역방향 판정. BE 공지 `docs/HANDOFF_BACKEND.md` |
| 53 | 2026-07-26 | AI | **근거 인용의 '문서 첫 문단' 폴백 제거 (코드리뷰 #3).** 구 구현은 상품명이 청크에 없으면 `chunks[0]` 을 지목했고 실측 **26건 중 24건이 그 경로**였다(F-000 인용문 = 브로슈어 머리말). 상품명 토큰 **과반**이 등장하는 청크만 고르고 동점이면 **더 짧은** 청크를 쓰며, 미달이면 `doc_chunk_ref=NULL`. 날조는 아니지만 **귀속 오류**이고, RAG 대신 id 직접 조회를 택한 논거(심사_QA 20)와 정면으로 어긋났다. 수정 후 26/26 매칭·청크 14→18개, 소진공 자금들이 통짜 첫 문단 대신 자기 자금 문단(#5·#6·#7·#9·#11·#14·#15)을 가리킨다 | `ai/batch/load/finance.py:select_chunk` | `finance_doc_chunk` 구성, 근거 충실도 지표 |
| 54 | 2026-07-26 | AI | **원문 청크에서 NUL 바이트를 제거한다 (코드리뷰 #4).** `소진공_소상공인정책자금_지원사업안내.txt` 에 pypdf 가 남긴 NUL 59개가 덤프에 실렸고, psql 이 NUL 주변을 삼켜 **DB 저장본이 파이썬 청크보다 411자 짧았다**(7,688 → 7,277). 삭제 구간에 「지원요건(2026년정책자금)세부지원요건…」 같은 실제 문장이 포함돼 「인용은 검색이지 생성이 아니다」가 깨졌다. NUL 은 공고문의 글자가 아니라 추출 산출물의 제어문자이므로 지우면 오히려 verbatim 이 복원된다. `emit._lit` 에 경계 방어(NUL 발견 시 예외)도 뒀다. 검증: Postgres 적재 후 18개 청크 전건이 정제 원문의 부분문자열이고, 청크의 모든 줄이 원문에 그대로 존재(재작성 0) | `ai/batch/load/finance.py:strip_print_artifacts`, `ai/batch/load/emit.py:_lit` | `finance_doc_chunk.text`, 근거 충실도 |
| 55 | 2026-07-26 | AI | **근거 충실도 지표를 적재 산출물 기준으로 재정의 (코드리뷰 #5).** 구 지표는 `coverage = 골드 인용 13 ÷ 초안 추출 29` 로 분자·분모 단위가 달랐고, 스위트가 **DB 에 실린 청크를 한 번도 보지 않았다** — 부록의 "붙지 않은 건은 비운 것" 서술도 사실과 반대였다(적재 전건에 ref 가 있었다). 이제 `20_finance.sql` 의 청크 실물을 원문과 대조하고(`chunk_verbatim_rate`), coverage 는 **적재 상품 중 인용 보유 비율**이다. 대조 기준은 `strip_print_artifacts` 를 적용한 정제 원문이다(#50·#54 정제분 반영). null 버킷 모수도 '인용이 빈 적재 상품의 문서' 로 바꿨다 — 골드 수록 여부로 판정하던 옛 정의는 실제로는 인용이 붙은 문서까지 '예상 밖 null' 로 몰았다. 실측 coverage **26/26** · 청크 verbatim **1.000(18개)** | `ai/eval/suites/grounding.py`, `ai/eval/common.py` | 부록 1 성적표 2행, 심사_QA 수치 근거 |
| 56 | 2026-07-26 | AI | **`gu_avg` 등급을 실제 자치구 평균으로 구현 (코드리뷰 #8).** `rent_join` 은 5등급을 문서화하며 `gu_avg` 를 「자치구 내 구획들의 평균」으로 정의했지만, 수치를 붙이는 `serving._build_rent` 에는 **권역 평균 경로밖에 없어** 자치구 평균이 어디서도 계산되지 않았다 — 실측 **gu_avg 524개 상권·23개 자치구가 단가 4종**(= R-ONE 권역 수)으로 붕괴. 자치구별 **구획 단위**(중복 구획 제거) 평균을 계산해 폴백에 우선 적용하고, 구획이 하나도 없는 자치구(성북·양천)만 권역 평균으로 내린다. 수정 후 gu_avg 단가 **23종**(자치구 수와 일치) | `ai/batch/load/serving.py:_build_rent` | `rent.unit_price`·`monthly_rent` 폴백 602행, 초기비용·부담률 파생 |
| 57 | 2026-07-26 | BE | **자격 매칭 P/R 골드 = 실적재 덤프 직접 대조 (#77).** 부록 1 성적표의 「자격 매칭 P/R」 행은 AI 하네스가 아니라 `EligibilityMatchingGoldTest` 가 산출한다(assumptions #29 의 위임 이행). 대상은 픽스처가 아니라 **배포되는 `db/init/20_finance.sql`** 을 직접 파싱한 26건이며, 기대 라벨은 필터 출력이 아니라 사람이 자격 축을 읽고 손으로 적은 것이다(`backend/src/test/resources/matching_gold.json`) — 필터로 골드를 만들면 순환 논증이라 아무것도 증명하지 못한다. 프로필 14종은 연령(38/39/40/65)·지역(서울/서울특별시/경기/경기도/미기재)·업종(카페/음식점/제조업)·예비창업 경계를 직접 때린다. 실측 **364쌍 · TP 277 · FP 0 · FN 0 · TN 87 → P/R/F1 = 1.000**. 결정적 순수 함수라 임계치 없이 완전 일치를 요구한다 | 이슈 #77 · 스펙 §12-1 | 부록 1 성적표 5행 |
| 58 | 2026-07-26 | AI | **적재본의 자격 축이 지역에 쏠려 있다 — 매칭 지표의 한계 (#77).** 검수본 26건의 자격 축 분포는 **지역 25건(`['서울']`) · 연령 1건(F-021 `max_age=39`) · 업종 1건(F-025 `['제조업']`) · `pre_startup_only` 0건 · 무제약 1건(F-001)** 이다. 이슈 #77 이 전제한 「예비창업 축 = F-008」은 전건 검수 과정에서 해당 축이 확인되지 않아 **적재본에 존재하지 않는다**. 따라서 실적재 교차만으로는 `pre_startup_only` 와 업종 경계(카페/음식점)가 한 번도 실행되지 않으므로 **합성 상품 회귀 테스트로 별도 증명**한다. 부록 1 은 이 한계를 명시한다 — 1.000 이라는 수치가 "모든 축을 실측했다"는 뜻이 아니기 때문이다 | `db/init/20_finance.sql` 실측 | 부록 1 5절 한계 문단 · #77 골드 설계 |
| 59 | 2026-07-26 | BE | **원문 인용은 상품 조회와 같은 쿼리에서 LEFT JOIN 으로 함께 읽는다 (BE-06 ①, #18).** `finance_product.doc_chunk_ref` → `finance_doc_chunk.chunk_id` **id 직접 조회**이며 유사도 검색·벡터DB 없음(DECISIONS §7). 상품당 청크가 1건이라 조인으로 행이 늘지 않고 「탐색당 쿼리 1회」(expl §5)가 유지된다. 출처 표기는 `doc_meta ->> 'org'|'doc'|'date'` 로 **Postgres 가 추출**해 자바 쪽 JSON 파싱을 두지 않았다. **LEFT** 인 이유는 청크 없는 상품이 목록에서 사라지면 안 되기 때문이며, 그 경우 `source_quote` 객체 자체를 만들지 않는다(빈 문자열·상품 설명으로 대체하면 그 순간 근거를 지어낸 것이 된다). **서버는 인용문을 자르지 않는다** — 요약·재작성 금지(§5-4)에 더해 길이 자르기도 하지 않으며, 바이트 동일성이 「인용은 검색이지 생성이 아니다」의 유일한 증명 수단이다. 대신 길이 편차(19~7,688자, 중앙 583)를 화면이 클램프로 떠안는다 | compose 실기동 `/api/check-area` 응답에서 25/25 인용 수신 + DB 조인 26/26·`doc_meta` 누락 0 확인 (2026-07-26) | `FinanceRepository`·`FundingProductRowMapper`, 계약 `source_quote`, FE 인용 렌더링 |
| 60 | 2026-07-26 | AI | **(해소 → #61)** **웹 수집본 6종은 문단 경계가 없어 통짜 청크가 된다 — 인용문 절반이 내비게이션으로 시작 (#18 결선 중 발견).** `chunk_document` 의 분할자 `_SPLIT` 은 **빈 줄** 또는 **`- N -` 페이지 표식**만 문단 경계로 본다. 그런데 `funding_web.to_text()` 가 빈 줄을 전부 버리고 `"\n".join(lines)` 로 합치는 탓에 서울신보 6종의 빈 줄이 **0개**이고, 소진공 지원사업안내도 페이지 표식이 없어 마찬가지다 → 각각 청크 `#0` 하나(1,519~7,688자)로 뭉친다. `select_chunk`(#53)는 후보가 하나뿐이면 그것을 고를 수밖에 없으므로, **상품 26건 중 13건의 인용문이 사이트 좌측 메뉴("보증상품/글자크기/글자 확대/프린트/…")나 PDF 인쇄 잔재(`(/web/main/index.kmdc)…`, `소상공인정책자금소상공인정책자금…`)로 시작**한다. 날조는 아니지만 #53 과 같은 **귀속 오류**다. 청크 18개 중 문단 단위로 제대로 쪼개진 10개(≤583자)는 정상이고, 통짜 8개(1.5~7.7KB)가 전부 이 경우다 | Postgres 실적재 대조: `text ~ '^(보증상품|\(/web|소상공인정책자금소상공인정책자금)'` 인 상품 **13/26**, 문서별 빈 줄 수 실측(서울신보 6종·소진공 2종 = 0) (2026-07-26) | `batch/collect/funding_web.py:to_text`·`batch/load/finance.py:_SPLIT`, `finance_doc_chunk` 구성, 근거 충실도 지표, CP4 판정 ② |
| 61 | 2026-07-26 | AI | **가정 #60 해소 — 인용문이 내비게이션으로 시작하던 문제를 청킹 단계에서 잡는다. 13/26 → 0/26.** 원인이 셋이라 셋 다 고쳤다. ① **웹 수집(`funding_web.to_text`)**: 이 사이트의 본문은 전부 `<table>` 안에 있고 **표 하나가 상품 하나**다(첫 셀이 "○○ 목록"). 앵커 문자열(`rfind("보증상품")`) 대신 **표만 추출**하고 표 사이를 **빈 줄**로 띄운다 → 좌측 메뉴·글자크기·프린트가 애초에 들어오지 않고, 2상품 페이지 2건이 상품별 청크로 갈린다. 표가 없으면 기존 앵커 경로로 폴백(사이트 개편 시 인용을 통째로 잃지 않도록). ② **인쇄 장식 줄을 지우지 말고 빈 줄로 치환**(`strip_print_artifacts`): 그 줄은 실제 페이지가 바뀐 지점이라 버릴 정보가 아니라 **문단 경계**다. 그냥 지우면 앞뒤 페이지가 한 문단으로 붙어, `- N -` 표식이 없는 인쇄본이 통짜 청크가 된다. ③ **인쇄본에 딸려온 사이트 링크 줄 제거**(`_PRINT_NAV = /\(\/web\//`): 836줄 중 **23줄**, 전부 소진공 지원사업안내 1문서의 링크 목록·빵부스러기다(산문에는 이 형태가 없다). 결과: 청크 **18 → 22개**, 최장 인용 **7,688 → 1,863자**(중앙 583 → 463), **크롬으로 시작하는 인용 13 → 0건**, 인용 미보유 상품 0건 유지. 남은 것은 PDF 볼드가 글자를 4중 인쇄한 흔적("융자대상융자대상융자대상융자대상")인데 **추출본의 사실 그대로라 손대지 않는다** — 지우려면 본문 글자를 고쳐야 하고 그건 §5-4 위반이다 | `make eval-grounding` 재실행: coverage **26/26 = 1.0** · chunk_verbatim **1.0**(22/22) · gold_verbatim **1.0**(13/13) · mismatch 0. compose 실기동 `/api/check-area` 25/25 인용이 공고 본문으로 시작 (2026-07-26) | `batch/collect/funding_web.py`·`batch/load/finance.py`, `db/init/20_finance.sql`, 화면 인용문 품질, CP4 판정 ② |
| 62 | 2026-07-26 | BE | **`/api/recommend` 응답에 gzip 압축을 켠다 (BE-07).** 실데이터 전환으로 후보가 목 4곳 → **1,061곳**이 되며 응답이 **769 KB** 가 됐다. 슬라이더를 움직일 때마다 재호출되는 경로라 전송량이 곧 체감 반응성이다(스펙 §7 `<100ms`). 압축만으로 **769 → 125 KB (6.1배)** 라 계약·프론트를 건드리지 않고 해소된다. 페이징은 계약 변경이라 D10 동결 전에는 하지 않는다(계약 §4 주석의 재검토 조건). **`text/event-stream` 은 압축 대상에서 제외**한다 — SSE 는 「템플릿 즉시 송출 → LLM refine 이 선택적 교체」가 설계의 핵심인데(expl §2-5) 압축이 붙으면 이벤트가 버퍼에 모였다 한꺼번에 나갈 수 있고, 이벤트 하나가 수 KB 라 얻을 것도 없다 | `curl -H 'Accept-Encoding: gzip'` 실측 769,392 → 125,511 bytes · QA 10종 재실행 전건 통과 (2026-07-26) | `backend/src/main/resources/application.yml`, 화면 3 슬라이더 반응성 |
| 63 | 2026-07-26 | BE | **`parse_source` 는 항상 `form_only` 다 — LLM 실파싱이 없다 (BE-07).** 구 구현은 `free_text` 가 비어 있지 않기만 하면 `"llm"` 을 실었는데, 실제 처리는 `freeText.contains("권리금")` 키워드 매칭이고 **API 키를 완전히 지운 스택에서도 `"llm"` 이 나갔다**. 계약이 `form_only` 를 「LLM 장애 폴백」으로 정의했으므로, LLM 경로가 아예 없는 현 상태의 정직한 값은 `form_only` 다. LLM 실파싱을 붙이는 시점에 그 호출의 성공 여부로 값을 정한다. 같은 성격의 미해결 건(리스크 검증 `applied=true`)은 이슈 #96 으로 분리했다 | 키 제거 스택 `/api/diagnose` 실호출 (2026-07-26) | `DiagnoseController`, 계약 §1 `parse_source`, FE `ParsedResult` 배지 |
| 64 | 2026-07-26 | BE | **리스크 검증 반박문의 캐시 키에서 확정 예산·후보 총수를 뺀다 (#96).** 스펙 §5-3 검증 에이전트를 결선하니 LLM 1왕복이 실측 **약 1.8초**인데, `/api/recommend` 는 슬라이더가 움직일 때마다 재호출되는 경로다(§7 「<100ms 재계산 체감」). 입력 사실에 예산·후보 총수를 넣으면 **드래그 한 칸마다 캐시가 빗나가** 매번 왕복을 탄다. 대신 **상위 3곳의 판정**을 넣는다 — 종합점수 정렬은 예산과 무관해 상위 3곳의 이름은 고정이고 예산이 움직이면 그 3곳의 판정만 바뀌므로, **사실이 달라지는 시점 = 추천이 실질적으로 달라지는 시점**이 된다. 대가로 반박문이 예산 액수를 인용할 수 없다(반박 대상이 예산이 아니라 그 예산이 만든 후보 구성이라 수용). 예산 인용이 필요하면 `risk_review` 를 별도 지연 조회로 빼야 하고 그건 계약 변경이라 동결 후 과제다. **캐시 빈을 분리**한 것도 실측 결과다 — 한 클래스에서 `@Cacheable` 메서드를 부르면 스프링 프록시를 우회해 캐시가 조용히 무력화되며, 2회차가 1.3초 걸리는 것으로 드러났다 | 슬라이더 시뮬레이션 실측: 9,000 **1.80s**(첫 호출) → 9,100 **0.010s** → 9,200 **0.008s** → 9,000 **0.007s** → 12,000 **1.38s**(판정 뒤집힘) (2026-07-26) | `RiskReviewAgent`·`ReviewGenerator`·`CacheConfig`, 화면 3 슬라이더 반응성 |
| 65 | 2026-07-26 | BE | **검증 반박문은 「입력 사실 밖 수치」가 하나라도 있으면 응답 전체를 버린다 (#96, 스펙 §5-3).** 프롬프트로 부탁하는 것은 제약이 아니므로 `ReviewPrompt.sanitize` 가 기계적으로 검증한다: ① 출력의 숫자 토큰이 전부 입력 사실 문자열에 등장 ② 용어 컴플라이언스(승인·권장·보장 등) ③ 한 문단·20~400자. 거부는 전부 템플릿 폴백 + `skipped=true` 로 수렴하며, **LLM 이 살아 있어도** 지어낸 수치가 있으면 폴백이다(가용성과 신뢰성은 다른 문제). **한계**: 수치 대조는 부분문자열 검사라 한 자리 숫자는 사실 문자열 어딘가와 우연히 일치할 수 있다. 반박문 한 문장이 한 자리 숫자만으로 허위 주장을 구성하기 어렵다고 보고 수용했으며, 필요하면 단위까지 묶어 대조하는 것이 다음 단계다 | 단위 테스트 7건(정상 인용·지어낸 수치·금지 표현·길이 경계·빈 응답·장식 제거) + 실키 실호출에서 반박문이 사실의 수치만 인용함을 확인 (2026-07-26) | `ReviewPrompt`, `risk_review.objection_text` |
| 66 | 2026-07-26 | AI·BE | **자격 축 `existing_business_only` 추가 — 예비창업 프로필의 대환대출 편성 해소 (#90 문제 2).** `pre_startup_only` 의 반대 축이며, 공고문이 **기존 사업/기존 대출 보유를 명시한 4건**에만 `true` 를 준다: F-016 대환대출("중‧저신용 소상공인이 **보유한** 은행권·비은행권의 7% 이상 고금리 **대출**"), F-017~019 재도전특별자금("**재창업** 준비단계"·"**재기사업화** 선정기업"·"성실상환 **재창업** 소상공인"). **보수적으로 4건에만 붙였다** — 긴급경영안정자금(매출감소 15% 확인)·신용취약소상공인자금도 사실상 영업 중인 사업자를 전제하나 공고문이 자격 요건으로 명시하지는 않으며, 넓게 붙이면 소진공 자금 대부분이 예비창업자에게서 사라져 **실제 제도보다 좁게** 답하게 된다(확인하지 못한 제약을 부여하지 않는다). 상품명 블랙리스트를 쓰지 않은 이유는 자격 판정이 코드에 숨으면 골드 대조·재검수·근거 제시가 전부 무너지기 때문이다(DECISIONS §15) | compose 실기동: 예비창업 자격 25→**21건**·적극 카드 대환대출→긴급경영안정자금, 기존 사업자 25건·대환대출 유지. 매칭 골드 **P/R/F1 1.000 유지**(TN 87→127) · QA 10종 통과 · `eval-grounding` coverage 26/26·verbatim 1.0 불변 (2026-07-26) | `db/init/01_schema.sql`·`ai/data/finance/reviewed.json`·`batch/load/finance.py`·`Eligibility`·`EligibilityFilter`·`matching_gold.json` |
| 67 | 2026-07-26 | AI | **문서별 수집일을 매니페스트로 보존한다 — `source_collected` 가 덤프 재생성일로 갱신되던 결함 해소 (#93).** 구 구현은 `finance.py` 가 `today` 를 찍어, **배치를 다시 돌리기만 해도 26건 전건의 수집일이 오늘로 바뀌었다**(같은 원문인데 데이터가 새것처럼 보인다). 수집일은 수집 시점에만 알 수 있으므로 **수집기가 기록하고 저장소에 커밋**한다(`ai/data/finance/collected.json`) — 적재 배치는 읽기만 하고 날짜를 만들지 않는다. 근거: PDF 12건은 **원본 파일 mtime**(내려받은 시각) = 2026-07-21, 서울신보 6건은 웹 재수집본이 정본이므로(가정 #32) 그 실행일 2026-07-26. 미기록 문서는 오늘로 폴백하되 **경고를 남긴다** — 조용히 오늘로 찍으면 같은 결함이 다시 숨는다. 결과 분포: **2026-07-21 18건 · 2026-07-26 8건** | 적재 배치 **2회 연속 실행 후 `20_finance.sql` 바이트 동일** 확인(재실행이 날짜를 바꾸지 않는다) + 단위 테스트 7건(갱신 범위·폴백 경고·mtime·배포 매니페스트가 적재 문서를 전부 덮는지) (2026-07-26) | `batch/collect/collected.py`(신설)·`funding_docs`·`funding_web`·`load/finance.py`, `finance_product.source_collected`, 화면 출처 표기 |
| 68 | 2026-07-27 | AI | **w3(경쟁여유) 입력을 면적 정규화 밀도로 확정 — 리뷰 #1 안 A, 3인 합의(2026-07-27).** 코드가 원시 업소 수(`permit_store_cnt`)를 쓰고 있었으나 스펙 §4-3·`competition.py` 독스트링·가정 #41 ⑥·§12 검증 피처가 모두 **면적 정규화 밀도(`store_per_10k_m2`)** 를 가리켰다. 원시 개수를 쓰면 넓은 상권이 구조적으로 불리해져 축이 면적 대리변수로 오염된다. **코드를 문서에 맞춘다**(안 B = 문서를 코드에 맞추기는 「면적이 넓어서 경쟁여유가 낮다」를 심사에서 방어해야 해 채택하지 않음). 부수 효과로 §12 검증의 w3 피처가 서빙 축과 **같은 변수**가 됐다 | 적재본 재계산 ρ(w3, −store_per_10k_m2) = 1.0000 (cafe n=1,018 · food n=1,421). 변경 전 ρ(w3, −permit_store_cnt) = 1.0000 | `ai/batch/load/serving.py` `_derive`, `10_data_core.sql` 재생성, 부록 1 민감도·부록 2 전 수치 |
| 69 | 2026-07-27 | AI | **인허가 미조인 (상권,업종)의 경쟁밀도는 0으로 두고 건수를 로그로 남긴다.** `or 0` 은 키 부재(None)만 처리하고 **NaN 은 통과**시켜(`bool(nan) is True`) 백분위 비교가 전부 False → w3 = 0.0(경쟁여유 **최하위**)으로 반전됐다. 경쟁 업소가 확인되지 않은 = 경쟁이 가장 적은 상권이므로 방향이 정반대였다. `.dropna()` + `.get(key, 0.0)` 로 키 부재·NaN 을 같은 의미(관측 업소 0건)로 통일한다. 미조인 건을 점수 대상에서 **제외하지는 않는다** — 인허가 포인트의 81.2%가 폴리곤 내부(가정 #19)라 미조인은 「실제로 업소가 없다」에 가깝고, 제외하면 후보가 근거 없이 줄어든다 | 변경 전 실측: `permit_store_cnt` NULL 58건 = w3 == 0.0 58건 = 전체 w3 == 0.0 58건 (정확히 일치). 변경 후 w3 == 0.0 **0건** | `serving.py` `_derive`, w3 절대값(순위는 보존) |
| 70 | 2026-07-27 | AI | **결측을 0으로 강등해 적재하지 않는다 — 추정매출·유동인구 결측 상권은 `location_score` 에서 제외한다.** ① 점포는 있는데 매출이 0으로 반올림되는 (상권,업종)은 「매출 0」이 아니라 **원천 미집계**다. 그대로 적재하면 BE 부담률이 `rent / 0` = `Infinity` 가 되어 계약(number)을 깨고 반박문에도 노출됐다(BE 리뷰 D-04, 이슈 #104 ⑤). ② 원천 유동인구에 없는 상권을 0으로 채우면 w1 최하위 + 화면 범위가 `0 ~` 로 시작한다(CM F-4). **NULL 적재는 선택지가 아니다** — `01_schema.sql` 이 두 컬럼을 `INTEGER NOT NULL` 로 잡아 스키마 변경 + BE RowMapper 수정이 따라온다. 행을 빼면 `v_candidate_area` 가 JOIN 이라 후보 풀에서 자연히 빠지고 BE·FE 코드 변경이 0이다 | 실측 제외: 추정매출 0 → (3110029 동대문역 1번, cafe) 1건 · 유동인구 결측 → 3110946 청계산원터골 2건(업종별). `location_score` 2,500 → **2,497행** (cafe 1,061 → 1,059 · food 1,439 → 1,438) | `serving.py` `_sales_and_stores`·`_derive`, 랜딩 지표 `total_count`, 부록 1 민감도 모수 |
| 71 | 2026-07-27 | AI | **`daily_floating` 은 분기 총계를 분기 실일수로 나눈 일평균이다 (CM F-2, 이슈 #106).** 원천 `TOT_FLPOP_CO` 는 분기 총계인데 나눗셈 없이 적재해 약 90배 과대였다. 계약과 FE 라벨이 이미 「일평균」이므로 **계약을 바꾸지 않고 데이터를 맞춘다**. 상주·직장 인구는 시점 재고량이라 환산 대상이 아니다. **전 행에 같은 상수를 나누므로 백분위(w1)는 불변**이며 표시값만 바뀐다 — 점수·판정·추천 순위에 영향이 없다 | 변경 전 최대 8,060,412 · 평균 950,946 → 변경 후 최대 **89,560** · 평균 **10,568**. 분기 실일수 20261 = 90일 | `serving.py` `assemble`, `/recommend`·`/budget` 표시값, FE 데모 이미지 재촬영(이슈 #106) |
| 72 | 2026-07-27 | AI | **인허가 미등재 업태는 `food` 로 남기되 건수·명칭을 로그로 드러낸다 (리뷰 #8).** 기본값이 `food` 라 신규 업태·표기 변형이 **조용히** 음식점 경쟁밀도로 흡수됐다(가정 #20 의 '까페' 1,230건이 그 선례). 다만 `other` 로 보내 모집단에서 빼면 **경쟁을 과소평가**하게 되어 하향 안전 마진 원칙(§0-1)과 반대 방향이다 — 문제는 흡수 자체가 아니라 **조용한** 흡수였다. `FOOD_TYPES` 실측 목록을 등재하고, 목록 밖 업태는 `food` 로 계상하되 `run()` 이 경고 로그를 남긴다 | 영업/정상 132,876건 전수 집계 = 29종. 그중 「기타」 21,103 + 「기타 휴게음식점」 7,471 + 공백 4 = **28,578건(21.5%)** 이 포괄어라 화이트리스트에서 빼면 food 모집단이 21.5% 줄어든다 | `ai/batch/collect/permits.py`, 경쟁밀도 모집단 |
| 73 | 2026-07-27 | AI | **인용 불가 문서 4종 지정 + 자격 표제어 우선 선택 (리뷰 #4).** KB 4문서는 브라우저로 인쇄된 웹 페이지라 **자격 요건 문단이 원문에 존재하지 않는다** — 어떤 청크를 골라도 내비게이션·FAQ·표지 배너가 되므로 인용 자체를 비운다(스펙 §5-4 「연결된 청크가 없는 상품은 인용 필드를 생략」). 또한 `select_chunk` 정렬 키에 **자격 표제어 보유**(「대상기업」·「신청대상」·「융자대상」·「지원자격」·「공통지원자격」·「지원대상」)를 길이보다 **먼저** 둔다 — 같은 상품명이 여러 문단에 나올 때 가장 짧은 문단이 이겨 금리표·융자조건이 인용으로 뽑히던 문제. **부록 1 의 「청킹을 고쳐 13 → 0건 해소」 서술을 정정**한다: 청킹 개선으로 13 → 4건이었고, 나머지 4건이 이번 문서 단위 조치로 0건이 됐다 | 자격 표제어 없는 인용: 규칙 적용 전 22건 중 **7건**(F-016·017·018·019·022·024·025) → 적용 후 **0건**. coverage 26/26 → **22/26 (0.846)**, 청크 22 → 16개, 골드 스팟체크 13 → 10건(KB 3건 제거) | `ai/batch/load/finance.py`, `20_finance.sql`, `eval/suites/grounding.py`(`non_quotable_docs` 버킷 신설), 부록 1 §2 |
| 74 | 2026-07-27 | AI | **`product_id` 는 검수본에 명시한다 — 위치 기반 채번 금지 (리뷰 #5).** `pid = p.get("product_id") or f"F-{idx:03d}"` 는 배열 인덱스를 ID로 삼았고, `reviewed.json` 26건에 그 키가 **하나도 없었다**(실측 0/26). 상품을 끼워 넣거나 순서를 바꾸는 순간 이후 전 상품의 ID가 밀리고 **골드는 다른 상품에 붙은 채 테스트가 통과**한다. BE 의 `matching_products` 동점 정렬(계약 §6 `product_id` 오름차순)도 이 ID의 안정성을 전제로 한다. 키 부재는 폴백이 아니라 **에러**로 처리한다 | `reviewed.json` 26건에 F-000~F-025 명시. 값 변화 0 — 재생성 결과가 `generated` 헤더 외 바이트 동일 | `ai/data/finance/reviewed.json`·`finance.py`, `matching_gold.json`·`grounding_quotes.jsonl` 의 ID 안정성 |
| 75 | 2026-07-27 | AI·BE | **자격 축 값 교정 기준 = 원문에 명시된 업력·가동의 '하한' (BE 리뷰 D-06 ①) + `target_group` 축 신설(D-06 ②).** 가정 #66 은 4건에만 `existing_business_only` 를 붙였고, 그 결과 이슈 #104 ③ 이후 적극 카드에 **F-014 긴급경영안정자금(매출 15% 감소 = 매출 이력 전제)** 이 편성됐다 — 증상만 옮겨가고 원인은 남아 있었다. 판정 기준을 명문화한다: **업력·가동의 하한이 원문에 명시되면 TRUE**, **상한(업력 N년 이내/미만)은 근거가 아니다**. 표현 축이 아예 없던 대상 한정 요건(장애인기업·사회적경제기업·인증기업·고용우수기업)은 `finance_product.target_group TEXT[]` 로 신설하고, `regions` 와 동형의 **하향 안전** 규칙(프로필에 표시가 없으면 탈락)을 적용한다 | TRUE 추가 8건: F-004(업력 6개월 이상)·F-005(업력 1년 초과·가동 중)·F-006(업력 2년 초과)·F-007(업력 6개월 이상)·F-008(사업자등록 후 3개월)·F-010(사업자등록 후 영업 중)·F-013(재해 확인증)·F-014(매출 15% 감소) → 총 12건. 유지(FALSE): F-011(업력 1년 **이내**)·F-021(업력 3년 **미만**) = 상한. `target_group` 5건: 장애인기업 2 · 사회적경제기업 1 · 인증기업 1 · 고용우수기업 1 | `reviewed.json`·`01_schema.sql`·`finance.py`·BE `EligibilityFilter`, 부록 1 §5 단서 |
| 76 | 2026-07-27 | AI | **분기 표기는 원천 최신 분기에서 파생한다 — 하드코딩 금지 (리뷰 #11).** `serving.py` 가 `"20261"`·`"2026-Q1"` 을 5곳에 박아 두어, 다음 분기 재수집 시 **값만 새것이고 화면 기준일이 옛것으로 남는** 사고가 가능했다(스펙 §0-4 「데이터 기준일 상시 표기」). 서울 상권분석은 `STDR_YYQU_CD`, REB 임대료는 `WRTTIME_IDTFR_ID` 최신값을 각각 흘려보낸다. 아울러 `data_source_meta` 에 **`floating` 행을 신설**한다 — 이 지표만 기준일·출처 라벨 없이 나가고 있었다(BE 리뷰 D-22 잔여 몫) | 현 원천 최신 분기: 서울 상권분석 `20261` · REB `202601` → 표기 `2026-Q1` (하드코딩 값과 일치함을 확인) | `serving.py`(`_yyqq`·`_as_of`·`_build_rent`·`_store_density`·`_derive`·`_data_source_meta`), `data_source_meta` 7행 |
| 77 | 2026-07-27 | AI | **§12 검증 프로토콜의 w4 프록시 설계 부호는 오등재였다 — 정정 사실을 병기한다 (가정 #35·#36 보완, 리뷰 #3).** `DESIGN_SIGNS["oper_avg_months"] = +1` 로 등재했으나 **서울 상권변화지표의 정의상 운영영업개월은 성장 등급과 음의 관계**다(HH 정체 142.3개월 / LH 상권확장 104.0개월). 따라서 부록 2 가 「w4 방향 불일치」로 보고한 것은 **설계의 반증이 아니라 사전 등록물의 부호 오기**다. 사후 조정 금지(리스크 #19)에 따라 **판정(게이트 B·ρ 경로)과 수치는 그대로 두고**, 방향 일치 판정을 **w3 1축으로 축소해 읽는다**. 기존 #36 서술은 삭제하지 않는다 — 발견 시점을 그대로 남기는 것이 사전 등록의 취지다 | `spearman(GROWTH_RANK, oper_avg_months) = −0.733` (n=1,650, `change_index` 실측). 실측 raw ρ = −0.019 를 서빙 w4 기준으로 읽으면 부호는 오히려 약하게 일치 | `docs/부록1_2_평가성적표.md` 정정 고지, `ai/eval/common.py` 의 `DESIGN_SIGNS` 는 **변경하지 않는다**(사전 등록물 보존) |
| 78 | 2026-07-27 | AI | **검증 모델의 베이스라인은 `train 전체 평균` 이다 — 등재 문안(#35 ④)의 「자치구 평균」은 구현 불가능한 서술이었다 (리뷰 #6).** 자치구 블록 GroupKFold 에서는 test 자치구가 train 에 없으므로 「test 자치구 평균」이 정의되지 않는다. 구현·보고값은 처음부터 `statistics.fmean(train_y)` 였다. 수치(−0.003)는 정직하며 **라벨만** 사실에 맞춘다 | `ai/eval/suites/model.py` 의 베이스라인 계산 코드 대조 | 부록 2 결과 표 라벨 (수치 불변) |
| 79 | 2026-07-27 | AI | **업종 정의는 두 소스에서 이원화돼 있고 이를 고지한다 (리뷰 #7).** 경쟁밀도(w3)의 분자는 인허가 `업태구분명`(KSIC I56), 매출·점포수(w2·w5·est_sales)는 서울 상권분석 서비스업종 코드다. `cafe` 는 인허가에서 I5622 6종, 상권분석에서 `CS100010`(커피-음료) 단일 코드다. 아이스크림·전통찻집·떡카페는 커피-음료에 없고, 제과점은 인허가 `food` / 상권분석 `CS100005` → `food` 로 양쪽 일치한다. 두 코드 체계 간 공식 매핑표가 없어 **각 소스의 표준 분류를 그대로 쓰고 차이를 고지**한다 — 억지 매핑이 오히려 근거 없는 조정이 된다. 영향: w3 의 카페 모집단이 w2·w5 보다 넓다 = **경쟁 과대 = 하향 안전 마진**(§0-1 정합) | 가정 #20(KSIC I56 재분류 확정본) + `serving.py:21` `CAFE_CODE = CS100010` 대조 | 부록 1 「데이터 기준일」 절 병기, w3 해석 |
| 80 | 2026-07-27 | AI | **초기비용 구간은 블록 독립을 가정하지 않은 단순 합산 = 보수적 구간이다 (리뷰 #13).** `cost.py` 가 블록별 low·high 를 그대로 더하므로 구간 폭이 실제 분포보다 넓다. 코드는 바꾸지 않는다 — 상단 과대는 **하향 안전 마진** 방향이고 §4-1 경계 규칙(⚪ 조건부 적합)도 그 방향에서 보수적으로 작동한다. 문서에 성질만 명시한다 | `ai/batch/preprocess/cost.py` 의 low/high 합산 로직 | 문서 서술(부록 1·기술설명서), 계산 변경 없음 |
| 81 | 2026-07-27 | BE | **초기비용 합계는 두 경로에 존재하며, 배포 덤프에서 두 경로가 일치함을 확인했다 (이슈 A 해소).** `initial_cost.cost_ex/incl_premium_*` 은 AI 배치(`preprocess/cost.py`)가 구운 값이고, 서빙 숫자는 BE `CostCalculator` 가 원재료 4블록에서 다시 만든다. 두 경로를 남기는 이유는 역할이 다르기 때문이다 — 뷰 컬럼은 적재 검증·SQL 탐색용이고 화면에 나가는 값은 engine 산출물이다. 위험은 **둘이 조용히 갈리는 것**이라 배포 덤프 전건을 대조했다. `CandidateRepository` 는 합계 컬럼을 **조회하지 않으므로** 갈려도 화면에 새지는 않지만, 갈린 채로 두면 문서·SQL 탐색이 서빙과 다른 숫자를 말하게 된다. 산식 수준의 고정은 `ai/tests/test_cost.py::test_engine_cost_composition_matches`(RESERVE_MONTHS=6 포함)가 담당한다 | 재현: `docker compose exec -T db psql -U ventry -d ventry -c "select count(*), count(*) filter (where cost_ex_premium_low <> deposit_low + interior_low + monthly_fixed_cost*6), count(*) filter (where cost_incl_premium_high <> deposit_high + interior_high + monthly_fixed_cost*6 + premium_high) from initial_cost;"` → **3,300행 · 불일치 0 · 0** (2026-07-27) | `CandidateRowMapper`·`CandidateRepository` 주석, `preprocess/cost.py`, `CostCalculator` |
| 82 | 2026-07-27 | BE | **후보 조회 캐시 키는 업종 단독으로 완전하다 (이슈 D 해소).** BE-02 당시 「캐시 키에 자치구 추가」를 예약해 뒀으나, 이는 후보 조회가 지역으로 좁혀질 것을 전제한 메모였다. `region_hint` 의 사정거리는 **지역 한정 상품의 자격 판정 전용**으로 확정됐고(#136), `CandidateRepository` 의 조건은 `WHERE industry = ?` 뿐이다. 조회 축이 하나이므로 키를 늘리면 같은 결과를 자치구 수만큼 중복 적재할 뿐이다. 후보 풀을 지역으로 좁히지 않는 것 자체가 설계 판단이다 — 「내 한도로 어디까지 가능한가」가 질문이므로 예산으로 갈 수 있는 범위를 자치구로 미리 지우지 않는다 | `CandidateRepository.SELECT_BY_INDUSTRY` 실물 · `심사_QA.md` 「지역을 입력했는데 왜 다른 구가 추천되나」 | `CandidateRepository.findCandidates` 의 `@Cacheable`(2026-07-27 이후 위치), Caffeine `candidates` 캐시 |
| 83 | 2026-07-27 | BE | **반박문의 수치 대조는 문자열이 아니라 값으로 한다 (코드리뷰 후속).** `ReviewPrompt.sanitize` 는 LLM 출력의 숫자가 전부 입력 사실에 있어야 통과시키는데, 구현이 `facts.contains(토큰)` 부분 문자열 검사였다. 사실이 「추정매출 1800만원」이면 출력의 **180·80** 이, 「환산임대료 198만원」이면 **98** 이 통과했다 — 지어낸 수치를 막는 것이 이 가드의 유일한 존재 이유인데 그 자리에 구멍이 있었다. 반대로 모델이 「1,800만원」·「0.11」처럼 쓰면 값이 같은데도 응답 전체가 폐기돼 화면에 「검증 생략」이 떴다. **BigDecimal 값 집합 비교**로 바꿔 둘 다 해소한다(쉼표 제거, 끝자리 0 동일시, 날짜 같은 비수치 토큰은 거부). 가드가 엄격해지는 방향이므로 `applied` 비율이 낮아질 수 있다 | 구 구현 재현: 사실 `추정매출 2400만원` + 출력 `월 240만원` → 통과(지어낸 수) / 출력 `2,400만원` → 폐기(정상 응답). 회귀 테스트 `ReviewPromptTest` 3건 신설 | `ReviewPrompt.usesOnlyGivenNumbers`, `risk_review.skipped` 분포 |
| 84 | 2026-07-27 | AI | **업종 대표면적 상수가 인허가 「대장 구분」으로 산출돼 있었다 — 코드를 문서에 맞춘다 (이슈 #152).** `cost.REPRESENTATIVE_AREA_M2` 2건 모두 `classify_category()`(KSIC I56) 업종이 아니라 인허가 **대장 구분**(휴게음식점 29.3㎡ · 일반음식점 55.4㎡) 중앙값이었다. 두 축은 서로 가로지른다 — 휴게 대장에는 카페가 아닌 업태 13,624건(분식·패스트푸드 등, 중앙값 26.4㎡)이 섞이고, 일반 대장에 카페로 신고된 1,269건(중앙값 51.8㎡)은 빠진다. 그 결과 카페 대표면적 29.2㎡ 가 **cafe 면적 분포의 p28.5**(8.8평 = 테이크아웃 키오스크)에 앉았다. **`cafe` 29.2 → 44.0㎡ · `food` 55.2 → 51.7㎡** 로 교체한다 — 주석과 등재 #41 ① 이 이미 `category` 중앙값이라고 적고 있었으므로 **문서는 정의 그대로 두고 수치만 맞춘다**(안 B = 정의를 「대장 구분 중앙값」으로 개정하기는, 카페 8.8평에 분식·아이스크림이 섞인 것을 심사에서 설명할 방법이 없어 기각. 등재 #68 선례). 42.9㎡(커피숍 업태 단독)도 후보였으나 수치 효과가 44.0 과 사실상 같은데 정의 개정이 필요해 채택하지 않았다. **산출을 코드 경로로 만든다** — `collect.permits.representative_area_m2()` 가 원천에서 재도출하고 `make collect` 로그에 매번 찍히며, 회귀 테스트 `tests/test_permits_area.py` 가 상수·원천·대장 구분 세 값을 못 박는다. 파싱 함정 1건 동봉: `소재지면적` 에 천 단위 쉼표(`1,496.01`)가 283건 있고 이를 결측으로 흘리면 **전부 네 자리 이상 = 대형 점포**만 골라 떨어져 음식점 중앙값이 51.69 → 51.52 로 밀린다(채움률 99.74% → 99.56%). **θ 는 0.15 단일 유지** — 카페가 면적당 매출이 낮아 부담률이 높은 것은 사실이고 그것을 과소 상수로 가려온 것이 문제였다. 업종별 θ 는 스펙 §4-1 이 단일 상수를 규정하므로 스펙 변경 사안이다. **w1~w4 는 바이트 불변, w5 는 완전 불변이 아니다** — 대표면적은 전 상권 공통 배수라 백분위가 보존될 것 같지만 `converted_rent` 가 만원 단위로 **반올림**하므로 동률 묶음이 미세하게 재편된다(ρ=0.999997, 값이 바뀐 행 cafe 307·food 676). 종합점수 순위 이동은 최대 **5칸**, 상위 50 집합은 두 업종 모두 동일하므로 **추천 결과는 실질 불변**이다 | 인허가 영업중 157,279건 재집계(2026-07-27): cafe n=17,578 중앙값 44.000 · food n=132,530 중앙값 51.685 · 대장 휴게 29.28 / 일반 55.41. 적재본 재계산: 부담률 중위 카페 0.251→0.380 · 음식점 0.211→0.198, θ=0.15 통과 카페 360→238 · 음식점 474→519 | `ai/batch/preprocess/cost.py`·`collect/permits.py`·`load/serving.py`(`data_source_meta.rent.note` 를 상수에서 파생), `db/init/10_data_core.sql` 재생성(initial_cost 3,300행 · rent 1,650행), `db/init/01_schema.sql` 주석, 등재 #41 ①·#52, 기술설명서·부록 1·2 수치, FE 라벨 #151 |
| 85 | 2026-07-27 | AI | **인용 원문의 4중 반복은 표 병합 셀이 아니라 브라우저 인쇄의 「가짜 볼드」였다 — 좌표 근거로 사본만 접는다 (이슈 #150).** 소진공 지원사업안내 인용에 `융자대상융자대상융자대상융자대상` 처럼 표제어가 4번씩 박혀, FE 클램프 4줄 중 2줄이 반복 문자열이라 인용 블록이 내용을 전달하지 못했다. 원인은 병합 셀 복제가 아니다 — **PDF 가 같은 글자열을 0.2pt 씩 밀며 4번 그린다**(Chrome 인쇄의 합성 볼드). `융자대상` 은 x = 170.77 · 170.98 · 170.98 · 171.19 에 그려진 한 낱말이다. 이슈의 1안(추출기 설정으로 복제 차단)은 pypdf 에 해당 옵션이 없어 불가하고(`extraction_mode="layout"` 도 중복은 그대로 남기며 산문 배치가 무너진다), 2안(문자열 반복 축약)보다 강한 **좌표 기반 제거**를 택했다. `_overprint_runs()` 가 pypdf `visitor_text` 로 글자마다 절대 좌표를 받아 **같은 자리에 겹쳐 그려진 레코드 열**을 찾고, `_collapse_overprint()` 가 추출문에서 그 사본만 1회로 접는다. 근거가 본문 어휘가 아니라 글자 좌표이므로 **원문 변형이 아니라 렌더링 중복 제거**이며, 지우는 쪽이 지면에 인쇄된 문장을 복원한다 — NUL·인쇄 장식 줄과 같은 성격의 정제다(등재 #50·#60·#61, 스펙 §5-4). 덧그리기는 **낱말이 조각나면 조각 단위로 번갈아** 일어나므로(`불`·`만족`·`불`·`만족`…) 인접 동일 레코드가 아니라 **레코드 열의 주기**를 본다(최대 24조각 — `자세한 사항은 여기로 문의하세요` 가 9조각). 반복 횟수는 전 문서 전 구간 예외 없이 **정확히 4회**였다 | 좌표 실측(2026-07-27): 덧그리기 발생 5문서 · 접은 자리 268군데(지원사업안내 53 · KB 4종 215), 서울신보 6종·소진공 융자공고는 0군데로 **바이트 동일**. 재추출본은 전부 원본의 **부분수열**(삽입·치환 0 = 없던 글자를 만들지 않음)이고, 비공백 4중 반복 잔존은 인용 가능 문서에서 0. 잔존 2건(`C106666` URL · `1588-9999` 전화)은 원문 그대로여야 할 진짜 반복 | `ai/batch/collect/funding_docs.py`, `ai/data/interim/funding_docs/*.txt` 5종 재추출, `db/init/20_finance.sql` 재생성(product 26 · chunk 16 · 상품↔청크 연결 22건 전부 동일), 회귀 테스트 `tests/test_funding_docs.py`. 근거 충실도 verbatim 1.000 · coverage 0.846 유지 |
| 86 | 2026-07-28 | BE | **진단 폼의 선택 입력은 「미기재」와 「잘못된 값」을 다르게 다룬다 (이슈 #155·#156·#157).** `age`·`monthly_investable` 은 폼에서 필수가 아닌데 검증이 하나도 없어, 둘 다 조용히 통과했다. ① **나이 미기재**는 `SessionMapper.nz()` 가 **0으로 강등**했고 0은 「만 39세 이하」 상한을 통과해 **나이를 적지 않은 사용자에게 청년 전용 상품이 편성**됐다. 강등을 없애고(`Profile.age` 를 `Integer` 로) `EligibilityFilter` 가 **나이 상한이 걸린 상품을 미기재 프로필에서 제외**한다 — `regions`·`targetGroups` 와 동형의 하향 안전 규칙이며 「확인하지 못한 자격을 주장하지 않는다」의 세 번째 축이다. ② **나이 범위**는 15~100 을 벗어나면 400. 하한 15는 법정 요건이 아니라 **음수·0 이 상한 조건을 통과하는 것을 막는 상식선**이고 상한 100도 같은 성격이다(상품 조건의 하한은 존재하지 않는다). ③ **`monthly_investable` 음수**는 400. 음수면 `m > monthlyInvestable` 이 항상 참이라 전 경계가 `REPAYMENT_OVER` 로 탈락해 T1 이 0건이 되는데, HTTP 200 + 「유의미한 대안이 없습니다」로 나가 정상으로 보였다. **0 은 허용한다** — 상환 여력 0 에서 차입 경계가 전부 빠지는 것은 정의상 맞는 결과이고, 음수와 달리 입력 자체가 모순이 아니다. ⚠️ 다만 그때 나가는 0건 사유 문장이 「현재 예산은 안정 구간입니다」(`NO_INSIGHT_REASON`)라 상환 여력이 구속 조건인 상황을 안심시키는 쪽으로 읽힌다 — 사유 갈래를 셋으로 늘리는 것은 D10 기능 동결 이후 변경이라 **본 등재로 남기고 코드는 두 갈래를 유지**한다 | 실측(2026-07-27 `57a1680` 구동본, `area_code 3120229`): age −5 / 0 / 미기재 전부 자격 부합 11건 + 청년고용연계자금 포함, age 40 은 10건. `monthly_investable` 250 → T1 2건 / −100 → 0건. 회귀: `InputValidationTest` 12건(픽스처) + 게이트 G6 확장(실적재) | `DiagnoseController.validate`, `SessionMapper.profile`, `EligibilityFilter.qualifies`, `Profile.age`(`int`→`Integer`), `scripts/qa_integration.py` G6 |
| 87 | 2026-07-28 | BE | **스코프(서울) 밖 `region_hint` 는 차단하지 않고 하향 안전 규칙에 맡긴다 — 안 B (이슈 #159).** 서울 외 지역이 들어와도 경고가 없다는 지적에 대해 **400 차단(안 A)을 채택하지 않는다.** 근거 셋: ① 현행 동작이 이미 안전한 쪽으로 틀린다 — `"부산 해운대구"`·`""`·`"ZZZ"` 입력 시 지역 제약 상품 25건이 **전건 탈락**해 자격 부합이 11건 → 1건으로 줄고(등재 #58), 이는 「확인하지 못한 자격을 주장하지 않는다」의 의도된 결과다. ② 진단 폼의 시/도 선택지가 **서울특별시 하나뿐**(`diagnoseData.SIDO`)이라 스코프 밖 입력은 **API 직접 호출로만** 도달한다 — 화면 경로에 없는 입력을 막으려고 계약의 자유 문자열 필드에 화이트리스트를 넣는 것은 비용 대비 얻는 것이 없다. ③ README 가 「후보는 **서울 전역**입니다 — 진단의 지역 입력은 지역 한정 상품의 자격 판정에 쓰입니다」로 이미 고지한다. **`/recommend` 가 서울 1,059곳을 그대로 반환하는 것도 의도된 동작이다** — 지역 입력은 후보 필터가 아니라 상품 자격 축이기 때문이다 | 실측(2026-07-27 `57a1680`): `region_hint` 를 부산/빈 문자열/무의미 문자열로 바꾼 3회 모두 자격 부합 1건 · `/recommend` 1,059곳 200. FE 시/도 선택지 1종 확인 | `EligibilityFilter.regionMatches`, `README.md` 도슨트 3번, 등재 #58 |
| 88 | 2026-07-28 | BE | **확정 예산(B₀) 미확정 시 자기자본을 예산으로 쓴다 (이슈 #167 ①·#168).** `SessionMapper.budget()` 은 `confirmedBudget` 이 null 이면 `capital` 로 떨어진다 — 「무권리 진입 구간」을 뜻하며 Javadoc 에는 있었으나 본 대장에 없어 스펙 §2-2 「모든 가정은 발생 즉시 등재」를 위반하고 있었다. **계산은 맞다** — `/budget` 없이 `/recommend` 를 부르면 자기자본 5,000 기준으로 1,059곳이 전건 `OUT_OF_SCOPE` 로 나오고, `capital` 을 20,000 으로 올리면 FIT/CAUTION 이 정상 분포한다. 문제는 **예산을 확정하지 않았다는 사실이 응답 어디에도 없다**는 것이며, 이는 계약에 해당 필드가 없어 **표기 계층에서 막는다** — FE 가 `Recommend.tsx` 의 `if (budget == null) return <Navigate to="/budget" />` 로 화면 경로를 차단하므로 데모·심사 동선에서는 재현되지 않는다. 4xx 가드는 넣지 않는다(계약 변경 없이 상태 코드만 바꾸면 FE 가드와 이중 방어가 되고, D10 이후 동작 변경이다) | 실측(2026-07-27 `57a1680`): `/budget` 미호출 `/recommend` → 200 · 1,059건 전건 OUT_OF_SCOPE. `capital 20000` → FIT 360 / CAUTION 699 | `SessionMapper.budget`, `/recommend`·`/check-area` 양쪽, FE `Recommend.tsx:257` |
| 89 | 2026-07-28 | BE | **`budget_min`~`budget_max` 는 화면 2 슬라이더의 가동 범위이며 서버 강제가 아니다 — 안 A (이슈 #167 ③·#170).** `POST /budget` 은 `< 0` 만 막고 상한을 보지 않아 `confirmed_budget: 999,999,999` 가 200 으로 통과하며 `preview.area_count` 가 전건(1,059)으로 확대된다. **서버는 범위를 기억하지 않는다** — `SessionState` 에 필드가 없고 `ScenarioBuilder.card()` 가 `/scenarios` 호출마다 계산해 던지고 버린다. 계약 §2 도 이 값을 **FE 표현 계층 언어**로 규정하며 서버 강제라고 쓰지 않았다. 세션에 카드별 `budgetMax` 를 저장해 대조하는 안 B 는 `SessionStore` 필드 1개 + 양쪽 ~12줄이며, 예선 스코프에서 얻는 것은 「API 직접 호출자의 오입력 차단」뿐이라 채택하지 않는다. **범위 밖 예산의 결과가 틀리지도 않는다** — 예산이 크면 진입 후보가 늘어나는 것이 계산의 정의다 | 실측(2026-07-27 `57a1680`): 8,398 → area_count 533 / 62,000 → 1,059 / 999,999,999 → 1,059 / −5,000 → 400 | `ScenarioController.confirmBudget`, `docs/API_CONTRACT.md` §2, FE 슬라이더 `Budget.tsx` |
| 90 | 2026-07-28 | CM | **`plan.rationale` 은 서버 템플릿 상수로 둔다 — LLM 전환 기각 (이슈 #160·#163).** 탐색 계획 이벤트에서 **축 선택(`axes`)은 LLM 이 실제로 정하지만**(`ExploreService` 가 `concerns` 를 실어 호출, `temperature=0.0`), **근거 문구(`rationale`)는 LLM 을 거치지 않는 상수 2개 중 하나**다. README 도슨트 4번이 이 문장을 「LLM 의 대화 맥락 판단」으로 안내하고 있어 도슨트 대본대로 따라가면 즉시 드러났다. **A안(문구를 사실에 맞게 정정)을 채택하고 B안(LLM 산출 전환)을 기각한다** — 전환하려면 호출 경로 + 수치 생성 금지(스펙 §0-1) 검증기 + 장애 시 템플릿 폴백이 함께 필요한데 이는 D10 기능 동결 이후의 신규 코드이고, 얻는 것은 **이미 사실인 축 선택 위에 문장 하나를 더 얹는 것**뿐이다. AI 역할 [2] 의 증빙은 `axes` 와 T1·T2 인사이트 수치가 이미 지고 있다. 도달 가능한 축 출력이 `[A1]`·`[A1,A4]` 둘뿐인 것은 A2·A3 미구현의 결과이며 등재 #47 에 있다 | 실측(2026-07-27 `57a1680`): `free_text` 를 빈 문자열/권리금/역세권/업종 변경 4종으로 바꿔 `plan` 관찰 — `concerns` 는 갈리고 `axes` 는 A1·A4, `rationale` 은 4회 동일. `risk_review.applied=true` 로 LLM 자체는 정상 동작 확인 | `ExploreService.plan`, `README.md` 도슨트 4번, `docs/API_CONTRACT.md` §5 |
| 91 | 2026-07-28 | BE | **요청 본문 상한 256 KB — 설정이 아니라 필터로 건다 (이슈 #171 ①·#172).** `application.yml` 에 본문 크기 제한이 없어 2만 자 / 200만 자 / 2천만 자 본문이 전부 200 으로 통과했다. **Tomcat 의 `maxPostSize`·`max-http-form-post-size` 는 `application/x-www-form-urlencoded` 에만 걸리고 `application/json` 에는 적용되지 않으므로** 설정 한 줄로는 닫히지 않는다. 상한값 256 KB 의 근거: 정상 요청 중 가장 큰 본문이 `POST /budget` 의 `composition` 이고 실측 200바이트 미만이며, `free_text` 가 한 화면 분량이어도 KB 단위다 — 넉넉히 세 자리 배수를 둔다. **`Content-Length` 만 본다** — 청크 전송(길이 미상)까지 막으려면 입력 스트림을 감싸 세어야 하는데 본 서비스 클라이언트는 전부 길이를 싣는다. 필터는 `DispatcherServlet` 바깥이라 `GlobalExceptionHandler` 가 닿지 않아 오류 본문을 직접 쓰되 같은 `{error:{code,message}}` 포맷을 지킨다. ⚠️ **파급이 크지 않다는 점은 그대로다** — `free_text` 는 키워드 `contains()` 매칭에만 쓰이고 세션·LLM 프롬프트에 들어가지 않아 토큰·로그 폭증 통로가 없으며, 남는 것은 요청 메모리뿐이다 | 실측(2026-07-27 `57a1680`): 2만/200만/2천만 자 전부 200. 조치 후 900 KB 본문 → 413 `PAYLOAD_TOO_LARGE`(회귀 `InputValidationTest` + 게이트 G6) | `RequestSizeLimitFilter`(신규), `scripts/qa_integration.py` G6 |
| 92 | 2026-07-28 | BE | **인사이트 언어화(refine)를 결선한다 — LLM 역할 ②의 실물 (BE-06 ③, CP4 이월 취소).** 스펙 §0-1이 규정한 LLM 3역할 중 ②(구조화된 결과의 언어화)만 화면에 실물이 없었고, 기술설명서 2장은 「LLM이 하는 일은 셋뿐」이라 적고 있어 심사위원이 「셋 다 돈다」로 읽을 수 있는 상태였다. CP4(07-26)에서 P1 이월로 판정했으나 **FE 수신부·계약 §5·타입이 이미 전부 결선돼 있고 서버 송출부만 비어 있어**(`ExploreController` 「[6]단계에서 붙는다」) 남은 일이 프롬프트와 검증기뿐이라 D10 동결 전에 마무리한다. **검증기가 반박문보다 한 단계 강하다** — `RefinePrompt.sanitize` 는 템플릿과 언어화본의 **수치 값 집합이 양방향으로 같은지**를 본다. 반박문은 「입력 밖 수치 금지」 한 방향(⊆)만 보면 됐지만 언어화는 누락도 막아야 한다: ⊆ 만 보면 **지속 안정 후보 수를 빼먹은** 문장이 통과해 상향 인사이트가 단독으로 나가고(절대 불변 원칙 3 위반), ⊇ 만 보면 **없던 수치를 덧붙인** 문장이 통과해 §0-1 이 무너진다. 둘을 함께 걸면 「수치는 그대로 두고 문장만 다시 쓴다」가 기계적으로 강제되며, 단위 변환(96개월→8년)·근사(1,014→약 1,000)도 같은 검사에 걸린다. **필수 고지는 LLM 에 주지 않는다** — 자격 한정 꼬리를 떼어 본문만 넘기고 통과한 문장 뒤에 서버가 다시 붙인다(`axis_labels`·`rate_note` 서버 단일 통제와 같은 이유). **왕복은 insight 송출 뒤, 상위 1건만.** `explore()` 안에 두면 첫 바이트가 왕복만큼 늦어져 「템플릿 즉시 → 선택적 교체」(expl §2-5)가 뒤집힌다. 전건 언어화는 동시 호출 상한 4(expl §5)를 한 요청이 3개 잡게 만들어 접속 두 명에 폴백이 시작되는데 얻는 것은 같은 종류의 문장 두 개 더다. 캐시 키는 템플릿 본문이며 실패는 캐시하지 않는다(등재 #64·D-12 와 같은 이유). **자유 텍스트 파싱은 LLM 으로 바꾸지 않는다** — 계약 §1 이 금액을 `form` 으로만 받도록 규정해 파싱이 만들 수 있는 것은 비수치 관심사뿐이고, 그 소비처인 축 선택도 도달 가능 출력이 `[A1]`·`[A1,A4]` 둘뿐이라(등재 #47) 정확도를 올려도 화면이 바뀌지 않는다. `parse_source` 는 `form_only` 로 유지한다(등재 #63) | 실기동(2026-07-28, 실적재본): 이벤트 `plan → insight×3 → refine → done`, 스트림 2.53초(냉)·1.28초(캐시 적중). 템플릿 「…진입 가능 후보는 382곳에서 1,014곳으로 늘어납니다. 다만 …지속 안정 후보는 244곳입니다.」 → 언어화 「…진입 가능한 후보 수가 382곳에서 1,014곳으로 증가합니다. 그러나 …지속 가능한 후보는 244곳으로 줄어듭니다.」 (수치 5종·고지 문구 전부 보존). 회귀: `RefinePromptTest` 12건 · `ExploreRefineTest` 5건 · 게이트 G10 | `llm/RefinePrompt`·`llm/LlmResponses`(신규, `ReviewPrompt` 중복 흡수)·`serving/RefineGenerator`(신규), `ExploreService.refine`, `ExploreController`, `CacheConfig`(`refines`), `scripts/qa_integration.py` G10, README·기술설명서 2장·심사_QA·`QA_REPORT_INTEGRATION_CM` F-7, FE 랜딩 문구 |
| 93 | 2026-07-29 | AI | **상주·직장 인구 결측은 0으로 채운다 — 유동인구·매출 결측(제외)과 규칙이 다르다 (전체 리뷰).** `serving.py:_derive()` 의 `backing = res.get(area, 0) + wrk.get(area, 0)` 이 그 자리다. 유동인구·추정매출은 결측이면 **점수 대상에서 제외**하는데(등재 #70·리뷰 #20) 배후 인구만 0 대입이라, 해당 상권은 w1 의 배후 성분이 실제보다 낮게 잡혀 **하향으로 틀린다**. 하향 편향이라 「없는 강점을 주장하지 않는다」에 어긋나지 않으므로 D10 동결 중에는 **동작을 바꾸지 않고 사실만 등재**한다. 다음 재적재 때 제외 규칙으로 통일할지 판단한다 | 실측(2026-07-29, 적재본): `location_score` 1,466개 상권 중 상주 인구 미보유 **11개** · 직장 인구 미보유 **4개** → 0 대입이 실제로 걸리는 점수 행 **18/2,497(0.72%)**. 원천 행 수 `resident_pop` 1,633 · `worker_pop` 1,641 vs `change_index` 1,650 | `ai/batch/load/serving.py` `_derive()`, `location_score.backing` → w1, 화면 3 종합점수 |
| 94 | 2026-07-29 | AI | **w4(성장)의 실질 정의는 상권변화지표 4등급의 서수 매핑이다 (전체 리뷰).** `GROWTH_RANK = {"LH": 3, "LL": 2, "HH": 1, "HL": 0}` — 상권확장 > 다이나믹 > 정체 > 축소 순으로, 이 상수가 곧 「성장」 축의 조작적 정의다. 등급 간 간격을 **등간(0·1·2·3)** 으로 두는 것도 가정이다(원 지표는 순서형이라 간격의 근거가 없다). 기술설명서 4장 표는 w4 를 「성장 — 상권변화지표」로만 적어 매핑이 문서에 없었다. 별도로, 매핑에 없는 코드가 오면 `growth.get(area, 1)`(= LL, 다이나믹)로 떨어지는 **기본값**도 가정이다 | 실측(2026-07-29, 적재본): `change_index.change_code` 분포 HH 552 · HL 353 · LH 239 · LL 506 — **4개 값이 전부 매핑에 있어 기본값 1 은 현재 한 번도 발동하지 않는다**. `location_score` 상권 1,466개 중 `change_index` 미보유 0개 | `ai/batch/load/serving.py` `GROWTH_RANK`·`_derive()`, w4 점수, `eval/common.py` 의 `DESIGN_SIGNS`(가정 #77 과 같은 축) |
| 95 | 2026-07-29 | AI | **경계 표시용 단순화 허용 오차를 5m 로 확정한다.** 상권 경계·부동산원 구획 경계 모두 EPSG:5179(미터 투영)에서 `simplify(5.0, preserve_topology=True)` 후 WGS84 소수 5자리(≈1.1m)로 기록한다. **지도 표시 전용이며 판정·점수·비용 계산에는 일절 쓰지 않는다** — 모든 수치는 기존 결정적 경로 그대로다(§0-1). 상권과 구획에 **같은 5m** 를 적용해 화면 고지 문구("최대 약 8m")가 실제 오차보다 작게 표기되는 일이 없게 한다. 10m·20m 는 「경계」라는 표기가 성립하지 않아 기각했다 | 실측(2026-07-29, 배치 동일 경로): 상권 1,650건 — 정점 221,824→38,294(17.3%), 하우스도르프 중앙 4.27m·p95 4.98m·**최대 8.08m**, 면적오차 중앙 0.351%·p95 2.079%·최대 8.330%. 상권 중앙 면적 71,874㎡(등가 정사각형 한 변 268m) 대비 변의 1.9%. 구획 72건 — 정점 4,408→2,137, 하우스도르프 최대 4.99m·면적오차 최대 0.680%. 원본 invalid 6건(Ring Self-intersection)은 단순화 이전부터 존재하며 **단순화가 새로 만든 무효·빈 도형 0건**. 기각 근거 — 10m 하우스도르프 최대 16.86m·면적오차 최대 15.04%, 20m 면적오차 최대 52.77% | `ai/batch/load/area_geojson.py`, `frontend/public/geo/area-scope.v1.json`, 화면 3 지도 캡션 고지 |
| 96 | 2026-07-29 | AI | **임대료 근거 범위는 두 경계의 넓이 차로 표시한다.** 상권을 선택하면 ① 그 상권 경계 ② 그 임대료가 실제로 조사된 부동산원 구획 경계를 **채움 없는 윤곽선 2개**로 함께 그리고, 두 면적의 배수를 근거 줄에 문장으로 적는다. 배수는 배치가 구운 EPSG:5179 투영 면적의 **나눗셈 1회**이며 서빙에서 재추정하지 않는다(§0-1). 폴백 상권은 특정 구획을 쓰지 않으므로 **구획 경계를 그리지 않는다** — 폴백이라는 사실이 경계의 부재로 화면에 드러난다. 상권 경계 조인이 실패하면 구획도 그리지 않는다(둘 중 하나만 뜨는 상태 금지) | 실측(2026-07-29): 상권 중앙 면적 0.072㎢ vs 부동산원 구획(서울 72) 중앙 0.575㎢ = **약 8배**. **district 부재 ⇔ fallback=true** 는 전건 성립하되 표현형이 계층마다 다르다 — `rent_join.py:130` 빈 문자열 → 적재본 SQL **NULL**(폴백 602행 전건) → `CandidateRowMapper:72` 가 `rent_district` 를 `orEmpty()` 없이 그대로 넘기므로(61행 `transit_station` 과 대조) API 는 **`"district": null`** 을 보낸다. FE 타입 `RentSource.district: string`(`types.ts:151`)은 이 nullable 을 반영하지 못하므로, **화면은 `district` 의 truthy 여부가 아니라 항상 존재하는 `fallback` 불리언으로 분기**한다. 구획명 유니크 72/72 — 부동산원 SHP 의 `지역코드` 가 서울 전건 NULL 이라 상권명이 유일 조인키다(등재 #12). 산출 면적 교차검증: 원천 `RELM_AR` 대비 중앙 0.075%·최대 0.101%. **배수 분포(실 API 응답 카페 비폴백 719건)**: 0.8배 미만 10건(1.4%)·0.8~1.3배 28건(3.9%)·1.3~3배 118건·3~10배 309건·10배 초과 254건(35.3%), 최대 379.7배. 두 구획이 서로 다른 분할이라 **구획이 상권보다 좁은 경우가 2.2% 있다**(명동 관광특구 0.32배) → 문구를 「보다 좁은 / 와 비슷한 / 보다 약 N배 넓은」 세 갈래로 나눈다. 실기동 조인 검증(2026-07-29, compose): 지도 상위 100곳 상권 경계 100/100 · 비폴백 81곳 구획 경계 81/81 · `district` null 340 = `fallback` 340, 불일치 0건 | `ai/batch/load/area_geojson.py`, `frontend/src/lib/format.ts` `formatRentScope()`, `frontend/src/components/AreaCard.tsx` 근거 4번째 줄 |
| 97 | 2026-07-29 | AI | **임대료 폴백은 자치구별로 극단 편중돼 있다 — 임대료 근거의 신뢰도가 지역마다 다르다.** 전체 비율(36.5%)만 적으면 이 불균등이 가려지므로 등재하고, 화면에서도 구획 경계의 유무로 상권 단위에서 드러낸다. 폴백 자체를 없애지는 않는다 — 부동산원 조사 구획이 서울에 72개뿐이라 커버되지 않는 상권이 **구조적으로** 존재한다. 문서 기재 모수는 상권 전체 1,650 기준으로 통일한다(업종별 후보 풀 기준과 값이 달라 혼선이 생긴다) | 실측(2026-07-29, 적재본 `rent` 1,650행): 폴백 **602건(36.5%)** — 등재 #18 의 36% 와 정합. 자치구별 폴백률 상위 도봉 81%·구로 77%·서대문 74%·노원 71%·동작 61%, 하위 중구 1%·동대문 4%·마포 10%·강남 15%·종로 16% | `docs/기술설명서_원고.md` 한계 서술, 화면 3 구획 경계 부재, 등재 #18(폴백 규칙) |
| 98 | 2026-07-29 | AI | **서울 상권영역은 4개 층이 한 파일에 들어 있어 상권끼리 겹친다 — 같은 지역이 둘 이상의 후보에 오른다.** 골목(A)·발달(D)·전통시장(R)·**관광특구(U)** 는 서로 배타적인 분할이 아니라 별개 레이어다. 관광특구 6곳(면적 중앙 0.63㎢)이 하위 상권을 통째로 품어, 잠실 관광특구 안에 방이동먹자골목·잠실역이 들어가는데 셋이 후보 목록에 **각각** 올라온다. **후보 목록을 바꾸지 않는다** — 관광특구를 빼면 판정 대상이 바뀌어 랜딩 지표와 도슨트 수치가 전부 따라 움직이고, 관광특구가 실제로 좋은 입지일 수 있는데 선택지에서 사라진다. 대신 **상권 구분 라벨과 중첩 사실을 화면에 적는다**. 중첩 판정에는 **자기 면적 대비 10% 임계**를 둔다 — 임계가 없으면 경계선이 스치는 수준의 쌍이 화면을 채운다 | 실측(2026-07-29, SHP 1,650): 구분별 골목 1,090(중앙 0.077㎢)·전통시장 305(0.023㎢)·발달 249(0.150㎢)·관광특구 6(0.630㎢). 교차 쌍 5,128 중 **98.0%(5,028쌍)가 0.01% 미만** — 실질 중첩은 ≥1% 77쌍·**≥10% 58쌍(52개 상권)**·≥30% 42쌍. 완전 포함 24쌍. 잠실 관광특구(2.46㎢)가 품는 곳 — 방이동먹자골목 76.0%·몽촌토성역 75.7%·잠실역 51.4%·석촌역 14.5%·송파나루역 10.2%. 산출물 872→949KB(gzip 224KB) | `ai/batch/load/area_geojson.py` `_overlaps()`·`OVERLAP_MIN_PCT`, `frontend/src/lib/format.ts` `formatScopeOverlap()`, `AreaCard` 유형 라벨·중첩 줄 |
| 99 | 2026-07-29 | AI | **중첩 상권에 걸친 인허가는 양쪽 상권에 각각 계상한다 (가정 #69 전제의 반증·수정).** 밀도는 상권마다 「제 폴리곤 안의 업소 ÷ 제 면적」으로 **독립 계산**되는 값이라 보존해야 할 전역 합계가 없다 — 명동 발달상권 안의 식당은 그 상권에 들어갈 사람에게도, 이를 품은 관광특구에 들어갈 사람에게도 똑같이 경쟁이다. 종전 `competition.py` 는 `sort_values("area_code")` + `duplicated(keep="first")` 로 업소당 한 상권만 남겼는데, 그 기준이 **area_code 사전순**이라 중첩 상권 중 코드가 작은 쪽이 인허가를 통째로 가져갔다. 그 결과 **밀도 결측 → 폴백 0 → `percentile(-0.0)` = w3(경쟁여유) 1.0**, 즉 서울에서 가장 빽빽한 상권이 「경쟁 여유 최상위」로 뒤집혔다. 가정 #69 의 폴백(미조인 = 관측 업소 0건) **자체는 유지**하되, 그 근거였던 「미조인은 실제로 업소가 없다에 가깝다」는 **이 수정 후에도 성립하지 않는다** — 잔여 27건은 인허가 원천 기준으로는 폴리곤 내부 0건이지만(containment 재계산으로 확인), 같은 행의 서울시 `store_cnt` 는 **27/27 전건이 2~17개**다(수정 전에도 58/58 전건). 폴리곤을 50m 버퍼하면 27곳 중 23곳에서 인허가 점이 잡히므로 실체는 「업소가 없는 곳」이 아니라 **「경계 바로 밖에 업소가 있는 좁은 상권」**이다. 즉 이번 수정이 바꾼 것은 **영향 범위(58→27건)이지 폴백의 전제가 아니며**, 잔여 27건은 여전히 w3 = 1.0 으로 나간다(최고 순위 cafe 63위·food 187위). 폴백을 `store_cnt ÷ 면적` 으로 바꾸면 w3=1.0 이 cafe 26→1 로 떨어지나 이는 #69 후속 과제로 남긴다 | 실측(2026-07-29, 배치 재실행 + 적재본 대조). **원인**: 잠실 관광특구(3001493)·명동 관광특구(3001492)가 사전순으로 앞서 방이동먹자골목(3120229)·명동거리(3120028)·을지로입구역(3120026)·북창동(3120022)의 인허가를 흡수 — 중첩 비율은 가정 #98 실측(방이동 76.0%·잠실역 51.4%)과 동일 원인. **변경 폭**: 밀도가 바뀐 (상권,업종) **95 / 3,183**, 관광특구·강남역 등 비중첩 상권은 전건 불변. `store_density` 밀도 NULL 201→157행, `location_score` 인허가 미조인 **58→27**건 (201 은 같은 문장 앞의 store_density 밀도 NULL **행수**이고, 미조인 지표는 `serving.py` 의 `missing_den` 이라 모수가 다르다 — 등재 #69 의 「58건」과 같은 정의). **w3 = 1.000 : cafe 41→26 · food 17→1**, 잔여분 최고 순위 cafe 63위/1,059 · food 187위/1,438 로 표시 상위권에서 이탈. **개별**: 명동(명동거리) food NULL→465개 20.992/만㎡, 북창동 food NULL→233개 21.471/만㎡, 방이동먹자골목 food 80→365개 5.454→24.882/만㎡. **독립 검증**: `shapely.within` 으로 폴리곤별 재계산 시 5개 표본 전건 일치, 잔여 미조인 표본 5건 모두 폴리곤 내부 실제 0건. 회귀: `ai` 122 passed · `ruff` clean · `backend` gradlew test 통과 | `ai/batch/preprocess/competition.py` `build()`, `db/init/10_data_core.sql` 재생성, w3 축, 가정 #19(중첩 정리)·#69(밀도 폴백)·#98(레이어 중첩) 상호작용 |
| 100 | 2026-07-29 | AI | **성장(w4) 결측의 기본값 1(LL·다이나믹)은 다른 결측과 규칙이 다르다 — 그 차이 자체를 등재한다.** 매출·유동인구는 결측이면 **점수 대상에서 제외**하는데(등재 #20·#21) 성장만 중간값을 대입한다. 성장은 5단계가 아니라 4등급 순서형이라 「제외」가 곧 상권 하나를 후보에서 통째로 지우는 것이 되고, 하향 안전 방향(정체=중립)으로 채우는 편이 손실이 작기 때문이다. 가정 #94 가 이 기본값의 **존재**는 적었으나 「왜 다른 결측과 규칙이 다른가」는 등재돼 있지 않았다 (AI 리뷰 P2) | 실측(2026-07-29, 적재본): `change_index.change_code` 4개 값이 전부 `GROWTH_RANK` 매핑에 있고 `location_score` 상권 1,466개 중 미보유 0개 — **현재 이 폴백은 한 번도 발동하지 않는다**. 즉 등재의 목적은 값의 정당화가 아니라 규칙 비대칭의 기록이다 | `ai/batch/load/serving.py` `growth.get(area, 1)`, w4 점수, 가정 #94 |
| 101 | 2026-07-29 | AI | **「은행 대출 금리 − 이자 지원 금리」는 변동금리로 적재한다 (F-002 교정).** 변동 판정 정규식이 「차감」이라는 낱말과 「기준금리+가산」 두 형태만 보고 있어, 뺄셈 기호로 쓴 같은 뜻의 표현을 놓쳤다. 그 결과 **은행금리에 연동해 움직이는 상품이 FE 분기 키(`rate_type`)에서 '고정'으로 실렸다.** 금융 정보 서비스에서 금리 *성격*의 오표기는 값의 오표기와 같은 무게다 — 사용자가 재확인 시점을 판단하는 근거가 그 한 글자다. 기준이 되는 은행금리를 지어내지 않는다는 원칙은 그대로다(`rate` 는 여전히 NULL + `rate_note` 원문) | 실측(2026-07-29): 검수본 26건의 `rate_note` 전건에 신·구 정규식을 대조한 결과 **판정이 바뀌는 행은 F-002 하나뿐**. 「정책자금 기준금리 3.85% + 0.6%p」 계열은 숫자가 사이에 있어 구 패턴에도 걸리지 않았고 검수 확정값 경로에서 이미 `variable` 이라 불변. `20_finance.sql` 재생성 diff = 해당 1행 + 헤더. 적재본 rate_type 분포 fixed 7→6 · variable 19→20 | `ai/batch/load/finance.py` `_BASE_VAR`, `db/init/20_finance.sql` F-002, 등재 #30(F-002 검수 게이트), QA 리뷰 Q-07 |
| 102 | 2026-07-29 | CM | **덤프 헤더에서 생성 날짜를 뺀다 — 재현성 대조 비용을 없애기 위해서다.** `emit_sql` 이 `date.today()` 를 헤더에 적어, 같은 입력으로 다른 날 재생성하면 1.6MB 파일에 헤더 한 줄 diff 가 났다. 부록이 재현성을 명시적으로 대조하는 프로젝트라 「내용이 같은가」를 확인하는 비용이 매번 발생했다. 덤프의 기준일은 각 행의 `data_as_of` 가 이미 싣고 있으므로 파일 헤더의 실행 날짜는 정보가 아니라 잡음이다 | 실측(2026-07-29): 헤더 제거 후 `python -m batch.load core` 재실행 시 `10_data_core.sql` diff **헤더 1줄 외 0** — 12테이블 전량이 바이트 단위로 동일하다. 재현성의 부수 증명이기도 하다 | `ai/batch/load/emit.py` `emit_sql()`, `db/init/10_data_core.sql`·`20_finance.sql` 헤더 |
| 103 | 2026-07-29 | CM | **오케스트레이션 헬스체크는 `/actuator/health` 본문의 `"status":"UP"` 으로 판정한다.** 계약 엔드포인트 `/api/health` 는 상수 응답이라 DB 가 완전히 내려가도 200 `ok` 를 준다 — compose·CI 스모크가 그것을 근거로 삼으면 **전 엔드포인트가 500 인 스택을 정상으로 통과시킨다**. 계약 응답 형태는 그대로 두고 판단 근거만 옮긴다. 검사 방식에 조건 둘이 붙는다: ① `CMD-SHELL` 이 아니라 **`CMD` + `bash -c`** — 이미지의 `/bin/sh` 가 **dash** 라 `/dev/tcp` 를 지원하지 않는다 ② 상태줄이 아니라 **본문**을 본다 — Tomcat 이 이유구를 넣지 않아 `200 OK` 로는 영원히 매칭되지 않는다. 둘 중 하나만 틀려도 헬스체크가 **항상 실패**해 장애를 드러내려던 장치가 거짓 장애를 만든다 | 실측(2026-07-29, compose 기동): `eclipse-temurin:25-jre` — `/bin/sh -> dash`, bash 있음, curl·wget·nc 없음. dash 에서 `/dev/tcp` → "cannot create /dev/tcp/…". 상태줄 실측 `HTTP/1.1 200 `(이유구 없음). **DB 정지 → 80초 후 `unhealthy`** 전이 확인, 같은 시점 `/api/health` 200 `ok` · `/actuator/health` 503 · 컨테이너 `running` 유지(C-03 보존). **DB 재기동 → 25초 후 `healthy` 복귀**, 재기동 없이 `POST /diagnose`·`GET /scenarios` 200 회복 | `docker-compose.yaml` api healthcheck·`restart: unless-stopped`, `.github/workflows/develop-ci.yml` 스모크, `docs/API_CONTRACT.md` §시스템 계약 |
| 104 | 2026-07-29 | BE | **`v_candidate_area` 의 `rent` INNER JOIN 으로 사라지는 상권은 없다.** `transit` 은 LEFT JOIN + `fallback_flag` 로 결측을 명시적으로 다루는데 `rent` 만 INNER 라, 임대료 행이 없는 상권이 후보 목록에서 **오류도 플래그도 없이 조용히 빠질** 수 있는 구조였다(BE 리뷰 N-11, 미확인 항목). 실측으로 현재 적재본에서는 결측이 0 임을 확인했다 — 조인 방식을 바꾸지 않는다. 다만 이 사실은 **적재본에 의존**하므로, `rent` 산출 경로가 바뀌면 같은 질의로 다시 확인해야 한다 | 실측(2026-07-29, 기동 DB): `SELECT count(*) FROM location_score s LEFT JOIN rent r USING (area_code) WHERE r.area_code IS NULL` → **0**. 후보 뷰 총계 cafe 1,059 · food 1,438 로 종전과 동일 | `db/init/01_schema.sql` `v_candidate_area`, `backend/.../CandidateRepository`, 랜딩·도슨트 후보 수 |
| 105 | 2026-07-30 | FE | **진단 폼의 「추가 정보(선택)」 3종(창업 희망 시기·희망 운영 형태·상권 유형)을 제거하고, 상권 유형은 화면 4의 필터로 옮겼다.** 셋 다 계약 `form` 에 필드가 없어 `free_text` 꼬리표로만 붙었는데, 서버가 읽는 키워드는 `권리금`·`임대/월세`·`유동/손님` 3군뿐이라 **어느 선택지도 한 번도 걸리지 않았다** — 골라도 조달 시나리오·예산·추천 목록·지도가 글자 하나 달라지지 않는 입력이었다. 특히 「상권 유형」의 선택지(오피스/주거/대학가/번화가)는 **실데이터에 대응 개념이 없다**: 서울시 상권 구분은 골목·발달·전통시장·관광특구이고, 배후 성격(직장/상주 인구)으로 다시 만들려면 임계값을 우리가 정해야 하는데 그러면 출처 없는 분류가 하나 생긴다(「왜 직장인구 비중 X%가 오피스 상권인가」에 답할 근거가 없다). 「대학가」는 데이터 자체가 없다. **자리도 틀렸다** — 예비창업자가 1단계에서 상권 유형을 알 리 없고, 이 서비스의 명제가 「미리 고르는 게 아니라 예산이 열어주는 걸 본다」이므로 미리 선언하게 하는 것은 방향이 반대다 | 실측(2026-07-29): 오피스 상권 → 대학가 → 번화가로 바꿔 각각 주행한 결과 진단 결과·조달 시나리오·지도 지표·추천 목록의 화면 문자열 해시가 **전부 동일**(`bf8b3ba082`·`6115c8d7bf`·`65c9031302`·`2d405858ed`) | `screens/diagnoseData.ts`, `screens/Diagnose.tsx`, `screens/Recommend.tsx` |
| 106 | 2026-07-30 | FE | **화면 4 상권 구분 필터는 서울시 공식 구분(`TRDAR_SE_CD`)을 그대로 쓴다 — 새 분류를 만들지 않는다.** 값은 `commercial_area.area_type_name`(A 골목/D 발달/R 전통시장/U 관광특구)이며 `/geo/area-scope.v1.json` 에 **1,650곳 전건** 실려 있어 API 왕복도 계약 변경도 없다(이미 `AreaCard` 의 구분 라벨과 근거 문장이 이 값을 쓰고 있었다). 칩의 설명 문구(「주택가·이면도로 중심」 등)는 **읽기만 돕고 분류를 바꾸지 않는다** — 화면이 이 이름을 적는 것은 분류가 아니라 인용이다. 경계 자산 로드는 선택 경로라 실패하면 필터가 아예 노출되지 않고 나머지 화면은 그대로 동작한다 | 실측 분포: 골목상권 1,090 · 전통시장 305 · 발달상권 249 · 관광특구 6 (합 1,650, `t` 필드 결측 0건) | `screens/Recommend.tsx`(`AREA_TYPE_FILTERS`), `lib/areaScope.ts` |
| 107 | 2026-07-30 | BE·FE | **목 폴백은 「서버에 닿지 못했다」의 대응이며 4xx 는 대상이 아니다.** 종전 `client.ts` 는 `if (!res.ok) throw` 한 줄로 400 과 5xx·네트워크 단절을 같은 자리로 보냈다. 그래서 진단에서 나이를 `999` 로 넣으면 서버가 「age 는 15~100 범위여야 합니다」로 정확히 거절했는데도 화면은 그 문장을 버리고 **목 데이터로 세션 전체를 전환**했고, 사용자는 거부당한 줄 모른 채 다음 화면으로 넘어갔다(목 데이터의 진입 후보가 0곳이라 막다른 길이 됐다). 폴백 플래그는 되돌지 않는 설계라 입력을 고쳐 성공한 뒤에도 진짜 수치 위에 「실제 조사 결과가 아닙니다」라는 **거짓 경고**가 남았다. `ApiError` 를 도입해 4xx 는 폴백·배너 없이 던지고, 진단은 **서버가 준 문장을 그대로** 표시하며, 404(세션 없음)는 첫 화면으로 되돌린다. SSE 두 경로는 `EventSource` 가 상태코드를 노출하지 않아 종전대로다 | 회귀: `diagnoseData.test.ts` 7건(경계 15·100 통과 / 14·101·999·1994 차단) | `api/client.ts`, `screens/Diagnose.tsx`, `screens/Budget.tsx`, `screens/Recommend.tsx`, `screens/diagnoseData.ts` |
| 108 | 2026-07-30 | BE | **리스크 검증 동기 상한 1.2초 → 3초.** 1.2초는 「빠른 왕복은 살리고 느린 왕복만 버리는」 자리로 잡았으나 실측에서 **살아남는 왕복이 하나도 없었다** — `limit_ms=1200` 호출 33건이 전건 timeout, `limit_ms=5000` 호출 6건은 전건 성공(elapsed 812·1557·1734·1844·2212·2347ms, 중앙값 약 1.79초). 게다가 `ReviewGenerator` 가 `unless="#result.skipped()"` 로 **성공만 캐시**하므로 전건 타임아웃이면 캐시가 영원히 비고 매 요청이 상한을 꽉 채워 실패했다 — 지연은 지연대로 내면서 결과는 항상 템플릿이었고, 화면에는 조건과 무관하게 「리스크 검증 생략」이 상시 노출됐다(AI 3역할 중 하나가 데모 내내 꺼져 있었다). 슬라이더 재호출 우려는 성립하지 않는다 — `RiskReviewAgent` 가 예산을 캐시 키에서 의도적으로 제외해 한 번 성공하면 상위 3곳 판정이 바뀌기 전까지 왕복이 사라진다 | 실측(2026-07-29, 기동 중 컨테이너 로그 통계) | `llm/LlmSettings.java` |
| 109 | 2026-07-30 | FE | **`/explore` 가 1280px(13인치)에서 가로로 넘쳤다 — 좌열 440px 고정 + 행의 `flex-wrap` 부재가 겹친 결과.** `ScenarioRow.top` 은 지표 3개가 `flex: none` + 값·부제 `nowrap` 이라 약 630px 하한을 가졌는데, 우열 폭은 1440에서 630px(겨우 걸침) → 1280에서 470px 로 줄어 **약 160px 이 카드 밖으로 나갔다.** 전역에 `overflow-x: hidden` 이 없어 페이지에 가로 스크롤바가 생겼다. 1440에서만 확인하면 보이지 않는 자리이며, 브레이크포인트가 **1201~1439 구간에 하나도 없어** 13·14인치가 전부 이 구간을 쓴다. 접히는 순서를 ①부제 줄바꿈 ②지표 축소 ③행 접힘으로 두고, `Explore` 브레이크포인트를 1200→1500 으로 올렸다. 전역에 `word-break: keep-all; overflow-wrap: anywhere` 를 넣어 끊을 수 없는 토큰(공고 URL·상권명)이 부모를 밀어내는 부류를 함께 없앴다 | 회귀 확인: `/explore` 를 1280×800 으로 띄우고 `document.documentElement.scrollWidth > innerWidth` 가 false | `components/ScenarioRow.module.css`, `screens/ExploreSummary.module.css`, `screens/Explore.module.css`, `styles/global.css` 외 5개 |
| 110 | 2026-07-30 | FE | **화면 4의 지도·목록 기본 표시를 「진입 가능(적합 + 유의)」으로 좁히고, 조건부 적합은 지도 아래 전용 패널로 분리한다.** 종전 기본값은 「전체」였고 마커 상한이 100곳이라, 확정 예산 1억에서 **지도의 마커 100개 중 실제로 갈 수 있는 곳은 36개**뿐이었다 — 나머지 64개는 무권리 매물을 잡아야 열리는 조건부 적합이 종합점수순으로 섞여 들어온 것이다. 「내 한도로 어디까지 가능한가」를 답하는 화면의 지도가 한도와 무관한 후보로 채워져 **풍족해 보이는 착시**를 만들고 있었다. 리스크 검증 주장문의 「상위 후보」도 같은 이유로 1순위가 신림역 8번(조건부 84점), 즉 무권리 매물 없이는 갈 수 없는 곳이었다. **스펙 §0-4 저촉 없음** — 금지된 것은 「상태 추가」이지 부분 표시가 아니며(판정별 필터 칩은 이전부터 부분 표시를 했다), 판정 어휘 3종은 필터 칩·분리 패널·범례가 계속 노출한다. **API 계약 무변경**(서버는 4판정 전부 반환) — D3 동결과 무관하고 BE·AI 무수정이다. 분리 패널의 문구는 **무권리 매물이라는 조건만 서술**한다: 「예산을 올리면 열립니다」로 쓰면 상향 인사이트가 되어 지속 후보 수·하향 안전 마진·고지 문구를 동반해야 하며(CLAUDE.md 원칙 3), 그 서사는 세 요소를 갖춘 탐색 화면의 몫이다. 범례도 **지금 찍힌 판정만** 싣도록 바꿨다 — 좁힌 지도에 조건부 적합 범례가 남으면 없는 마커를 찾게 된다. 상단 카드의 큰 숫자도 1,018곳(범위 외만 제외) → 382곳(진입 가능)으로 옮겨, 하단 슬라이더의 「진입 가능 N곳」·서버 프리뷰 `area_count` 와 같은 정의를 쓴다 | 실측(2026-07-30, 기동 스택 예산 스윕 — 적합/유의/조건부/범위 외 · 진입 가능 · 상위 100 중 진입 가능): 6,000만 0·0·0·1,059 → 0곳 · **0개**; 8,000만 1·5·542·511 → 6곳 · **1개**; **1억(도슨트) 94·288·636·41 → 382곳 · 36개**; 1.2억 192·645·218·4 → 837곳 · 76개; 1.5억 227·808·20·4 → 1,035곳 · 97개. 즉 좁힌 뒤에도 도슨트 경로에서는 마커 100개가 그대로 차고(진입 가능 382곳), 지도가 비는 것은 6,000~8,000만 구간뿐인데 그 구간의 빈 지도는 결함이 아니라 이 서비스의 명제 자체다. 회귀: FE 43건 통과(`matchVerdict`·`ENTRY_VERDICTS` 3건 신설) · 통합 게이트 **21/21 PASS**(D1 대본 상위 후보를 진입 가능 기준으로 교체 + 조건부 상위 3곳 단언 신설) | `lib/verdict.ts`(`ENTRY_VERDICTS`·`matchVerdict`), `screens/Recommend.tsx`(`VERDICT_FILTERS` 기본 `ENTRY`·분리 패널·`riskClaim`), `components/KakaoMap.tsx`(`drawnLegend`·`legendAction`·`fitToken`), `README.md` 도슨트 3번, `docs/tasks/CM-04_도슨트_테스트_프로토콜.md` §1-1, `scripts/qa_integration.py` `DOCENT["top3"]`·`top3_conditional` |
| 111 | 2026-07-30 | FE | **지도 뷰포트의 기준은 「표시 중인 마커」가 아니라 「후보 전체(서울)」다 — 가정 #110 의 후속 교정.** 표시 범위를 진입 가능으로 좁히자 후보가 적은 예산에서 초기 뷰포트가 통째로 확대됐다. 뷰포트를 표시 마커에 맞추고 있었기 때문이며, 「서울 전체 보기」 버튼도 같은 기준을 써서 눌러도 서울로 돌아오지 못했다. 뷰포트 기준을 **범위 외까지 포함한 후보 전체**로 옮긴다 — 그 집합만이 예산·필터와 무관하게 서울 전역으로 고정돼 있다. 상수를 박지 않고 응답에서 얻으므로 적재본이 바뀌어도 따라온다. **자동 선택으로는 지도를 움직이지 않는 규칙**도 함께 넣는다: 진입 시 1위 상권이 자동 선택되는데 그 말풍선이 상단에 걸리면 잘림 방어(`panTo`, 이슈 #154)가 곧바로 발동해 첫 화면이 서울 전체가 아니게 된다. 이동 규칙 자체는 **사용자가 마커를 고른 뒤**의 규칙으로 남긴다 | 실측(2026-07-30, 기동 스택): 확정 예산 8,000만의 진입 가능 6곳은 전부 노원구 상계동 — 위도 37.6564~37.6673 · 경도 127.0675~127.0776, 즉 **위도폭 0.0109°·경도폭 0.0101°(약 1.2km × 0.9km)** 안에 몰려 있다. 반면 후보 전체 1,059곳은 위도 37.4346~37.6898 · 경도 126.8085~127.1738 로 서울 전역. 자동 선택 1위(상계5동주민센터 37.6642)는 서울 전체 뷰의 상단 잘림 임계(약 37.60)를 넘어 종전 규칙에서는 진입 즉시 `panTo` 가 발동했다. 1억(도슨트)에서는 진입 가능 상위 100곳이 이미 위도폭 0.1995°·경도폭 0.3367° 로 서울 전역이라 이 경로에서는 증상이 드러나지 않았다 | `components/KakaoMap.tsx` (`fitAreas`·`initialSelectRef`), `screens/Recommend.tsx` (`fitAreas={data.areas}`) |
| 112 | 2026-07-30 | AI·FE | **상권 경계 자산에 자치구(`g`)를 함께 굽고, 화면 4에 자치구 필터를 신설한다 — 진단의 「희망 지역」이 화면에 닿는 유일한 축이다.** 종전에는 희망 지역을 무엇으로 고르든 화면이 한 글자도 달라지지 않았다. 서버는 상권 후보를 지역으로 자르지 않고(서울 전역, 설계 판단 — 심사_QA.md), 적재된 금융상품 26건의 지역 제약이 **25건 `{서울}` + 1건 무제약**, 즉 전부 시/도 해상도라 `EligibilityFilter.regionMatches` 가 시/도에서 이미 통과시키기 때문이다. 게다가 폼의 `SIDO` 선택지가 서울 하나뿐이고 구는 필수 검증이라, **폼으로 만들 수 있는 어떤 조합도 결과를 바꾸지 못했다.** 자르는 주체를 서버에서 **사용자**로 옮긴다 — 기본값은 전체(미리 좁히지 않는다)이고, 진단에서 고른 구는 「희망 지역 ○○구만 보기」로 한 번에 적용된다. 자치구를 계약(`/api/recommend`)이 아니라 **정적 자산**에 싣는 이유는 유형(`t`)과 같다: 상권의 불변 속성이라 응답에 넣으면 슬라이더를 움직일 때마다 1,059곳분이 재전송된다. 자산 로더의 캐시 모드도 `force-cache` → `no-cache` 로 바꾼다 — 파일명·schema 는 판본(v1)만 가리키고 내용을 가리키지 않아, 필드를 더해도 옛 파일을 쓰는 브라우저에서 **오류 없이 필터만 사라진다** | 실측(2026-07-30, 기동 스택): 같은 프로필로 지역만 바꿔 전 구간 주행 후 해시 대조 — 마포구·강남구·노원구 3행이 시나리오(`5d7adeec0e`)·추천 목록(`9ad4252c70`)·자격 부합 상품(11건 `ec511f0929`) **전부 동일**, 부산 해운대구·미입력만 시나리오 `ef27f1e20b` · 자격 부합 **1건**으로 갈렸다. 추천 목록은 다섯 행 모두 동일. 자산 재생성 대조: 상권 1,650곳 코드 집합 동일, **`g` 를 뺀 나머지 필드가 달라진 상권 0건**, `districts`·`overlaps`·헤더 전부 불변, `g` 결측 0, 자치구 25종(진단 폼 `SEOUL_GU` 와 글자 단위 일치). 파일 949→975KB. 회귀: AI 133건(`test_areas_carry_sigungu` 신설) · ruff clean · FE 47건(`guFromRegionHint` 4건 신설) | `ai/batch/load/area_geojson.py` `_pack(extra=...)`, `frontend/public/geo/area-scope.v1.json`, `lib/areaScope.ts`(`sigungu`·`guFromRegionHint`·캐시 모드), `screens/Recommend.tsx`(`guFilter`·`wishGu`·자치구 드롭다운), `docs/심사_QA.md` 지역 문답, `README.md` 도슨트 3번 |
| 113 | 2026-07-30 | FE | **화면 4의 자치구 필터 기본값을 「전체」에서 「진단의 희망 지역」으로 옮긴다 — 첫 진입에서도, 예산을 조정한 뒤에도.** 가정 #112 는 「서비스가 먼저 좁히면 사용자가 모르고 지나쳤을 선택지가 화면에 오르지 못한다」는 이유로 기본을 전체로 뒀는데, 그러면 희망 지역을 고른 사람이 자기 지역을 보기 위해 그것을 **한 번 더** 눌러야 한다. 좁힘을 되돌리는 비용(칩 한 번)이 좁히지 않아 생기는 비용(내 지역을 못 찾음)보다 작다. 「모르고 지나칠 선택지」는 드롭다운이 25개 구의 후보 수를 전부 노출하는 것과 「서울 전체로」 한 번으로 지킨다 — 지워지는 것이 아니라 한 칸 뒤에 있다. **예산마다 다시 판단한다**(한 번만 적용하지 않는다): 확정 예산 8,000만에서 마포구는 0곳이고 1억이 되어야 20곳이 열리므로, 1회 적용이면 낮은 예산으로 들어온 사람은 예산을 올려도 자기 지역으로 돌아오지 못한다. **후보가 0곳이면 좁히지 않는다** — 진입하자마자 빈 목록을 보여 주는 대신 서울 전체를 두고 「확정 예산으로 ○○구에서 …판정을 받은 곳은 아직 없어 서울 전체를 보여드립니다」라고 적는다. 사용자가 자치구를 **직접** 건드리면(`guTouchedRef`) 자동 적용이 영구히 멈춘다 — 「서울 전체로」를 눌러 둔 사람에게 슬라이더를 움직일 때마다 자기 구로 되돌아오게 하면 화면이 사용자의 결정을 계속 무르는 셈이 된다. 지도 안내 문구에는 좁힌 자치구명과 **서울 전체 수**를 함께 적어, 상단 카드의 큰 숫자와 지도의 수가 달라 보이는 이유를 그 자리에서 말한다 | 실측(2026-07-30, 기동 스택 — 희망 지역 마포구, 진입 가능 기준 자치구별 후보 수): 8,000만 → 전체 6곳(노원구 6 · **마포구 0** · 후보가 있는 구 1개), 1억 → 전체 382곳(**마포구 20** · 송파구 58 · 강남구 4 · 노원구 6 · 후보가 있는 구 19개), 1.2억 → 837곳(마포구 33 · 25개 구 전부), 1.5억 → 1,035곳(마포구 57). 즉 0곳 예외는 가상의 방어가 아니라 **보수 카드 기본 예산(8,000만)에서 실제로 걸리는 경로**다. 회귀: FE 47건 · 통합 게이트 21/21 PASS | `screens/Recommend.tsx`(`guTouchedRef`·`chooseGu`·희망 지역 자동 적용 효과·`mapNote`·0곳 안내), `docs/심사_QA.md` 지역 문답, `README.md` 도슨트 3번 |
