# FE 태스크 상세 — `frontend` 브랜치 · `frontend/` 디렉토리

담당: FE 1인 · 스택: React 18 + Vite + TS + 카카오맵 JS SDK + SSE(EventSource)
개요·의존 관계는 [../TASKS.md](../TASKS.md), API 스키마는 [../API_CONTRACT.md](../API_CONTRACT.md).

## 0. 환경 준비 (Day 0)

- [ ] `git config core.hooksPath .githooks` (co-author 차단 훅)
- [ ] `cp .env.example .env` → `VITE_KAKAO_APP_KEY` 입력 (FE-01에서 발급)
      — Vite dev·docker compose 모두 **루트 .env** 하나를 읽는다 (frontend/.env 만들지 말 것)
- [ ] `cd frontend && npm install && npm run dev` → http://localhost:5173 확인
- [ ] `npm run lint && npm run build` 그린 확인 (CI와 동일 명령)

## FE-01 (D1) — 카카오맵·SSE 스파이크

- [ ] developers.kakao.com 앱 생성 → JS 앱키 발급 → 플랫폼 도메인 등록
      (`localhost:3000`, `localhost:5173`) → **'카카오맵 사용 설정' ON** (2024-12 이후 신규 앱 필수)
- [ ] `index.html`의 SDK 스크립트 주석 해제, `autoload=false` + `kakao.maps.load()` 패턴 확인
- [ ] 스파이크: 지도 렌더 + 마커 3개 + 폴리곤 1개 + 클릭 이벤트 (버리는 코드 허용, `frontend/spike-*` 브랜치)
- [ ] 스파이크: `EventSource`로 SSE 수신 데모 (BE 목 서버 또는 public echo)
- **DoD**: 지도+마커+SSE가 로컬에서 도는 스크린 레코딩 1개 팀 공유

## FE-02 (D3~4) — 화면 1: 자금 진단

- [ ] `src/screens/Diagnose.tsx` — 폼(나이·자기자본·업종·지역 힌트) + 자연어 텍스트 영역 하이브리드
- [ ] **"데모 프로필 불러오기" 버튼** (화면 상단 고정): 외식창업자/만 32세/자기자본 5,000만/망원 카페
      1클릭 주입 (스펙 §0-8 — 심사위원 원클릭 동선의 시작점)
- [ ] `POST /api/diagnose` 연동 → `parsed_profile` 확인 UI (파싱 결과를 사용자가 수정 가능하게)
- [ ] API 클라이언트 모듈 `src/api/client.ts` — 계약 문서의 DTO를 TS 타입으로 옮김 (`src/api/types.ts`)
- **DoD**: 목 API로 진단→파싱 결과 확인 흐름 재현 (CP1 합동 점검 참여)

## FE-03 (D4~6) — 화면 2·3 골격

화면 2 (조달 시나리오):
- [ ] `src/screens/Scenarios.tsx` — 카드 2장(보수/적극), 예산·구성 내역·**추천 배지 + 출처 배지**
      (스펙 §7 — 심사 도슨트 2단계에서 "배지 주목"이 명시된 요소)
- [ ] `GET /api/scenarios/{sid}` SSE 수신 → 카드 점진 렌더
- [ ] 시나리오 선택 → `POST /api/budget/{sid}` → 화면 3 전환

화면 3 (지도 판정):
- [ ] `src/screens/MapView.tsx` — 카카오맵 + 마커 3종 (🟢 적합 / 🟠 정속 / ⚪ 조건부 적합)
      ⚠️ 마커 종류 임의 추가 금지 (스펙 §0-4)
- [ ] 근거 패널: 마커 클릭 시 breakdown·비용 구간(권리금 이중 표기)·reason_text
      + **임대료 출처 셀** "임대료: 한국부동산원 {상권명} 분기 평균 (추정)" (fallback 시 "자치구 평균")
      + **교통 셀** "도보 {N}분 내 {역명}({호선}) — 일평균 승하차 {인원}"
- [ ] 예산 슬라이더 (하단 고정): 값 변경 → `/api/recommend` 재호출 골격
- [ ] 데이터 기준일 상시 표기 (`data_as_of`)
- **DoD**: 목→실데이터 전환(CP2) 후 3화면 왕복 동작

## FE-04 (D7~8) — 탐색 카드·역방향 판정

- [ ] `GET /api/explore/{sid}?v=` SSE: `plan` → `insight`(1건씩 추가) → `refine`(문장 교체) → `done`
- [ ] 탐색 인사이트 카드 (스펙 §0-8 가시성 원칙 — 리스크 #16 대응):
  - [ ] **세션 최초 1회 자동 펼침**, 이후 접힘 + "탐색 인사이트 {n}건" 카운트 배지
  - [ ] 카드 헤더에 plan 근거 1줄: "대화 맥락 기반: {축} 우선 검토"
  - [ ] T1 문장에 **진입 {N}곳 / 지속 안정 {N}곳 병기** 확인 (서비스 철학의 핵심 표기)
  - [ ] 모든 T1 하단 고정 고지 문구 (계약 `disclaimer: true`)
- [ ] 임의 상권 클릭 → `POST /api/check-area` → 판정 4단계 + 부족분 + 자격 부합 상품
      (source_quote 있으면 원문 인용 블록 표시)
- [ ] **도구 호출 로그 표시** (스펙 §5-2): 탐색 SSE 진행 중 오케스트레이터의 도구 호출
      로그를 접이식 영역에 노출 — "AI가 무엇을 계산했는지"의 가시화 (리스크 #16 대응)
- **DoD**: 데모 동선(README 도슨트 가이드) 3~4번 항목 재현 = CP3

## FE-05 (D9~10) — 검증 패널·반응성 (기능 동결 전 마감)

- [ ] 판정 옆 "왜? (검증 의견 {n}건)" 버튼 → 클릭 시 리스크 검증 반박 왕복 전개
      (기본 화면에는 카운트 배지만 — 산출 과정 흘러가는 연출 금지, 스펙 §5-3)
- [ ] 슬라이더 debounce 300ms + version 증가 전달, recommend·explore 동일 version 공유
- [ ] <100ms 재계산 체감 확인 (저성능 스로틀링 테스트)
- [ ] (P1-②) 프론티어 미니 차트: `done.frontier_points` 계단 차트 렌더
      + **기술설명서용 정적 이미지 내보내기** (미구현이어도 AI-07 배치 산출 차트로 문서 삽입 가능)
- [ ] **LLM 장애 폴백 상태 표시**: refine 미도착 시 템플릿 문장 유지(무표시), 검증 에이전트
      장애 시 "리스크 검증 생략" 플래그 노출 — 용어 사전(§0-4) 준수, BE-07 차단 QA와 합동 확인
- **DoD**: D10 기능 동결 시점에 전 P0 UI 완료

## FE-06 (D11~12) — 마감 스윕

- [ ] 스타일링 통일 (심사위원이 직접 만지는 화면 — 완성도가 채점 대상)
- [ ] **용어 컴플라이언스 전 화면 스윕**: "승인"·"조달 가능합니다"·"권장/추천드립니다"(자금 관련)
      금지어 grep + 육안 (CLAUDE.md 표)
- [ ] 고지 문구·데이터 기준일·추정치 라벨·권리금 "연간 조사(전년 기준)" 표기 확인
- [ ] LLM 차단 상태에서 화면 품질 확인 (BE-07 합동 — 템플릿 문장만으로 자연스러운지)
- [ ] **데모 이미지 3종 캡처** (1280×800, 데모 프로필): 화면 1·2·3 → `docs/images/demo-{1,2,3}.png`
- [ ] CM-04 외부 1인 3분 테스트 주관 → 실패 시 문구·화면 재조정
- **DoD**: README 이미지 교체 + 스윕 결과 기록

## PR 슬라이스 권장

| PR | 내용 | 시점 |
|---|---|---|
| 1 | FE-01 스파이크 정리 + SDK 셋업 | D2 |
| 2 | FE-02 화면 1 + API 클라이언트/타입 | D4 |
| 3 | FE-03 화면 2 (SSE) | D5 |
| 4 | FE-03 화면 3 (지도+패널+슬라이더) | D6 |
| 5 | FE-04 탐색 카드+역방향 | D8 |
| 6 | FE-05 검증 패널+반응성(+차트) | D10 |
| 7 | FE-06 스윕+캡처 | D12 |
