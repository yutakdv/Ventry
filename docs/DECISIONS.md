# 설계 확정 사항 기록

설계·계약상의 갈림길에서 무엇을 택했고 **왜 그 안이었는지**, 그리고 **어떤 조건에서
되돌리는지**를 결정 시점에 남긴 기록이다. 각 항목의 「반영」은 그 결정이 실제로 들어간 파일이다.

## 1. 판정 용어 = "유의" (enum `CAUTION`)

- **문제**: 스펙·탐색 스펙은 "유의", README·API 계약은 "정속"으로 갈라져 있었다.
  용어 컴플라이언스는 이 서비스의 하드 룰이다.
- **확정**: 판정 4단계 = `적합 / 조건부 적합 / 유의 / 범위 외`. API enum은
  `FIT / CONDITIONAL / CAUTION / OUT_OF_SCOPE` ("SLOW" 폐기).
- **근거**: "문서와 코드가 어긋나면 스펙 문서가 우선" + "유의"가 읽는 사람에게 자명한 표준 용어.
- **반영**: PROJECT_RULES.md · README.md · API_CONTRACT.md · `Verdict` enum · 화면 문구·마커 라벨.

## 2. 금액 단위 = 만원 단위 정수

- **문제**: 스펙 §6에 "원 단위 정수"로 오기 (계약·원칙 문서·모든 예시는 만원).
- **확정**: API 전 구간 **만원 단위 정수** (예: 8000 = 8,000만 원). 스펙 §6 정정 완료.
- **반영**: specs/최종_스펙문서.md §6.

## 3. `/api/scenarios` SSE 이벤트 스키마

- **확정**: `scenario` 이벤트(카드 1장씩, 2회) → `done` `{scenario_count}`. 하트비트 15s 공통.
- **반영**: API_CONTRACT.md 엔드포인트 2.

## 4. `/api/recommend` 응답에 `risk_review` 추가

- **문제**: "왜? (검증 의견 n건)" 패널(추천 판정 옆)의 데이터 원천이 계약에 없었음
  (`risk_review`는 check-area에만 존재).
- **확정**: recommend 응답에 `risk_review: {objection_text, applied, skipped}` 포함.
  리스크 검증 장애 시 `skipped: true` = "검증 생략" 플래그(용어 수위 §0-4 준수).
- **반영**: API_CONTRACT.md 엔드포인트 4.

## 5. `/api/diagnose` 응답에 `parse_source` 추가

- **확정**: `parsed_profile.parse_source: "llm" | "form_only"` — LLM 파싱 폴백(폼 값만 사용)
  여부를 FE가 표시·QA가 확인할 수 있게.
- **반영**: API_CONTRACT.md 엔드포인트 1.

## 6. LLM 제공자 = OpenAI API  (2026-07-21 변경 — 기존 Anthropic Claude 확정 대체)

- **확정**: 서빙(진단 파싱·탐색 plan·언어화 refine·리스크 반박) = **`gpt-4o-mini`**
  (저지연 — 타임아웃 5s·데모 반응성 제약에 정합). AI-06 배치 추출(정책자금 구조화) =
  **`gpt-4o`** (정확도 우선 — 전건 사람 검수 전 초안 품질이 검수 공수를 좌우).
- **키**: 루트 .env `OPENAI_API_KEY` 하나로 서빙·배치 공용 (SDK가 자동 인식하는 표준 변수명).
- **불변 원칙 유지**: 키 부재·장애 시 템플릿 폴백이 최종본(데모 무중단). LLM 수치 생성 금지.
- **변경 이력**: 2026-07-20 Anthropic Claude(haiku/opus)로 확정 → 2026-07-21 OpenAI로 변경.
  저지연 서빙 / 정확도 배치의 역할 매핑은 그대로 승계했다.
- **반영**: .env.example §5 · docker-compose.yaml.

## 7. 정책자금 원문 청크 저장 = Postgres 테이블 (pgvector 미도입)

- **근거**: source_quote는 매칭된 상품의 `doc_chunk_ref`로 원문 청크를 **id 직접 조회**하는
  구조(스펙 §5-4 "인용은 검색이지 생성이 아니다") — 유사도 검색이 필요 없으므로 벡터DB는
  과설계. compose에 컨테이너 추가 없이 기존 PostgreSQL로 충분.
- **확정**: `finance_doc_chunk` 테이블(chunk_id PK, 문서 메타(org·doc·date), 원문 text)로 적재.
  스펙의 "벡터DB(또는 청크 테이블)" 표기 중 후자 채택.
- **반영**: `01_schema.sql` DDL · 적재 배치 · assumptions.md 등재. 인용 조회는 단순 SELECT다.
- **표기 통일 (2026-07-25 추가)**: 이 결정 이후에도 스펙·태스크 문서에 「RAG 근거 인용」
  「벡터DB」 표기가 남아 있어, 없는 기술을 쓴다고 말하는 상태였다(리스크 #17). 전 문서를
  **「원문 근거 인용」·「청크 테이블 id 직접 조회」** 로 정정했다. 설계 변경이 아니라 본 결정과의
  동기화이며, 근거는 예상 Q&A 20번 문항에 정리했다.

## 8. 진단 폼 신규 3필드 — 수치는 구조화 필수 (2026-07-21, FE 제안 ①)

- **문제**: 화면 1이 받는 입력 중 재창업 여부·담보 제공 가능 여부·월 투자 가능액 세 가지가
  `form`에 자리가 없었다. FE는 이 값들을 `free_text`에 실어 보내는 우회안을 제시했다.
- **확정**: 세 필드 모두 `form`에 구조화한다.
  - `monthly_investable`(int, 만원) — **자유 텍스트 경유 불가**. "250만원"을 LLM이 250 /
    2,500,000 / 누락 중 무엇으로 뽑을지가 비결정적이며, 이는 LLM이 수치를 만드는 통로가 되어
    불변 원칙 §0-1과 정면 충돌한다. FE의 지적이 정확하다.
  - `is_existing_business`(bool) — 자격 정규칙 축이다. 스펙 §5-4의 인용 예시가 이미
    "만 39세 이하 **예비창업자**로서…"이므로 예비창업자 한정 상품을 거를 근거가 된다.
    `Eligibility.pre_startup_only`로 EligibilityFilter에 즉시 결선.
  - `collateral_available`(bool) — 스펙에 담보 축이 없어 **현재 판정에 쓰지 않는다.** 프로필로
    기록만 하고 조달 검증의 보조 입력으로 예약한다. 계약 문서에 "미사용"을 명시해
    "받아놓고 안 쓰는 필드"임을 숨기지 않는다.
- **미사용 필드 처리 원칙**: `monthly_investable`은 한계 조달의 월 상환액 m에 대한 상환 여력
  상한(m ≤ monthly_investable)으로 결선하며, 그 가정은 assumptions.md에 등재한다.
- **반영**: API_CONTRACT.md 엔드포인트 1.

## 9. 예산 프리뷰의 주체 = `POST /budget` 응답 (2026-07-21, FE 제안 ②)

- **문제**: 화면 2의 예산 슬라이더가 즉시 프리뷰(예상 후보 수·임대료·유동인구)를 그리는데
  `POST /budget`에 응답 스키마가 없었다. FE가 "/budget이 주는가, 프론트가 /recommend를
  재호출하는가"를 아키텍처 결정 사항으로 물었다.
- **확정**: **`POST /budget` 응답이 프리뷰를 준다** (FE 선호안과 동일).
- **근거**: `/recommend`는 세션의 확정 예산 B₀로 계산한다. 화면 2 시점에는 B₀가 아직 없어
  자기자본으로 폴백하므로, 슬라이더가 가리키는 임의 예산의 프리뷰를 **원리적으로** 만들 수
  없다. `/recommend?budget=` 쿼리로 우회하면 예산의 진실 원천이 세션과 쿼리로 이중화되어
  `/explore`(세션 B₀ 기준)와 어긋난다.
- **부수 확정**: B₀는 덮어쓰기다. 화면 2 슬라이더는 debounce 후 `POST /budget`을 반복 호출하고
  마지막 값이 확정값이 된다. 화면 3 슬라이더는 스펙 §7·expl §5대로
  `POST /budget` → `/recommend?v=`·`/explore?v=`(동일 version) 순서를 유지한다.
- **반영**: API_CONTRACT.md 엔드포인트 3.

## 10. `/recommend` 정렬·필터 = 클라이언트 처리 (2026-07-21, FE 제안 ③)

- **확정**: 응답 필드는 FE 요청분(`score`·`total_count`·`summary` + area별 `monthly_rent`·
  `est_sales`·`daily_floating`)을 **전부 추가**하되, 정렬·필터 **쿼리 파라미터는 두지 않는다.**
- **근거**: ① 정렬·필터에 필요한 값이 모두 응답에 실리므로 프론트가 왕복 0회로 처리할 수 있고,
  그편이 스펙 §7의 "<100ms 재계산 체감" 목표에 유리하다. ② 서버 정렬·필터를 두면
  `total_count`·`summary`의 기준 집합이 파라미터마다 달라져 계약이 복잡해진다.
- **재검토 시점**: 실데이터 전환으로 후보 풀이 커져 페이징이 필요해지면 그때 도입한다.
- **부수 정정**: `burden_ratio`를 픽스처 상수에서 **환산임대료 ÷ 추정매출 파생**으로 바꿨다.
  스펙 §4-2의 정의 그대로이며 배치 산출물(부담률 분모·분자)과도 정합한다.
- **반영**: API_CONTRACT.md 엔드포인트 4 · assumptions.md #8.

## 11. 결과 저장·PDF 보고서 = 스코프 외 유지 (2026-07-21, FE 제안 ⑥)

- **확정**: 결과 저장·PDF 상담 리포트·공유 링크·저장 목록 API를 **신설하지 않는다.**
  프론트도 해당 화면(Step 5)을 만들지 않는다.
- **근거**: 스펙 §0-2가 "PDF 상담 리포트"를 P2로 명시 이월했고, P2는 "생각하지 못한 것이 아니라
  의도적으로 미룬 것"으로 로드맵에 서술하는 항목이다. 남은 공수는 P0 미완료분에 배정한다.
- **반영**: API_CONTRACT.md "스코프 외" 절.

## 12. 데이터 수집·검증 확정 5건 (2026-07-25)

정책자금 수집·검증에서 열려 있던 5건을 확정하고 근거를 문서에 남겼다. 각 결정은 "되돌리는
조건"을 함께 적어, 전제가 바뀌면 문서를 고치고 코드를 되돌리는 경로를 열어 둔다.

- **D-A. 소진공 변동금리 `rate` 를 공시 표값으로 채운다** (#72)
  - 근거: 융자공고가 지목한 「금리안내」 표(‘26년 3/4분기 기준금리 3.85%)가 이미 수집 원문
    `소진공_소상공인정책자금_지원사업안내.txt` 에 수록돼 있다. 표의 「금리」 열 값을 그대로 옮기며,
    기준금리+가산을 직접 계산하지 않는다. 2차 출처(블로그)는 §0-1 미충족으로 미채택.
  - 되돌리는 조건: 분기가 바뀌면(1·4·7·10월 10일) 값이 낡는다 → `rate_note` 의 적용 분기를 보고
    재수집·갱신한다. 갱신 전까지는 표기된 분기 기준으로 읽는 것이 정확하다.
  - 반영: assumptions #34 · `20_finance.sql` rate NULL 14→7.

- **D-B. 골드셋 수치는 제거가 아니라 의미 정렬로 고친다** (#72)
  - 근거: 서울신보 골드의 `rate` 는 **보증료율**이었다. 값이 틀린 게 아니라 필드 의미가 어긋난
    것이므로, 재수집 원문의 `[보증료]`/`[대출금리]` 라벨로 전건 대조해 재배치했다.
  - 되돌리는 조건: `rate` 의 정의(월 상환액용 연 대출금리)가 바뀌면 전면 재검토.
  - 반영: assumptions #33.

- **D-C. 서울신보는 OCR이 아니라 웹 재수집으로 해결한다** (#72)
  - 근거: tesseract 미설치이며 설치는 심사 환경 재현성을 해친다. 웹 본문은 인쇄본보다 오히려
    1차 출처에 가깝고 §5-4("인용은 검색이지 생성이 아니다")에 정합한다.
  - 되돌리는 조건: 서울신보가 페이지 구조를 바꾸면 `funding_web.py` 의 본문 앵커를 조정한다
    (텍스트를 손으로 고치지 않는다 — 원문 verbatim 원칙).
  - 반영: assumptions #32.

- **D-D. 검증 모델의 프로토콜·게이트를 모델 코드보다 먼저 커밋한다**
  - 근거: §12-3의 방어선은 임계치가 아니라 **"결과를 보기 전에 등록했다"는 커밋 순서**다.
    순서가 뒤집히면 게이트의 심사 가치가 0이 된다(리스크 #19 — 사후 조정 금지).
  - 되돌리는 조건: 없음. 프로토콜 변경이 불가피하면 **실행 전** assumptions 재등록만 허용한다.
  - 반영: assumptions #35, 커밋 순서.

- **D-E. 창업비용 단위·가맹점 편의 판정을 확정 종료한다**
  - 근거: 근거가 assumptions #38~#41에 등재됐고 코드·라벨 반영도 끝났다.
  - 되돌리는 조건: 원 통계(KOSIS 음식점업 총액) 대조에서 단위 판정이 뒤집히면 재개.
  - 반영: assumptions #38~#41 · `load/serving.py` 의 `data_source_meta` 라벨.

---

## 13. 실테이블 결선 전제 2건 — 금리 미상 상품 · 시나리오 상품 선택 (2026-07-26)

`finance_product` 26건을 서빙에 결선(#87)하기 전에 결정해야 하는 두 건. 실데이터를 물리는 순간
표면화되는 문제라 결선과 같은 PR에서 반영한다.

### 13-1. 확정 이율이 없는 상품(7건) — 조달 조합 제외 · 자격 목록 노출

적재 26건 중 **7건이 `rate = NULL`** 이며, 전부 "은행금리에서 X% 차감" 형태라 **절대 금리를
알 수 없다**(F-002·F-004·F-005·F-006·F-008·F-010·F-011). 기준금리를 지어내지 않는다는 원칙은
이미 assumptions #34 에 있다.

- **조달 시나리오 편성에서는 제외한다.** 월 상환액 m을 계산할 수 없으면 상환 여력 상한
  (`m ≤ monthly_investable`, assumptions #23)을 검증할 수 없고, 검증하지 못한 조달을 기회로
  제시하는 것은 하향 안전 마진 원칙에 어긋난다. 구현은 `FundingProduct.hasKnownRate()` 게이트
  (`FundingCheck.coverWithReason`, 실패 사유 `RATE_UNKNOWN`).
- **자격 요건 부합 상품 목록에는 남긴다.** 자격 축은 연령·업종·지역·예비창업이지 금리가 아니다.
  금리를 모른다는 이유로 자격 자체를 부정하면 서울신보 주력 보증상품 6건이 화면에서 사라진다.
  DTO 는 `rate=null` + `rate_type="variable"` + `rate_note` 원문을 그대로 실어 화면이
  **"금리 미공시(한도·승인은 기관 심사 사항)"** 로 표기한다.
- 기각한 안: **대표금리 대입** — 수치 조작에 해당해 스펙 §0-1 위배. 논의 대상이 아니다.
- 되돌리는 조건: 기관이 절대금리를 공시하면 검수본에 반영해 `rate` 를 채운다.
- 반영: `EligibilityFilterTest.unknownRateProduct_stillQualifies_soItCanBeListed` ·
  `CoverOutcomeTest` (`RATE_UNKNOWN`) · 이슈 #82

### 13-2. 조달 시나리오 상품 선택 = 「필요분 최소 커버」

현행 규칙(assumptions #25)은 **"보수 = 보증형 중 한도 최대 / 적극 = 전체 중 한도 최대"** 다.
데모 픽스처(한도 1,500·3,000)에서는 문제가 없었으나 실테이블에는 **한도 8억 보증상품이 3건**
(F-004·F-007·F-008) 있어, 그대로 결선하면 소상공인 카페 창업에 **8억 보증을 제시**하게 되고
데모 서사(보수 6,500 / 적극 8,000, expl §8)도 성립하지 않는다.

- **개정**: 보수 = 보증형 중 **부족분을 커버하는 최소 한도** 상품 / 적극 = 전체 중 **부족분을
  커버하는 최소 한도** 상품. 부족분 = 필요 예산 − 자기자본. 커버하는 상품이 없으면 그중
  한도 최대로 폴백한다.
- 근거: 이 서비스가 답하는 질문은 **"내 한도로 어디까지 가능한가"** 이지 "얼마나 크게 빌릴 수
  있는가"가 아니다. 필요분을 넘는 조달을 기본값으로 제시하는 것은 상향 인사이트를 단독 노출하지
  않는다는 원칙(PROJECT_RULES §2)과도 어긋난다.
- 기각한 안: **한도 상한 캡(부족분 × N)** — N의 근거가 없어 θ처럼 "통용 기준" 후퇴가 또 필요해진다.
- 되돌리는 조건: 사용자가 의도적으로 상향을 요청하는 동선이 생기면 그 경로에서만 한도 최대를 쓴다.
- 반영: assumptions #25 개정 · `ScenarioBuilder` · 이슈 #87

---

### 13-3. 시나리오 카드 — 라벨은 '수단', 초기값은 '필요분' (2026-07-26, 이슈 #90)

§13-2 를 실데이터에 적용해 기동한 결과 두 가지가 드러났다. 둘 다 규칙의 결함이 아니라
**가용 상품 구조가 그렇다는 사실**이었고, 왜곡하지 않는 쪽으로 정리했다.

**관측** (데모 프로필: 자기자본 5,000 · 카페 · 서울 마포구)

| 카드 | 예산 범위 | 편성 |
|---|---|---|
| 보수 | 5,000 ~ 15,000 | KB소상공인 보증서대출 10,000만 (연 2.1%) |
| 적극 | 5,000 ~ 10,000 | 대환대출 5,000만 (연 4.5%) |

필요분은 보수 1,713만·적극 3,398만인데(카페 진입 비용 중앙값 권리금 제외 6,713·포함 8,398),
**확정 이율이 있는 보증형의 최소 한도가 1억**이라 보수 카드가 더 큰 예산을 갖게 됐다.

**결정 ①  라벨은 금액의 대소가 아니라 수단이다.**
보수 = 보증형만, 적극 = 전체 상품. 두 카드의 후보 풀이 다르므로 **적극 ≥ 보수라는 보장이 없다**.
크기 순서를 억지로 맞추려면 (a) 적극에 하한을 걸어 보수와 동일해지거나 (b) 필요분을 넘는
상품을 일부러 고르게 되는데, 둘 다 사실을 왜곡한다. 화면은 두 카드를 **수단의 차이**로 서술한다.

**결정 ②  슬라이더 초기값 = `budget_min` + 필요분.**
구 규칙은 `budget = budget_max`(한도 전액 활용 가정)였다. 가용 상품의 최소 한도가 필요분보다
크면 **과잉 조달이 기본 선택**이 된다 — 위 사례에서 보수 카드의 기본값이 1.5억이 된다.
초기값을 필요분에 맞추면 실측 보수 6,713 · 적극 8,398 로 시장 진입 비용 중앙값에 안착한다.
상한은 그대로 노출하므로 사용자가 올릴 수 있고, 상향은 여전히 사용자의 선택이다.

- 계약 영향: `budget` 필드의 **값 의미 변경**(타입·이름 불변). `API_CONTRACT.md` 갱신 + FE 공지.
- 기각한 안: 적극 한도 ≥ 보수 한도 불변식 — 실데이터에서 두 카드가 동일해져 카드가 무의미해진다.
- 되돌리는 조건: 소액 보증상품이 적재되어 보증형 최소 한도가 필요분 수준으로 내려오면 재검토.
- 반영: `ScenarioBuilder` · `ScenarioBuilderTest` · assumptions #25 · 이슈 #90

**~~미해결로 남긴 것~~ — 대환대출 편성** (#90 문제 2) → **2026-07-26 해소, §15 참고**
적극 카드가 대환대출을 골랐다. 기존 대출을 갈아타는 상품이라 예비창업자 맥락에 맞지 않으나,
`finance_product` 에 "기존 대출 보유자 한정"을 나타낼 축이 없었다(`pre_startup_only` 는 반대 방향).
당시에는 기능 동결이 임박해 스키마 변경을 미뤘다. 임시로 상품명 블랙리스트를 넣는 것은
자격 판정을 코드에 숨기는 것이라 그때도 채택하지 않았고, 결국 **자격 축으로 표현**했다.

---

## §14. 원문 근거 인용은 「이월」이 아니라 「구현」으로 확정 (2026-07-26)

**결정**: 스펙 §5-4가 기능 동결 시점에 확정하도록 남겨 둔 구현/이월 이원화를
**구현**으로 확정한다.

- **판정 ①** `finance_product` 26건 서빙 결선 → **충족**
- **판정 ②** `source_quote` 원문 인용 → **충족**

**「구현」의 기준을 어디에 두었나** — 필드가 채워지는 것이 아니라 **인용문이 실제로 그 상품의
근거인 것**에 두었다. 결선 직후 실기동해 보니 26건 중 13건의 인용문이 공고 본문이 아니라 웹
페이지 내비게이션·PDF 인쇄 잔재로 시작했다. 원문의 정확한 부분문자열이므로 verbatim 지표는
1.000 이었지만, 그 상품의 근거가 아닌 문단을 근거로 지목하는 **귀속 오류**였고 유사도 검색
대신 id 직접 조회를 택한 논거(심사_QA 20)를 정면으로 부정하는 상태였다. 청킹을 고쳐
13 → 0건으로 해소한 뒤에 충족으로 판정했다 (assumptions #60·#61).

**서버는 인용문을 자르지 않는다** — 요약·재작성 금지에 더해 길이 자르기도 하지 않는다.
바이트 동일성이 「인용은 검색이지 생성이 아니다」의 유일한 증명 수단이기 때문이다. 길이
편차(19~1,863자)는 화면 클램프가 떠안는다. 대안(서버 절단 + 말줄임)은 §5-4 주장에 예외
조항을 만들어 방어해야 하므로 채택하지 않았다.

**이월** — 근거문 캐시·개인화 언어화는 P1 잔여로 이월한다. §5-4 원문 인용과 별개 기능이다.

---

## §15. 자격 축에 `existing_business_only` 를 추가한다 (2026-07-26, #90 문제 2 해소)

**결정**: `finance_product` 에 `existing_business_only BOOLEAN NOT NULL DEFAULT FALSE` 를 추가하고,
공고문이 **기존 사업/기존 대출 보유를 명시한 상품**에만 `true` 를 준다. `pre_startup_only` 의
반대 축이다.

**왜 지금인가**: 실데이터 데모에서 **적극 카드가 예비창업 프로필에 대환대출을 편성**했다.
가설이 아니라 화면 2에 그대로 보이는 상태였다. 애초에 이월한 이유가 「동결 임박」이었는데
일정 제약이 해제되면서 전제가 사라졌다.

**왜 블랙리스트가 아닌가**: 상품명으로 거르면 자격 판정이 코드에 숨는다. 자격은 데이터이고,
데이터로 표현해야 골드 대조·재검수·근거 제시가 전부 성립한다.

**어디에 붙였나 (4건)** — 근거는 전부 적재된 공고 원문에 있다.

| 상품 | 공고문 근거 |
|---|---|
| F-016 대환대출 | "중‧저신용 소상공인이 **보유한** 은행권·비은행권의 7% 이상 고금리 **대출**" |
| F-017 재도전특별자금 (일반형) | "**재창업** 준비단계, **재창업** 초기단계, 채무조정 소상공인" |
| F-018 재도전특별자금 (희망형) | "희망리턴패키지 **재기사업화** 선정기업" |
| F-019 재도전특별자금 (도약형) | "성실상환 **재창업** 소상공인" |

**보수적으로 4건에만 붙였다.** 긴급경영안정자금(매출감소 15% 확인)이나 신용취약소상공인자금도
사실상 영업 중인 사업자를 전제하지만, 공고문이 「기존 사업 보유」를 자격 요건으로 **명시**하지는
않는다. 넓게 붙이면 소진공 자금 대부분이 예비창업자에게서 사라져 **실제 제도보다 좁게** 답하게
된다 — 확인하지 못한 제약을 부여하지 않는 것도 확인하지 못한 자격을 부여하지 않는 것과 같은 원칙이다.

**결과** (실기동 실측)

| 프로필 | 자격 부합 | 적극 카드 |
|---|---|---|
| 예비창업자 | 25건 → **21건** | 대환대출 → **긴급경영안정자금(일시적 경영애로)** |
| 기존 사업자 | **25건** (변동 없음) | **대환대출** (정당한 편성) |

**후속 (2026-07-27) — 「4건에만」이 좁았다.** 위 조치 후 적극 카드 자리를 `F-014 긴급경영안정자금`
(「매출 15% 감소」= 매출 이력 전제)이 물려받았다. 상품만 바뀌고 **증상은 그대로**였다는 뜻이다.
보수적으로 붙인다는 판단 자체가 축의 판정 기준을 정하지 않은 채 내려져 있었으므로, 기준을
명문화하고 전건을 다시 읽었다.

- **판정 기준**: 공고 원문에 업력·가동의 **하한**(「영업 중」·「매출 발생」·「기존 대출 보유」)이
  명시되면 `existing_business_only = TRUE`. 「1년 이내」·「3년 미만」 같은 **상한**은 반대 방향이므로
  붙이지 않는다(F-011·F-021은 그대로 FALSE).
- **결과**: `existing_business_only` 4 → **12건**. 프로필로 확인할 수 없는 대상 한정 요건
  (장애인기업·사회적경제기업·인증기업·고용우수기업)은 `target_group` 축을 신설해 분리했다(5건).

| 프로필 | 자격 부합 (07-26) | 자격 부합 (07-27) |
|---|---|---|
| 예비창업자(데모) | 21건 | **11건** |

축이 코드가 아니라 데이터에 있으므로, 빠진 상품마다 근거 문장을 공고 원문에서 되짚을 수 있다.
매칭 골드는 P/R/F1 **1.000 을 유지**한다(기대 라벨을 사람이 다시 적었다).

매칭 골드는 P/R/F1 **1.000 을 유지**했고 TN 이 87 → 127 로 늘었다(예비창업 프로필 10종 × 4건).

---

## §16. 기능 동결 직전 코드리뷰 조치 4건 (2026-07-29)

기능 동결일에 서빙·프론트·배치·QA 4개 영역을 훑은 코드리뷰가 「설계 결정 필요」로 남긴
항목들이다. 결정했다는 사실이 문서에 남아야 코드와 주석이 서로 다른 말을 하지 않는다.

### ① 동기 API 의 LLM 대기 — (a) 동기 경로 타임아웃 분리를 채택

**문제**: `/api/recommend`·`/api/check-area` 가 톰캣 워커 스레드에서 LLM 을 최대 5초 기다린다.
`SseSupport` 자신의 주석이 「톰캣 워커 스레드에서 LLM 대기 금지」라 적어 두었는데 동기 API 2종만
그 원칙 밖에 있었고, 하필 그 경로가 슬라이더가 움직일 때마다 재호출되는 곳이다(스펙 §7
「<100ms 재계산 체감」).

**결정**: `LlmSettings.SYNC_TIMEOUT = 1.2s` 를 신설해 `ReviewGenerator` 만 짧은 상한을 쓴다.
SSE 계열(plan·refine)은 기존 5초 그대로다.

**왜 (b) 사전 계산도 (c) 계약 변경도 아닌가**: (b) 는 `POST /budget` 시점에 반박문을 미리 데우는
방식이라 구현량이 중간이고 **새 실패 모드**(예열 실패 시의 동작)를 만든다. (c) 는 `risk_review`
를 응답 필수 필드에서 빼는 계약 변경이라 동결 이후 절차 비용이 크다. 반면 (a) 는 상수 하나이고,
상한을 넘겼을 때의 동작이 **기존 폴백과 완전히 동일**하다 — `skipped=true` + 템플릿이 최종본.
새 실패 모드가 생기지 않는 것이 결정적이었다. 1.2초는 저장소의 기존 실측(캐시 히트 수 밀리초 /
미스 약 1.9초)에서 **빠른 왕복은 살리고 느린 왕복만 버리는** 자리다.

### ② `?v=` version 서버 발급은 하지 않는다 — 상한 검증으로 수용

**문제**: version 은 클라이언트가 주고 서버가 `max(cur, v)` 로만 올리므로 되돌릴 수 없다.
큰 값 하나로 그 세션의 `/explore` 를 TTL 60분 동안 무응답으로 만들 수 있다.

**결정**: 상한 검증(`0 ≤ v ≤ 1,000,000`, 위반 시 400)으로 **수용**한다. version 을 서버가
발급하도록 바꾸는 근본 해소는 **하지 않는다**.

**근거**: ① 근본 해소는 계약 §공통 규약의 변경이고 프론트의 슬라이더 경로 전체가 그 규약 위에
있다 — 예선 스코프에서 건드릴 표면이 아니다. ② 도달 조건이 **세션 id 를 아는 제3자**다. 세션 id 는
UUIDv4 이고 정상 FE 는 version 을 되돌리지 않으므로 **정상 동선에서는 재현되지 않는다**.
③ 상한 검증이 피해 범위를 줄인다(상한 안의 값으로도 같은 독점은 가능하다 — 그 한계를 함께 적는다).
④ 이 서비스는 로그인이 없고 세션 소유권 검증도 없는 예선 스코프다 — version 만 서버 발급으로
바꿔도 같은 계열의 다른 경로가 남는다. **인증과 함께 다룰 항목으로 이월한다.**

### ③ 새로고침 세션 소실 — 안내만 붙이고 영속화하지 않는다

**결정**: 세션 가드가 `/diagnose` 로 되돌릴 때 라우터 state 를 실어 「이전 진행 내용이 남아 있지
않아 처음부터 시작합니다」를 노출한다. `sessionStorage` 영속화는 **하지 않는다**.

**근거**: 영속화는 개인 자금 정보(나이·자기자본)를 브라우저에 남기는 일이라 저장 항목·만료·
개인정보 문구를 함께 손봐야 하고, 화면 1의 「입력한 정보는 서버에 저장하지 않습니다」와의 관계도
다시 정리해야 한다. 「개인정보를 수집·저장하지 않는다」는 이 서비스의 서술 중 하나이고, 그
사정거리를 흔들 이유가 없다. 사고의 실질(F5 한 번에 처음부터)은 **안내 한 줄로 설명되면
사용자에게 충분**하고, 데이터는 남기지 않는 편이 이 프로젝트의 서사와 일관된다.

### ④ 언어화(refine)의 순서 뒤바뀜 — (B) 증감 어휘 × 대소 관계 교차 검증

**결정**: 수치 집합 비교를 순서 비교로 바꾸지 않는다. 대신 「A에서 B로 늘어납니다」 꼴에서
증감 어휘와 두 수의 대소가 모순이면 폐기한다.

**근거**: (A) 순서까지 동일 요구는 「1,014곳으로, 382곳에서」 같은 정상적 어순 변경을 폐기해
**언어화의 자유도 자체를 없앤다** — 그러면 refine 을 두는 의미가 줄어든다. 그 자유도는
`RefinePrompt` 가 명시적 설계 결정으로 적어 둔 것이라 되돌리려면 근거가 더 필요하다.
(B) 는 설계 결정을 보존하면서 **뜻이 반대가 되는 경우 하나만** 잡고, 해당 꼴이 없는 문장에는
아무것도 판단하지 않아 정상 문장을 걸러낼 여지가 좁다.
