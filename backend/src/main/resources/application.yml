server:
  port: 8080

spring:
  application:
    name: ventry-api
  # BE-02 (D4~5) JDBC 도입 시 활성화 — compose가 DB_URL/DB_USER/DB_PASSWORD 환경변수를 주입한다.
  # datasource:
  #   url: ${DB_URL:jdbc:postgresql://localhost:5432/ventry}
  #   username: ${DB_USER:ventry}
  #   password: ${DB_PASSWORD:ventry-local}

management:
  endpoints:
    web:
      exposure:
        include: health
